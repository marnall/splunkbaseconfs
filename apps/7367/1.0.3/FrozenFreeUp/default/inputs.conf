[script://./bin/ProcessFrozen.py]
# In this, the stanza specifies how often to call the script to free up the forzen

python.version = python3

# change to false to start the input, requires a restart
disabled = true           

# frequency to run the script, in seconds
interval = 86400