# Splunk source parsing configuration file

[bitglass]

# CHARSET = <string> UTF-8
# TRUNCATE = <non-negative integer> 10000           # Set to 0 - no truncation?
# LINE_BREAKER = <regular expression> ([\r\n]+)
# LINE_BREAKER_LOOKBEHIND = <integer> 100           # Try increasing to max event length if there are still problems

# Need to override the default (true) to cancel the special line merging logic b/c it still causes occasional merging
SHOULD_LINEMERGE = false

