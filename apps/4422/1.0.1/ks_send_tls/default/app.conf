[install]
state = enabled
is_configured = 0

[launcher]
author = Kudelski Security CFC
description = Send alerts to an endpoint via TLS
version = 1.0.1

[package]
id = ks_send_tls

[ui]
is_visible = 0
label = KS send alerts via TLS
