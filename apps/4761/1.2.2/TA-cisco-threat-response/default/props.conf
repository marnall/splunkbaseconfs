[cisco:tr:data]
SHOULD_LINEMERGE = 0
category = Splunk App Add-on Builder
pulldown_type = 1
KV_MODE = JSON
TRUNCATE = 0
# Disabling line merging allows
# us to submit multiple json events
# to the streaming receiver in one call.
# Be aware this requires each json event
# to be formatted on a single line.
# SHOULD_LINEMERGE=false

[source::...ta-cisco-threat-response*.log*]
sourcetype = ta:cisco:threat:response:log

[source::...ta_cisco_threat_response*.log*]
sourcetype = ta:cisco:threat:response:log

