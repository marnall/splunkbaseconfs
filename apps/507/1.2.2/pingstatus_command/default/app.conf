#
# Splunk app configuration file
#

[ui]
is_visible = 0
label = PING Status


[launcher]
author = Nimish Doshi
version=1.2.2
description = pingstatus command returns realtime ping status (delay) for an address field. The field could be a DNS name or IP address mapped to an URL field.

[package]
id=pingstatus_command


