[kpi_getEventtypes]
definition  = rest /services/admin/eventtypes  |search NOT eai:acl.sharing=system| table title | sort title

[kpi_getFieldsByEventtype(1)]
args = evttype
definition = eventtype=$evttype$ | head 1000 |  stats dc(*) as * | transpose | table column
