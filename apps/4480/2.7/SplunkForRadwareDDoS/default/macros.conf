[Request_Status_over_time_by_webapp(1)]
args = ServerName
definition = timechart count by policy

[Violations_distribution_over_time_by_webapp(1)]
args = ServerName
definition = timechart count by attack_name 

[Attack_type_distribution_over_time_by_webapp(1)]
args = ServerName
definition = timechart count by attack_category

[Top_HTTP_methods(1)]
args = ServerName
definition = top method limit=20

[Top_HTTP_Host(1)]
args = ServerName
definition = top policy limit=20

[Top_Object_count_ip_to_country(1)]
args = ServerName
definition = iplocation src_ip | rename src_ip as SourceIP | stats count by SourceIP Country | sort -count

[Top_Object_by_webapp_and_device(1)]
args = ServerName
definition = top http_security_violation limit=20

[Top_attack_types(1)]
args = ServerName
definition = top attack_category limit=20

[Top_severities(1)]
args = ServerName
definition = top risk limit=20

[Top_attacking_countries(1)]
args = ServerName
definition = iplocation src_ip | top Country limit=10

[Top_attacking_IPs(1)]
args = ServerName
definition = rename src_ip as SourceIP | top SourceIP limit=20

[Top_signatures(1)]
args = ServerName
definition = top attack_category limit=20

[Violations_count_by_URIs(1)]
args = ServerName
definition = stats count(attack_category) as #_Violations by dst_ip | sort -#_Violations

[Events_distribution_by_web_app(1)]
args = ServerName
definition = timechart count by policy limit=20

[Top_attacked_web_apps(1)]
args = ServerName
definition = top policy limit=20

[Attacked_IPs(1)]
args = ServerName
definition = stats max(policy)

[Anomalies_ip_to_country(1)]
args = ServerName
definition = iplocation src_ip | stats max(attack_category) as Vulnerability by Country


