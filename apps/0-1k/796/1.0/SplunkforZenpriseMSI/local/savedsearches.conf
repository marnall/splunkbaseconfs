
[ActiveSync Connections by Device Type]
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
search = sourcetype="syslog" | stats count by devicetype | sort -count
vsid = *:qtb61mkc

[ActiveSync Connections by User]
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
search = sourcetype="syslog" | stats count by user | sort -count
vsid = *:su2mfdoo

[Denied ActiveSync Connections by User, IP]
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
search = sourcetype="syslog" action=*deny* | stats count by user,ip | sort -count
vsid = *:sksq36sw

[Allowed ActiveSync Connections by Username and IP]
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
search = sourcetype="syslog" action=*allow* | stats count by user,ip | sort 10 -count
vsid = *:2ci7fcpp

[ActiveSync Connections by IP]
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
search = sourcetype="syslog" |  stats count by ip | sort 10  -count
vsid = *:kf7f7lsr

[Total Devices - Last 24 Hours]
cron_schedule = 0 0 * * *
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
search = sourcetype=syslog | stats dc(deviceid)

[Total Unique Users - Last 24 Hours]
cron_schedule = 0 0 * * *
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
search = sourcetype=syslog | stats dc(user)

[Top 10 Rogue devices]
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
search = sourcetype="syslog" action=*deny* | stats count by  user,deviceid,devicetype | sort 10 -count
