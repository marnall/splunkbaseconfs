<view template="dashboard.html" onunloadCancelJobs="False">

    <label>Overview</label>

    <module name="AccountBar" layoutPanel="appHeader"/>

    <module name="WindowsMonitoringCommon" layoutPanel="appHeader" />

    <module name="WindowsMonitoringAppBar" layoutPanel="appHeader"/>

    <module name="Message" layoutPanel="messaging">

        <param name="filter">*</param>

        <param name="clearOnJobDispatch">False</param>

        <param name="maxSize">1</param>

    </module>

    <module name="WindowsMonitoring_FTR" layoutPanel="viewHeader">

        <param name="configLink">setup</param>

    </module>

    <module name="WindowsMonitoringContent" layoutPanel="panel_row3_col1">

      <param name="style"></param>

      <param name="content">

          <![CDATA[

          <h1>Windows events</h1>

      ]]>

      </param>

    </module>

    <module name="HiddenSearch" autoRun="True" layoutPanel="panel_row3_col1_grp1">

        <param name="search">| inputlookup windows_event_system.csv | dedup Host | stats count</param>

        <module name="SingleValue">

            <param name="field">count</param>

            <param name="afterLabel">Hosts</param>

            <param name="additionalClass">totalButton</param>

            <param name="linkFields">result,afterLabel,labelafter</param>

            <param name="linkView">flashtimeline</param>

            <param name="linkSearch">eventtype="windows_events" earliest=@d | stats values(LogName) as "Event Logs" by host</param>

        </module>

    </module>

    <module name="WindowsMonitoringSearch" autoRun="true" layoutPanel="panel_row3_col1_grp2">

      <param name="search">| inputlookup windows_event_details.csv</param>

      <module name="WindowsMonitoringPostProcess" layoutPanel="panel_row3_col1_grp2">

          <param name="search">| dedup LogName | stats count</param>

          <module name="SingleValue">

              <param name="field">count</param>

              <param name="afterLabel">Log Names</param>

              <param name="additionalClass">totalButton</param>

              <param name="linkFields">result,afterLabel,labelafter</param>

              <param name="linkView">flashtimeline</param>

              <param name="linkSearch">| inputlookup windows_event_details.csv | dedup LogName | table LogName | sort LogName</param>

          </module>

      </module>

      <module name="WindowsMonitoringPostProcess" layoutPanel="panel_row3_col1_grp3">

          <param name="search">| stats count</param>

          <module name="SingleValue">

              <param name="field">count</param>

              <param name="afterLabel">Event IDs</param>

              <param name="linkFields">result,afterLabel,labelafter</param>

              <param name="additionalClass">totalButton</param>

              <param name="linkView">flashtimeline</param>

              <param name="linkSearch">| inputlookup windows_event_details.csv | table LogName, SourceName, TaskCategory, EventCode | sort LogName, TaskCategory, EventCode</param>

          </module>

      </module>

    </module>

    <module name="WindowsMonitoringContent" layoutPanel="panel_row4_col1">

      <param name="style"></param>

      <param name="content">

          <![CDATA[

          <h1>Windows performance counters</h1>

      ]]>

      </param>

    </module>

      <module name="HiddenSearch" autoRun="True" layoutPanel="panel_row4_col1_grp1">

          <param name="search">| inputlookup windows_perfmon_system.csv | stats count</param>

          <module name="SingleValue">

              <param name="field">count</param>

              <param name="afterLabel">Hosts</param>

              <param name="additionalClass">totalButton</param>

              <param name="linkFields">result,afterLabel,labelafter</param>

              <param name="linkView">flashtimeline</param>

			<param name="linkSearch">eventtype="windows_performance" object=* earliest=@d | stats dc(counter) as dccount by object, Host | eval comb=object." (Total Counters = ".dccount.")" | stats values(comb) as Perfmon_Counter_Category by Host | table Host, Perfmon_Counter_Category

			</param>

          </module>

      </module>

      <module name="WindowsMonitoringSearch" autoRun="True" layoutPanel="panel_row4_col1_grp2">

        <param name="search">| inputlookup windows_perfmon_details.csv</param>



        <module name="WindowsMonitoringPostProcess" layoutPanel="panel_row4_col1_grp2">

            <param name="search">| dedup object | stats count</param>

            <module name="SingleValue">

                <param name="field">count</param>

                <param name="afterLabel">Objects</param>

                <param name="additionalClass">totalButton</param>

                <param name="linkFields">result,afterLabel,labelafter</param>

                <param name="linkView">flashtimeline</param>

                <param name="linkSearch">eventtype="windows_performance" object=* earliest=@d | stats dc(counter) as Number by object, host | sort -Number</param>

            </module>

        </module>

        <module name="WindowsMonitoringPostProcess" layoutPanel="panel_row4_col1_grp3">

            <param name="search">| dedup counter | stats count</param>

            <module name="SingleValue">

                <param name="field">count</param>

                <param name="afterLabel">Counters</param>

                <param name="linkFields">result,afterLabel,labelafter</param>

                <param name="additionalClass">totalButton</param>

                <param name="linkView">flashtimeline</param>

                <param name="linkSearch">| inputlookup windows_perfmon_details.csv | eval Perfmon_Counter=counter | eval Perfmon_Counter_Category=object | dedup Perfmon_Counter, instance | table Perfmon_Counter_Category, Perfmon_Counter, instance | sort Perfmon_Counter_Category, Perfmon_Counter, instance</param>

            </module>

        </module>

      </module>

    

  <module name="StaticContentSample" layoutPanel="panel_row5_col1" group="All indexed data">

      <param name="text">This lists all of the data you have loaded for Windows Data. <![CDATA[<a href="/manager/windows/datainputstats"> Use Manager to add more data</a>]]>.</param>

  </module>

  <module name="LinkSwitcher" group="All indexed data" layoutPanel="panel_row5_col1">

      <param name="mode">independent</param>

      <param name="label"> </param>



      

        <!--

            Last tabbed panel of the indexed data dashboard

            All time

        -->

        <module name="NullModule" group="All time">

        

            <!-- The first list of sources -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp1" autoRun="true">

                <param name="search">| metadata type=sources (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, source, totalCount,type | dedup source </param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Sources (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp1">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Source</param>

                        <param name="value">source</param>

                    </list>

                    <list>

                        <param name="label">Total Count</param>

                        <param name="value">totalCount</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list1</param>

                        <param name="search">| metadata type=sources (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, source, totalCount,type | dedup source </param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">source</param>

                                <param name="value">source</param>

                            </list>

                            <list>

                                <param name="label">totalCount</param>

                                <param name="labelFormat">number</param>

                            </list>

                        </param>

                        <module name="ConvertToIntention">

                            <param name="settingToConvert">list1</param>

                            <param name="intention">

                                <param name="name">addterm</param>

                                <param name="arg">

                                    <param name="source">$target$</param>

                                </param>

                            </param>

                            <module name="ViewRedirector">

                                <param name="viewTarget">flashtimeline</param>

                            </module>

                        </module>

                    </module>

                </module>

            </module>



            <!-- The list of sourcetypes -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp2" autoRun="true">

                <param name="search">| metadata type=sourcetypes (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, sourcetype, totalCount,type | dedup sourcetype </param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Sourcetypes (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp2">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Sourcetype</param>

                        <param name="value">sourcetype</param>

                    </list>

                    <list>

                        <param name="label">Total Count</param>

                        <param name="value">totalCount</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list2</param>

                        <param name="search">| metadata type=sourcetypes (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, sourcetype, totalCount,type | dedup sourcetype </param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">sourcetype</param>

                                <param name="value">sourcetype</param>

                            </list>

                            <list>

                                <param name="label">totalCount</param>

                                <param name="labelFormat">number</param>

                            </list>

                        </param>

                        <module name="ConvertToIntention">

                            <param name="settingToConvert">list2</param>

                            <param name="intention">

                                <param name="name">addterm</param>

                                <param name="arg">

                                    <param name="sourcetype">$target$</param>

                                </param>

                            </param>

                            <module name="ViewRedirector">

                                <param name="viewTarget">flashtimeline</param>

                            </module>

                        </module>

                    </module>

                </module>

            </module>

        

            <!-- The list of hosts -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp3" autoRun="true">

                <param name="search">| metadata type=hosts (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, host, totalCount,type | dedup host </param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Hosts (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp3">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Host</param>

                        <param name="value">host</param>

                    </list>

                    <list>

                        <param name="label">Total Count</param>

                        <param name="value">totalCount</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list3</param>

                        <param name="search">| metadata type=hosts (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, host, totalCount,type | dedup host</param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">host</param>

                                <param name="value">host</param>

                            </list>

                            <list>

                                <param name="label">totalCount</param>

                                <param name="labelFormat">number</param>

                            </list>

                        </param>

                        <module name="ConvertToIntention">

                            <param name="settingToConvert">list3</param>

                            <param name="intention">

                                <param name="name">addterm</param>

                                <param name="arg">

                                    <param name="host">$target$</param>

                                </param>

                            </param>

                            <module name="ViewRedirector">

                                <param name="viewTarget">flashtimeline</param>

                            </module>

                        </module>

                    </module>

                </module>

            </module>

        </module>

        <!--

            Second tabbed panel of the indexed data dashboard

            Last 24 hours

        -->

        <module name="NullModule" group="Last 24 hours">

        

            <!-- The first list of sources -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp1" autoRun="true">

                <param name="search">| metadata type=sources (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, source, totalCount,type | dedup source |  where lastTime &gt; now()-86400</param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Sources (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp1">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Source</param>

                        <param name="value">source</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list1</param>

                        <param name="search">| metadata type=sources (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, source, totalCount,type | dedup source |  where lastTime &gt; now()-86400</param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">source</param>

                                <param name="value">source</param>

                            </list>

                        </param>

                        <module name="HiddenSearch">

                            <param name="search">*</param>

                            <param name="earliest">-24h</param>

                            <module name="ConvertToIntention">

                                <param name="settingToConvert">list1</param>

                                <param name="intention">

                                    <param name="name">addterm</param>

                                    <param name="arg">

                                        <param name="source">$target$</param>

                                    </param>

                                </param>

                                <module name="ViewRedirector">

                                    <param name="viewTarget">flashtimeline</param>

                                </module>

                            </module>

                        </module>

                    </module>

                </module>

            </module>



            <!-- The list of sourcetypes -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp2" autoRun="true">

                <param name="search">| metadata type=sourcetypes (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, sourcetype, totalCount,type | dedup sourcetype | where lastTime &gt; now()-86400</param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Sourcetypes (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp2">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Sourcetype</param>

                        <param name="value">sourcetype</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list2</param>

                        <param name="search">| metadata type=sourcetypes (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, sourcetype, totalCount,type | dedup sourcetype | where lastTime &gt; now()-86400</param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">sourcetype</param>

                                <param name="value">sourcetype</param>

                            </list>

                        </param>

                        <module name="HiddenSearch">

                            <param name="search">*</param>

                            <param name="earliest">-24h</param>

                            <module name="ConvertToIntention">

                                <param name="settingToConvert">list2</param>

                                <param name="intention">

                                    <param name="name">addterm</param>

                                    <param name="arg">

                                        <param name="sourcetype">$target$</param>

                                    </param>

                                </param>

                                <module name="ViewRedirector">

                                    <param name="viewTarget">flashtimeline</param>

                                </module>

                            </module>

                        </module>

                    </module>

                </module>

            </module>

        

            <!-- The list of hosts -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp3" autoRun="true">

                <param name="search">| metadata type=hosts (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, host, totalCount,type | dedup host |  where lastTime &gt; now()-86400</param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Hosts (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp3">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Host</param>

                        <param name="value">host</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list3</param>

                        <param name="search">| metadata type=hosts (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, host, totalCount,type | dedup host |  where lastTime &gt; now()-86400</param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">host</param>

                                <param name="value">host</param>

                            </list>

                        </param>

                        <module name="HiddenSearch">

                            <param name="search">*</param>

                            <param name="earliest">-24h</param>

                            <module name="ConvertToIntention">

                                <param name="settingToConvert">list3</param>

                                <param name="intention">

                                    <param name="name">addterm</param>

                                    <param name="arg">

                                        <param name="host">$target$</param>

                                    </param>

                                </param>

                                <module name="ViewRedirector">

                                    <param name="viewTarget">flashtimeline</param>

                                </module>

                            </module>

                        </module>

                    </module>

                </module>

            </module>

        </module>

        <!--

            First tabbed panel of the indexed data dashboard

            Last 15 minutes

        -->

        <module name="NullModule" group="Last 15 minutes">

        

            <!-- The first list of sources -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp1" autoRun="true">

                <param name="search">| metadata type=sources (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, source, totalCount,type | dedup source | where lastTime &gt; now()-900</param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Sources (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp1">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Source</param>

                        <param name="value">source</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list1</param>

                        <param name="search">| metadata type=sources (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, source, totalCount,type | dedup source | where lastTime &gt; now()-900</param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">source</param>

                                <param name="value">source</param>

                            </list>

                        </param>

                        <module name="HiddenSearch">

                            <param name="search">*</param>

                            <param name="earliest">-15m</param>

                            <module name="ConvertToIntention">

                                <param name="settingToConvert">list1</param>

                                <param name="intention">

                                    <param name="name">addterm</param>

                                    <param name="arg">

                                        <param name="source">$target$</param>

                                    </param>

                                </param>

                                <module name="ViewRedirector">

                                    <param name="viewTarget">flashtimeline</param>

                                </module>

                            </module>

                        </module>

                    </module>

                </module>

            </module>



            <!-- The list of sourcetypes -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp2" autoRun="true">

                <param name="search">| metadata type=sourcetypes (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, sourcetype, totalCount,type | dedup sourcetype | where lastTime &gt; now()-900</param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Sourcetypes (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp2">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Sourcetype</param>

                        <param name="value">sourcetype</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list2</param>

                        <param name="search">| metadata type=sourcetypes (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, sourcetype, totalCount,type | dedup sourcetype | where lastTime &gt; now()-900</param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">sourcetype</param>

                                <param name="value">sourcetype</param>

                            </list>

                        </param>

                        <module name="HiddenSearch">

                            <param name="search">*</param>

                            <param name="earliest">-15m</param>

                            <module name="ConvertToIntention">

                                <param name="settingToConvert">list2</param>

                                <param name="intention">

                                    <param name="name">addterm</param>

                                    <param name="arg">

                                        <param name="sourcetype">$target$</param>

                                    </param>

                                </param>

                                <module name="ViewRedirector">

                                    <param name="viewTarget">flashtimeline</param>

                                </module>

                            </module>

                        </module>

                    </module>

                </module>

            </module>

        

            <!-- The list of hosts -->

            <module name="HiddenSearch" layoutPanel="panel_row5_col1_grp3" autoRun="true">

                <param name="search">| metadata type=hosts (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, host, totalCount,type | dedup host |  where lastTime &gt; now()-900</param>

                <module name="SimpleResultsHeader">

                    <param name="entityName">results</param>

                    <param name="headerFormat">Hosts (%(count)s)</param>

                </module>

            </module>

            <module name="Sorter" layoutPanel="panel_row5_col1_grp3">

                <param name="sortKey">lastTime</param>

                <param name="sortDir">desc</param>

                <param name="fields">

                    <list>

                        <param name="label">Host</param>

                        <param name="value">host</param>

                    </list>

                    <list>

                        <param name="label">Last Updated</param>

                        <param name="value">lastTime</param>

                    </list>

                </param>

                <module name="Paginator">

                    <param name="count">10</param>

                    <param name="entityName">settings</param>

                    <param name="maxPages">10</param>

                    <module name="SearchLinkLister">

                        <param name="settingToCreate">list3</param>

                        <param name="search">| metadata type=hosts (eventtype="windows_performance" OR eventtype="windows_events") | table firstTime, lastTime, recentTime, host, totalCount,type | dedup host |  where lastTime &gt; now()-900</param>

                        <param name="searchFieldsToDisplay">

                            <list>

                                <param name="label">host</param>

                                <param name="value">host</param>

                            </list>

                        </param>

                        <module name="HiddenSearch">

                            <param name="search">*</param>

                            <param name="earliest">-15m</param>

                            <module name="ConvertToIntention">

                                <param name="settingToConvert">list3</param>

                                <param name="intention">

                                    <param name="name">addterm</param>

                                    <param name="arg">

                                        <param name="host">$target$</param>

                                    </param>

                                </param>

                                <module name="ViewRedirector">

                                    <param name="viewTarget">flashtimeline</param>

                                </module>

                            </module>

                        </module>

                    </module>

                </module>

            </module>

        </module> 



  </module> <!-- end LinkSwitcher -->

</view>

