[install]
configured_version = <value>
# Current configured app version.

[hosts]
refresh_interval = <value>
# Interval to refresh data into hosts dashboard.

[home]
refresh_interval = <value>
# Interval to refresh data into home dashboard.