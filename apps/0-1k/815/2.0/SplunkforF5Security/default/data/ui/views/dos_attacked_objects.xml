<form class="formsearch">
  <label>Attacked URLs Stats</label>

  <fieldset>
       <input type="dropdown" token="hostname" searchWhenChanged="true">
          <label>BIG-IP Hostname:</label>
          <default>*</default>
          <populatingSearch fieldForValue="hostname" fieldForLabel="hostname"> sourcetype="asm_log" dos_attack_name != "" | stats count by hostname </populatingSearch>
          <choice value="*">all</choice>
       </input>

       <input type="dropdown" token="context_name" searchWhenChanged="true">
          <label>Context Name:</label>
          <populatingSearch fieldForValue="context_name" fieldForLabel="context_name"> sourcetype="asm_log" dos_attack_name != "" | stats count by context_name </populatingSearch>
          <choice value="*">all</choice>
       </input>

        <input type="time" searchWhenChanged="true"/>
  </fieldset>

  <row>
    <html><center><h1>Attacked URLs Stats - DoS</h1></center></html>
  </row>
  <row>
      <chart>
          <title>Time Distribution of Dropped Requests by Attack ID (total)</title>
          <searchTemplate>sourcetype="asm_log" dos_attack_name != "" context_name=$context_name$ `Anomalies_url_count_time_distribution($hostname$)`</searchTemplate>
          <option name="charting.chart">column</option>
          <option name="charting.secondaryAxisTitle.text">Count</option>

      </chart>

  </row>
  <row>
      <chart>
          <title>Attacked URLs (total)</title>
          <searchTemplate>sourcetype="asm_log" dos_attack_name != "" context_name=$context_name$ `Anomalies_attacked_URLs_total($hostname$)`</searchTemplate>
          <option name="charting.chart">pie</option>
      </chart>
      <chart>
          <title>Attacked URLs by Attack ID (total)</title>
          <searchTemplate>sourcetype="asm_log" dos_attack_name != "" context_name=$context_name$ `Anomalies_attacked_URLs_per_attack_id($hostname$)`</searchTemplate>
          <option name="charting.chart">bar</option>
          <option name="charting.secondaryAxisTitle.text">Dropped Requests Count</option>

      </chart>
  </row>
</form>