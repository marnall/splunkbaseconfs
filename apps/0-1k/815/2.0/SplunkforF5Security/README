BIG IP - Access Security Manager

On the ASM and PSM set the logging profile to Reporting Server. 

Open up a receiving port on the Splunk data inputs page

The sourcetype of your ASM log needs to be set to asm_log

The sourcetype of your PSM log needs to be set to psm_log