[ui]
is_visible = 0
label = Configuration Sync

[launcher]
author = vbumgarner
description = This application uses rsync and scp to send all local configurations to another server.
version=1.0

[install]
state = disabled

[package]
id = ConfigurationSync

