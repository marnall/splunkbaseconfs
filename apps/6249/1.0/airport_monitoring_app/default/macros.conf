# appleairportindexes
# -----------------------------
# Defines indexes where Apple Airport data is stored.
#

[appleairportindexes]
definition = index=prod_wifi
iseval = 0

# appleairport_stats_index
# -----------------------------
# Defines indexes where Apple Airport statistics are kept.
#

[appleairport_stats_index]
definition = index=prod_wifi
iseval = 0

# airport_collection_indexes
# -----------------------------
# Defines where collection indexes are saved.
#
[airport_collection_indexes]
definition = index=prod_network
iseval = 0

