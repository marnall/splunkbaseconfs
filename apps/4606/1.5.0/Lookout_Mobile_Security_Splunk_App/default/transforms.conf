#
# Splunk app KV Store lookup configuration file
#
[lookout_mra_kvstore_lookup]
external_type = kvstore
collection = lookout_mra_configuration_data
fields_list = _key, startPosition, streamPosition, access_token, refresh_token, application_key, ent, is_updated, is_valid
