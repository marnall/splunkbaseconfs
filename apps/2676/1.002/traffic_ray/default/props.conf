[access_combined]
EVAL-site = replace(source, "^.*/([^/]+?)((-[A-Z]|-ssl_log|\.bkup).*)?$", "\1")
EVAL-outbytes = if(isnum(bytes), bytes, 0)
FIELDALIAS-FIELD_ALIASES_access_combined = bytes AS outbytes clientip AS ip host AS site uri_path AS page uri_query AS qs useragent AS ua
