[get_ps_flashblade_index]
definition = index=main
iseval = 0

[get_ps_flasharray_index]
definition = index=main
iseval = 0

[get_ps_pure1_index]
definition = index=main
iseval = 0

[summariesonly]
definition = summariesonly=false
iseval = 0

[get_ps_flasharray_array]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:array
iseval = 0

[get_ps_flasharray_hosts]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:hosts
iseval = 0

[get_ps_flasharray_logs_alerts]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:alerts
iseval = 0

[get_ps_flasharray_logs_audit]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:audit
iseval = 0

[get_ps_flasharray_logs_login]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:login
iseval = 0

[get_ps_flasharray_pgroups]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:pgroups
iseval = 0

[get_ps_flasharray_pods]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:pod
iseval = 0

[get_ps_flasharray_snapshots]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:snapshots
iseval = 0

[get_ps_flasharray_vgroups]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:vgroup
iseval = 0

[get_ps_flasharray_volumes]
definition = `get_ps_flasharray_index` sourcetype=purestorage:flasharray:volumes
iseval = 0