[install]
is_configured = 0

[ui]
is_visible = 0
label = AWS Web Application Firewall Add-on

[id]
version = 1.0.5
name = TA-aws_waf

[launcher]
author = Hurricane Labs
description = Add-on used to parse events from the AWS WAF over HEC.
version = 1.0.5

[package]
check_for_updates = true
id = TA-aws_waf
