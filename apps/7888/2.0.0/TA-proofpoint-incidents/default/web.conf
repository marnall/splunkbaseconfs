[expose:TA-proofpoint-incidents_account]
pattern = TA-proofpoint-incidents_account
methods = POST, GET

[expose:TA-proofpoint-incidents_account_specified]
pattern = TA-proofpoint-incidents_account/*
methods = POST, GET, DELETE

[expose:TA-proofpoint-incidents_settings]
pattern = TA-proofpoint-incidents_settings
methods = POST, GET

[expose:TA-proofpoint-incidents_settings_specified]
pattern = TA-proofpoint-incidents_settings/*
methods = POST, GET, DELETE

[expose:TA-proofpoint-incidents_proofpoint_incidents]
pattern = TA-proofpoint-incidents_proofpoint_incidents
methods = POST, GET

[expose:TA-proofpoint-incidents_proofpoint_incidents_specified]
pattern = TA-proofpoint-incidents_proofpoint_incidents/*
methods = POST, GET, DELETE
