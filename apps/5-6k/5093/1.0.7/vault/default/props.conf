[hashicorp_vault_audit_log]
INDEXED_EXTRACTIONS = json
KV_MODE = none
LINE_BREAKER = ([\r\n]+)
NO_BINARY_CHECK = true
category = Structured
description = Vault 1.5 and below audit log format
disabled = false
pulldown_type = 1

[hashicorp_vault_telemetry]
LINE_BREAKER = ([\r\n]+)
NO_BINARY_CHECK = true
category = Metrics
description = StatsD telemetry from HashiCorp Vault
pulldown_type = 1
