[vmray_query_hash]
param.hash_type = <string>
   * One of md5, sha1, sha256
   * Required.
   * Defaults to None.

param.hash_value = <string>
   * The field which houses the hash value.
   * Required.
   * Defaults to None.

[vmray_submit_url]
param.url_value = <string>
