[vmware-edr-kill-process]
param._cam = <value>
* CIM Actions / Adaptive Response Requirement

param.api_config = <value>
* The VmWare api_config configuration to use

param.proxy_guid = <value>
* The Proxy Guid to use

param.verify_ssl = <value>
* Verify SSL

param.process_field = <string>
param.device_id_field = <string>

[vmware-edr-isolate-device]
param._cam = <value>
* CIM Actions / Adaptive Response Requirement

param.api_config = <value>
* The VmWare api_config configuration to use

param.proxy_guid = <value>
* The Proxy Guid to use

param.verify_ssl = <value>
* Verify SSL

param.device_id_field = <string>

[vmware-edr-unisolate-device]
param._cam = <value>
* CIM Actions / Adaptive Response Requirement

param.api_config = <value>
* The VmWare api_config configuration to use

param.proxy_guid = <value>
* The Proxy Guid to use

param.verify_ssl = <value>
* Verify SSL

param.device_id_field = <string>


[vmware-edr-ban-hash]
param._cam = <value>
* CIM Actions / Adaptive Response Requirement

param.api_config = <value>
* The VmWare api_config configuration to use

param.proxy_guid = <value>
* The Proxy Guid to use

param.verify_ssl = <value>
* Verify SSL
param.hash_field = <string>
param.dryrun = <bool>