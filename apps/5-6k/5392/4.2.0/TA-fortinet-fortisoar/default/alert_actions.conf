# In order to override the hostname in the link from Splunk you can set the hostname field here; for example:
# hostname = https://splunk-cname.domain.com:8000

[cs_alert_alert]
is_custom = 1 
disabled = 0  
label = FortiSOAR: Create Alert
description = Create an alert in FortiSOAR
icon_path = alert-action.png 
payload_format = json
python.version = python3

[cs_alert_incident]
is_custom = 1 
disabled = 0  
label = FortiSOAR: Create Incident
description = Create an Incident in FortiSOAR
icon_path = alert-action.png 
payload_format = json
python.version = python3

[cs_playbook]
is_custom = 1
disabled = 0
label = FortiSOAR: Run Playbook
description =  Run a FortiSOAR Playbook
icon_path = alert-action.png
payload_format = json
param.cyops_uri =
param.cyops_public_key =
param.cyops_private_key =
param.playbook = "Playbook"
param._cam = {"category": ["Information Tracking"], "subject": ["process"], "supports_adhoc": true, "technology": [{"vendor": "Fortinet", "product": "FortiSOAR", "version": ["4.2.0"]}], "task": ["create"]}
python.version = python3