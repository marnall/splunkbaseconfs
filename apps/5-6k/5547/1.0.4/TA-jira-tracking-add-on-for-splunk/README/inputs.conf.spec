[input_jira_issues://<name>]
url_base = Jira server URL
global_account = 
reset_checkpoint = Warning; Resets checkpoint. Needs to be disabled once the reset actually happens before next interval.

[input_jira_worklogs://<name>]
url_base = Jira server URL
global_account = 
reset_checkpoint = Warning; Resets checkpoint. Needs to be disabled once the reset actually happens before next interval.