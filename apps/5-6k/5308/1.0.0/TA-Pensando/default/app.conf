#
# Splunk app configuration file
#

[install]
is_configured = 0
state = enabled
build = 15

[launcher]
author = Pensando
description = Parses the firewall logs collected from the Pensando DSC platform.
version = 1.0.0

[ui]
is_visible = 0
label = Pensando Add-on for Splunk

[package]
id = TA-Pensando
