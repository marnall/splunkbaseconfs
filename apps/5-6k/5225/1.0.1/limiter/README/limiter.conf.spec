[<index_name>]
bytes = <int>
* Index limit in bytes

hours = <int>
* Limit applying timespan

snapping = <bool>
* Whether or not to snap to the start of day, week, etc (determined by timespan length)

used = <int>
* Don't modify (currently indexed bytes, filled automatically)
