[limiter]
create_aliases = <bool>
* Is alias index creation enabled

alias_suffix = <string>
* If the Limiter creates alias indices, the name is composed by taking the index name and appending <alias_suffix>
