[mulesoft_audit://<name>]
username = 
password = 
client_id = Only populate if using connected apps
client_secret = Only populate if using connected apps
grant_type = Only populate if using connected apps
organization_id = 
seconds_lookback = 
results_limit = Cannot be greater than 200
token_url = URL to grab bearer token. If using connected apps, change URL.
data_url = If you require to change the data URL, you may do so. '+organization_id+' will be replaced with the Organization ID above.