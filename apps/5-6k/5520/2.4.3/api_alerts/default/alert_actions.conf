[api_alerts]
is_custom = 1
label = API alerts
description = Send alert through HTTP request
icon_path = api_alert_icon.png
payload_format = json

# Default global configuration
param.url =
param.proxy =
param.header = default_header
# Search query
param.search_query = $job.search$

python.version=python3
