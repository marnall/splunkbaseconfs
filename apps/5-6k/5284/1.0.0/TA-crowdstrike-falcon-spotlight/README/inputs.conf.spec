[crowdstrike_falcon_spotlight://<name>]
global_account = 
access_token_endpoint = 
api_v1_endpoint = 
api_v2_endpoint = 
updated_timestamp = Set initial starting point in time.Only the initial run needs this value. The delta is gathered by checkpoint which the add-on creates on each cycle of data ingestion Format:YYYY-MM-DDTHH:MM:SS
limit =