[violations_all_channels://<name>]
token_url = 
violation_url = 
channel_url = 
channel_ids = Defaults to select "All" channels, enter comma separated list to only select specific channels.
channel_statuses1 = Use to select the status of the channels selected above
checkpoint = Selected by default to prevent duplicate data retrieval. When selected new data will only be retrieved once.