[cloudknox://<name>]
auth_system_type = Type of auth system to collect data for
auth_systems = Comma-separated List of auth system to collect data for

[cloudknox_auditlogs://<name>]
start_datetime = Date from which data will be collected

[cloudknox_alerts://<name>]
alert_type = Alert type to be collected
start_datetime = Date from which data will be collected
