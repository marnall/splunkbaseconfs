# In prior versions of the content pack, automatic lookup configurations where shipped in this .conf file.
# But they have been moved directly into correlation search SPL. Therefore this file
# is now shipped with commented out configs to show what was previously done in the old CP versions


# [itsi_notable:group]
# LOOKUP-itsi_episode_contact_by_alert_group = itsi_episode_contact_map alert_group AS alert_group OUTPUTNEW episode_contact_detail AS episode_contact_detail episode_contact_method AS episode_contact_method

# [source::disabled_kpis_healthscore_generator]
# LOOKUP-itsi_kpi_attributes = itsi_kpi_attributes kpiid AS kpiid OUTPUTNEW

# [source::Indicator - *]
# LOOKUP-itsi_kpi_attributes = itsi_kpi_attributes kpiid AS kpiid OUTPUTNEW

# [source::kpi_backfill]
# LOOKUP-itsi_kpi_attributes = itsi_kpi_attributes kpiid AS kpiid OUTPUTNEW

# [source::service_health_monitor]
# LOOKUP-itsi_kpi_attributes = itsi_kpi_attributes kpiid AS kpiid OUTPUTNEW
