#
# exclude the tSessions and tHostInfo CSV Lookups so that we don't
# get multi-GB files being transferred over to the indexers.
#
[replicationBlacklist]
MSAD_lookups = .../DA-ITSI-CP-windows-dashboards/lookups/(tHostInfo|tSessions).csv$

