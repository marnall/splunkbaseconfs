[<ztap_host_stanza>]
*This is how the ZTAP host is configured

org_key = <value>
* The Critical Start Customer ID (api required)

ztap_env = <value>
* The ZTAP Environment (url, api required)

guid = <value>
* The GUID unique to this tenant

comment = <value>
* A descriptive comment