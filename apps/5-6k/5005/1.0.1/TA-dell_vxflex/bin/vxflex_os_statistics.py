import ta_dell_vxflex_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from solnlib.packages.splunklib import modularinput as smi



from input_module_vxflex_os_statistics import VxFlexStatisticsCollector

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_vxflex_os_statistics.py
'''
class ModInputvxflex_os_statistics(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        use_single_instance = False
        super(ModInputvxflex_os_statistics, self).__init__("ta_dell_vxflex", "vxflex_os_statistics", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputvxflex_os_statistics, self).get_scheme()
        scheme.title = ("VxFlex OS Statistics")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("instances_rest_endpoint", title=" Instances rest endpoint",
                                         description="The rest endpoint which provides the list of instances for which the statistics should be collected.Ex. /api/types/Volume/instances",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("statistics_rest_endpoint", title=" Statistics rest endpoint",
                                         description="Rest endpoint of the statistics which should be collected for instances.Ex. /api/instances/Volume::{id}/relationships/Statistics",
                                         required_on_create=True,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-dell_vxflex"

    def validate_input(self, definition):
        """validate the input stanza"""
        VxFlexStatisticsCollector.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        vxflex_statistics_collector = VxFlexStatisticsCollector(self, ew)
        vxflex_statistics_collector.collect_events()

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputvxflex_os_statistics().run(sys.argv)
    sys.exit(exitcode)
