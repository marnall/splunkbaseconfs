import ta_qualtricsaudit_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from solnlib.packages.splunklib import modularinput as smi



import input_module_QualtricsAudit as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_QualtricsAudit.py
'''
class ModInputQualtricsAudit(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputQualtricsAudit, self).__init__("ta_qualtricsaudit", "QualtricsAudit", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputQualtricsAudit, self).get_scheme()
        scheme.title = ("Qualtrics")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("qualtrics_server_selection", title="Qualtrics Server Selection",
                                         description="See https://api.qualtrics.com/api-reference/reference/audits.json for available options. At the time of dev valid entries are: syd1, ca1, iad1, sjc1, fra1, sin1, gov1 only one entry is supported",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("qualtrics_domain_selection", title="Qualtrics Domain Selection",
                                         description="Leave at default, unless Qualtrics changes domains.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("clientid", title="OAuth ClientID",
                                         description="",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("clientsecret", title="OAuth ClientSecret",
                                         description="",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("oauth_2_0_scope", title="OAuth 2.0 Scope",
                                         description="See https://api.qualtrics.com/api-reference/docs/Instructions/oauthscopes.md",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("disable_https_cert_validation", title="Disable HTTPS Cert Validation",
                                         description="",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("delete_existing_checkpoints", title="Delete existing checkpoints",
                                         description="This will delete all saved checkpoints and cause indexing to start from the beginning again. NOTE: The script will not index events while this is selected",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("activity_types_to_collect", title="Activity Types to Collect",
                                         description="",
                                         required_on_create=True,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-qualtricsaudit"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("disable_https_cert_validation")
        checkbox_fields.append("delete_existing_checkpoints")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputQualtricsAudit().run(sys.argv)
    sys.exit(exitcode)
