#
# Splunk app configuration file
#

[id]
name = propinator
version = 1.0.2

[install]
is_configured = false
build = 101

[ui]
is_visible = 1
label = Propinator

[launcher]
author = DiscoveredIntel
description = The Propinator Splunk App from Discovered Intelligence is an opinionated parsing configuration generator from events already indexed in Splunk.
version = 1.0.2

[package]
id = propinator