# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 4

[launcher]
author = Leonardo Armesto
version = 1.0.2
description = SNMP Getter Splunk queries remote devices for software/hardware configurations via SNMP protocol with just one or more object identifiers (OIDs) given as arguments.

[ui]
is_visible = 1
label = Simple SNMP Getter
docs_section_override = AddOns:released

[package]
id = TA-simple-snmp-getter

[triggers]
reload.addon_builder = simple
reload.ta_simple_snmp_getter_account = simple
reload.ta_simple_snmp_getter_settings = simple
reload.passwords = simple

