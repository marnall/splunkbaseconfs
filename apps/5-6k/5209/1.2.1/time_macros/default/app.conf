[launcher]
version = 1.2.1
description = The Time Macros app provides a set of macros for time-related conversions: epoch to formatted strings and vice versa, timerangepicker output to epoch, relative time epoch shifts, business-hour-only time deltas
author = martin_mueller

[ui]
is_visible = 0
label = Time Macros

[package]
id = time_macros

[id]
name = time_macros
version = 1.2.1

