[<tenant_stanza>]
*This is how the Tenant is configured
tenant = <value>
* Tenant Name

org_key = <value>
* The CBC Org Key (api required)

cbc_env = <value>
* The CBC Environment (url, api required)

api_key = <value>
* The API Key (api, required)

guid = <value>
* The GUID unique to this tenant

comment = <value>
* A descriptive comment