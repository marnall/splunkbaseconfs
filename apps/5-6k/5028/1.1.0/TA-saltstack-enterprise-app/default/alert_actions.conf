
[saltstack_alert_action]
python.version = python3
description = SaltStack's alert action for Security Orchestration, Automation and Response (SOAR)
label = Invoke SaltStack Enterprise Alert Action
is_custom = 1
payload_format = json
icon_path = alert_saltstack_alert_action.png
param.cmd = local
param.fun = test.ping
param.master = *
param.tgt_type = select
param.verify_ssl = 1

