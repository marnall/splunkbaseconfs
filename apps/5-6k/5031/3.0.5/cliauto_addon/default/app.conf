#
# Splunk app configuration file
#

[install]
is_configured = 0

[ui]
is_visible = 0
label = CLI Auto Add-on for Splunk

[launcher]
author = Jeff Berry
description = CLI Auto Add-on provides automation to execute CLI commands on mulitple hosts via ssh
version = 3.0.5

[package]
id = cliauto_addon
