[alert_cs_contain]
is_custom = 1
label = Host Auto-Contain for CrowdStrike
description = Uses the sensor ID from a Falcon log stream event to network contain a host.
icon_path = alert_cs_contain.png

payload_format = json
