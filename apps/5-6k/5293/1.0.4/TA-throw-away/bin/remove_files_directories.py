import ta_throw_away_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from solnlib.packages.splunklib import modularinput as smi



import input_module_remove_files_directories as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_remove_files_directories.py
'''
class ModInputremove_files_directories(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputremove_files_directories, self).__init__("ta_throw_away", "remove_files_directories", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputremove_files_directories, self).get_scheme()
        scheme.title = ("Remove files/directories")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("working_directory", title="Working Directory",
                                         description="This will be the directory, where the script will run from and where the objects will be removed from. Use absolute path, e.g. \"/data/directory/\".",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("pattern", title="Pattern",
                                         description="Use RegEx to describe the pattern of files/directories to remove from the working directory.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("retention_policy", title="Retention Policy",
                                         description="Turn this on if you\'d like to remove files/directories based on their age.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("retention_period", title="Retention Period",
                                         description="Provide the age of files/directories to be deleted in seconds, e.g. \"86400\" for 24 hours. Also, make sure you have enabled the \"Retention Policy\" option first.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("timestamp_location", title="Timestamp Location",
                                         description="\"Last Modified\" will get timestamp from the \"last modified\" attribute in the filesystem. Use \"Bucket\" only with Splunk frozen buckets.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-throw-away"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("retention_policy")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputremove_files_directories().run(sys.argv)
    sys.exit(exitcode)
