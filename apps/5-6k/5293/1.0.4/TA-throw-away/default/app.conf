# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 11

[launcher]
author = Bright Consulting
version = 1.0.4
description = This add-on is designed to remove files/directories from the disk. The names of the removed items are collected as events for easy reporting. Now supporting Python v3.

[ui]
is_visible = 1
label = Throw Away
docs_section_override = AddOns:released

[package]
id = TA-throw-away

[triggers]
reload.addon_builder = simple
reload.ta_throw_away_account = simple
reload.ta_throw_away_settings = simple
reload.passwords = simple

