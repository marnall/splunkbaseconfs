import ta_rapid7_insightvm_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_insightvm_asset_import as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_insightvm_asset_import.py
'''
class ModInputinsightvm_asset_import(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputinsightvm_asset_import, self).__init__("ta_rapid7_insightvm", "insightvm_asset_import", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputinsightvm_asset_import, self).get_scheme()
        scheme.title = ("InsightVM Asset Import")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("insightvm_connection", title="InsightVM Connection",
                                         description="",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("asset_filter", title="Asset Filter",
                                         description="Asset filter to limit assets within scope (eg. \"sites IN [\'site-name\']\")",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("import_vulnerabilities", title="Import vulnerabilities",
                                         description="Select to enable the import of Asset Vulnerability Findings",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("vulnerability_filter", title="Vulnerability Filter",
                                         description="Vulnerability filter to limit vulnerabilities within scope (eg.  \"cvss_v2_score > 6\")",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("include_same_vulnerabilities", title="Include same vulnerabilities",
                                         description="Select to enable the import of vulnerabilities that are not new or remediated since the last import",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("full_import_schedule", title="Full Import Schedule",
                                         description="Number of days between full input import",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-rapid7-insightvm"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        account_fields.append("insightvm_connection")
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("import_vulnerabilities")
        checkbox_fields.append("include_same_vulnerabilities")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputinsightvm_asset_import().run(sys.argv)
    sys.exit(exitcode)
