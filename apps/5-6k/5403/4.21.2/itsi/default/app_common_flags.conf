[itsi-bidirectional-ticketing]
feature=Bidirectional Ticketing
description=Enables the ability to automatically update the state of an episode when its linked ticket is updated.
disabled=0

[itsi-remedy-bidirectional-ticketing]
feature=BMC Remedy Bidirectional Ticketing
description=Enables the ability to automatically update the state of an episode when its linked ticket is updated.
disabled=0

[itsi-jira-bidirectional-ticketing]
feature=Jira Bidirectional Ticketing
description=Enables the ability to automatically update the state of an episode when its linked ticket is updated.
disabled=0

[itsi-similar-episodes]
feature=Similar Episodes
description=Enables the 'Similar Episodes' tab in Episode Review.
disabled=0

[itsi-gt-aggregate-searches]
feature=Aggregate KPI searches in glass tables
description=Toggles the state of KPI search aggregation in beta glass tables.
disabled=0

[itsi-kpi-security-check]
feature=Security validation for KPI base searches and service KPIs
description=Disable this setting to bypass the security check run on KPI searches during KPI creation. Disabling this setting may expose index data to users without read permission.
disabled=0

[itsi-sim-integration]
feature= Splunk Infrastructure Monitoring entity integration
description=Enables the Splunk Infrastructure Monitoring integration to bring metrics and event data into ITSI.
disabled=0

[itsi-dashboard-entity-parameters]
feature=Filtering parameters for embeddable dashboards
description=Controls rendering of the entity parameters configuration for embeddable Splunk dashboards in entity types.
disabled=1

[itsi-content-pack-authorship]
feature=Content pack authorship
description=Provides better content pack authorship experience.
disabled=0

[itsi-at-outlier-removal]
feature=Outlier removal in Adaptive Thresholding
description=Outlier removal in Adaptive Thresholding
disabled=0

[itsi-alerts-identifier-based-throttling]
feature=Enables the itsi-alerts-identifier-based-throttling field
description=Enables the new required itsi-alerts-identifier-based-throttling field for the alert connection configuration
disabled=0

[itsi-rulesengine-adhoc]
feature=Enable the adhoc search for rules engine process
description=The rules engine adhoc search is running and the realtime search is disabled
disabled=1

[itsi-rulesengine-queue]
feature=Enable the queue for rules engine process
description=The rules engine queue mode is running and the realtime search is disabled
disabled=0

[itsi-health-analytics-console]
feature=Configuration assistant
description=Gain insights and troubleshoot configuration issues in your ITSI environment.
disabled=0

[itsi-bulk-delete-retired-entities]
feature=Bulk Delete Retired entities
description=Enable the bulk delete operation for retired entities.
disabled=0

[itsi-duplicate-entity-normalization]
feature=Duplicate entity normalization
description=Enables the ability to automatically merge new entities into duplicate entities
disabled=0

[itsi-restore-configuration]
feature=Missing objects validation before restore operation
description=Enable the restore to view missing objects details in restore preview operation.
disabled=0

[itsi-backup-configuration]
feature=Include other apps splunk dependencies used for ITSI objects in ITSI backup
description=Enable the backup to add missing splunk dependencies in ITSI backup
disabled=0

[itsi-high-scale-at]
feature=High Scale Adaptive Thresholding
description=Enables the ability to handle KPI adaptive thresholds at a high scale via batching.
disabled=0

[itsi-entity-level-adaptive-thresholding]
feature=Entity level adaptive thresholding
description=Enables the ability to run entity level adaptive thresholding
disabled=0

[itsi-at-incremental-learning]
feature=Adaptive Thresholding Incremental Learning
description=Enables the incremental learning process for adaptive thresholding
disabled=1

[itsi-drift-detection]
feature=Drift detection
description=Enables the ability to create drift detection for KPIs
disabled=0

[itsi-ea-enhanced-smart-mode]
feature=Enable Event iQ for EA
description=Enable the Event iQ for EA
disabled=0

[itsi-ea-snow-ticket-avoid-closed-status]
feature=Disable ticket creation with close status
description=Does not allow snow ticket to be create when state is Closed
disabled=0

[itsi-send-episode-event]
feature=Send episode update events to process
description=Send the episode update events for status, severity, owner updates to process
disabled=0

[itsi-apply-splunk-throttling]
feature=Apply splunk throttling to the correlation searches
description=Apply splunk throttling to the correlation searches generated via Alerts Onboarding
disabled=0

[itsi-partial-backup-optimization]
feature=Optimize partial backup time
description=Enable the optimization for partial backup
disabled=0

[itsi-si-recurring-maintenance-windows]
feature=Recurring maintenance windows
description=Enables user to schedule recurring maintenance windows
disabled=0

[itsi-episode-summarization]
feature=Episode summarization
description=Enables summarization for episode
disabled=1

[itsi-active-episode-check]
feature=Admin event active episode check
description=Skip sending admin events to the Rules Engine if the episode is inactive.
disabled=1

[itsi-neap-enhancement]
feature=NEAP enhancement
description=Enables NEAP enhancement for EA
disabled=0

[itsi-enable-parallel-kbs-creation-during-restore]
feature=Enable parallel creation of KPI Base Search during ITSI restore operations.
description=Enables parallel creation of KPI Base Search during ITSI restore to improve performance.
disabled=0

[itsi-ea-integration-enrichment]
feature=Enables enriched fields to be used and previewed in data integrations
description=Configure an enrichment policy to define the additional data fields to include when alerts are triggered.
disabled=1

[itsi-si-notify-missing-deps-default-backup]
feature=Notify users about missing dependencies in default backup
description=Alerts users when dependencies are missing during default backup
disabled=0

[itsi-restore-optimize-service-save]
feature=Optimize restore time by avoiding unnecessary service save operations
description=Enables faster restore by avoiding unnecessary service-related saves during the restore process
disabled=0

[itsi-service-analyzer-optimization]
feature=Service analyzer optimization
description=Optimizes search efficiency to reduce page load times for service and KPI data
disabled=0

[itsi-contentpack-conf-driven-service-config]
feature=Conf driven service configuration
description=Enables user to onboard services with conf file settings
disabled=0

[itsi-entity-lifecycle-save-batch]
feature=Use save_batch for entity lifecycle writes
description=Switches entity lifecycle management writes to save_batch (identifies services dependent on a entity and disassociates it); when disabled, falls back to batch_save_backend
disabled=0
