# Collection to store summarization work queue items for async AI summarization workflow.
[itsi_summary_work_queue]
field.status = number
field.updated_at = number
field.attempts = number
field.priority = number
field.created_at = number