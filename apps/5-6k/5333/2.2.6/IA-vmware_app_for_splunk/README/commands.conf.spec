[cbcdvcinfo]
tenant = <value>
* Extending Spec to allow a tenant to be configured (or multiple)
proxy_guid = <value>
* The guid of the proxy to use. Optional.

[cbchashinfo]
tenant = <value>
* Extending Spec to allow a tenant to be configured (or multiple)
proxy_guid = <value>
* The guid of the proxy to use. Optional.

[cbcquery]
tenant = <value>
* Extending Spec to allow a tenant to be configured (or multiple)
proxy_guid = <value>
* The guid of the proxy to use. Optional.