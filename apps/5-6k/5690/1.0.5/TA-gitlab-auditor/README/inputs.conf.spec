[gitlabapi://<name>]
gitlab_url = 
token = You can generate a personal access token for each application you use that needs access to the GitLab API
endpoint = 
created_after = Return events created after the given date
project_id = Optional, otherwise returns all available events within the projects