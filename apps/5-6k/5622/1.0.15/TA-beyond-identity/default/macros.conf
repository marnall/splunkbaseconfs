[beyond_identity_index]
definition = index=*
description = Index where Beyond Identity data is stored

[beyond_identity_search]
definition = `beyond_identity_index` sourcetype="beyond_identity_log"
; definition = `beyond_identity_index` (sourcetype="beyond_identity_log" OR (sourcetype="_json" AND "tenant_id"))