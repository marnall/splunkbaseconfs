import ta_intsights_declare

import os
import sys
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi


import input_module_intsights_alerts as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_intsights_alerts.py
'''


class ModInputintsights_alerts(modinput_wrapper.base_modinput.BaseModInput):
    """Define schema of the modular input."""

    def __init__(self):
        """Initialize class variables."""
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputintsights_alerts, self).__init__("ta_intsights", "intsights_alerts", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """Overloaded splunklib modularinput method."""
        scheme = super(ModInputintsights_alerts, self).get_scheme()
        scheme.title = ("intsights_alerts")
        scheme.description = (
            "Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu."
        )
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="Enter a unique name for this account.",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("report_date", title="Report Date",
                                         description="Select Alert report date. Default: 30 days ago.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("alert_severity", title="Alert Severity",
                                         description="Select the severity of Alerts to import.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("alert_type", title="Alert Type",
                                         description="Select the type of Alerts to import.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("alert_status", title="Alert Status",
                                         description="Please select status of Alert to import.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        """Obtain the name of the app."""
        return ta_intsights_declare.ta_name

    def validate_input(self, definition):
        """Validate the input stanza."""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """Write out the events."""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        """Obtain the global account."""
        account_fields = []
        account_fields.append("global_account")
        return account_fields

    def get_checkbox_fields(self):
        """Obtain the fields which use checkbox."""
        checkbox_fields = []
        checkbox_fields.append("checkbox")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        """Obtain global checkbox parameter names."""
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields


if __name__ == "__main__":
    exitcode = ModInputintsights_alerts().run(sys.argv)
    sys.exit(exitcode)
