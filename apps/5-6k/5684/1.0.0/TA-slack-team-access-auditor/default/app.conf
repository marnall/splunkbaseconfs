# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 1

[launcher]
author = Leonardo Armesto
version = 1.0.0
description = This Add-On is used to retrieve the access logs for users on a workspace.

[ui]
is_visible = 1
label = Slack Team Access Auditor
docs_section_override = AddOns:released

[package]
id = TA-slack-team-access-auditor

[triggers]
reload.addon_builder = simple
reload.ta_slack_team_access_auditor_account = simple
reload.ta_slack_team_access_auditor_settings = simple
reload.passwords = simple

