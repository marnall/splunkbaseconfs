[api-setup]

api_key = <string>
* The API key to authenticate requests to SlashNext CMS cloud

base_url = <string>
* The Base URL for accessing SlashNext CMS APIs. Do not change until or unless explicitly specified by SlashNext