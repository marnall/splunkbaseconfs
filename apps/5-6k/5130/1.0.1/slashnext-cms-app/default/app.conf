#
# Splunk app configuration file
#

[install]
is_configured = 0

[ui]
is_visible = 1
label = SlashNext CMS App for Splunk
setup_view = Setup

[launcher]
author = SlashNext, Inc
description = SlashNext CMS App for Splunk enables the Splunk user to have access to a complete snapshot of his/her organization's endpoints, secured by SlashNext Endpoint Security, with detailed statistics and visualizations that provide insights on the security status of all the endpoints and the organization as a whole.
version = 1.0.1

[package]
id = slashnext-cms-app
