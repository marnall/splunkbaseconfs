[pinger]
SHOULD_LINEMERGE = 0
category = Splunk App Add-on Builder
pulldown_type = 1
EXTRACT-field_extraction = ^(?P<datetime>.*?),(?P<destination>.*?),(?P<ping_status>.*?)$

[source::...ta_host_pinger*.log*]
sourcetype = tahostpinger:log

