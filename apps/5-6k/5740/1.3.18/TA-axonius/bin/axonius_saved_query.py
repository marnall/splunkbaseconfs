import ta_axonius_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_axonius_saved_query as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_axonius_saved_query.py
'''
class ModInputaxonius_saved_query(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputaxonius_saved_query, self).__init__("ta_axonius", "axonius_saved_query", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputaxonius_saved_query, self).get_scheme()
        scheme.title = ("Axonius Saved Query")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("api_host", title="Axonius Host",
                                         description="The URL of the Axonius web host",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("entity_type", title="Entity Type",
                                         description="The entity type of the saved query",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("saved_query", title="Saved Query",
                                         description="The name of the saved query",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("page_size", title="Page Size",
                                         description="The number of asset entities to fetch during each API call, higher is quicker while lower takes less memory.The maximum value is 2,000",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("standoff_ms", title="API Standoff (milliseconds)",
                                         description="The number of milliseconds to wait between successive API calls",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("shorten_field_names", title="Shorten Field Names",
                                         description="Shortens the long dotted notation field names by removing the prefixes \"specific_data.data.\" and \"adapters_data.\".",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("dynamic_field_mapping", title="Dynamic Field Mapping",
                                         description="Rename fields using a JSON-formatted string, renaming occurs prior to data ingest",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("cron_schedule", title="Cron Schedule",
                                         description="Use this parameter when you want to use a cron schedule to schedule the data ingestion.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("incremental_data_ingest", title="Incremental Ingest",
                                         description="Include only the entities that have a fetch timer newer than last collection",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("incremental_ingest_time_field", title="Incremental Ingest Time Field",
                                         description="Time field to use for comparison for incremental ingest. For Vulnerabilities use specific_data.data.first_seen",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("enable_include_details", title="Enable \"Include Details\"",
                                         description="Enable extra information to be returned in the result set that marries fields to their source adapter.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("ssl_certificate_path", title="CA Bundle Path",
                                         description="The filesystem path to the CA bundle used for SSL certificate validation",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("skip_lifecycle_check", title="Skip Lifecycle Check",
                                         description="This option will skip the lifecycle check. This should remained unchecked unless otherwise advised to turn on.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-axonius"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("shorten_field_names")
        checkbox_fields.append("incremental_data_ingest")
        checkbox_fields.append("enable_include_details")
        checkbox_fields.append("skip_lifecycle_check")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputaxonius_saved_query().run(sys.argv)
    sys.exit(exitcode)
