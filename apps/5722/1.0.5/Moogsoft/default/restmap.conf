[validation:savedsearch]
# Require url to be set if action is enabled
action.Moog_Integration = case('action.Moog_Integration' != "1", null(), 'action.Moog_Integration.param.url' == "", "No Moogsoft Integration URL specified", 'action.Moog_Integration.param.is_moogsoft_on_prem_edition' == '0' AND 'action.Moog_Integration.param.api_key' == "", "No API Key specified", 'action.Moog_Integration.param.is_proxy_config' == '1' AND ('action.Moog_Integration.param.proxy_host' == "" OR 'action.Moog_Integration.param.proxy_port' == ""), "Proxy Server Hostname and Port is required if Proxy Config is enabled", 1 == 1, null())