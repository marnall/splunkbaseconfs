[currency - table of all rates]
action.email.reportServerEnabled = 0
alert.track = 1
dispatch.earliest_time = -2h
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = sourcetype="currency_entry" |fields - EX*, splunk*, date*, time*, source, host, sourcetype, eventtype, linecount,eventcount, punct, index, tag* _raw|table _time *
vsid = gpepmnsu
