[source::...ta-site-pinger*.log*]
sourcetype = pinger

[source::...ta_site_pinger*.log*]
sourcetype = pinger

[pinger]
EXTRACT-field_extraction = ^(?P<datetime>.*?),(?P<destination>.*?),(?P<status>.*?)$
category = Splunk App Add-on Builder
pulldown_type = 1

