[Session Audit Start]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:session:audit:trail:start"

[Session Audit]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:session:audit:trail"

[Unblocked Connections]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:unblock:host"

[Blocked Connections]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:block:host"

[Haif Opened Connections]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:host:tcp:alert:on"

[Alerts On]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:alert:on"

[Alerts Off]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:alert:off"

[Session Maximum]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:sessions:maximum"

[Packets Passed]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:pass:pkt"

[Packets Dropped]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:drop:pkt"

[Log Summary]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:log:summary"

[UTD Logs]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:utd:logs" 

[Netflow Data]
search = `cisco_sdwan_index` sourcetype="stream:netflow"

[Platform Error]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:smart:lic:platform:error"

[Engine Write Fail]
search = `cisco_sdwan_index` sourcetype="cisco:sdwan:dmi:cli:engine:write:fail"