1. When configuring the JSON Object, a City name may have a space in it. This must be urlencoded for the API call to work properly. Encode this within the API Call Class.
2. Create Saved Searches for errors with the API.

