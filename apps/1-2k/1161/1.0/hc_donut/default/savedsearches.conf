[Donut Chart Datacube]
action.email.inline = 1
action.email.reportServerEnabled = 0
alert.digest_mode = True
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = 0
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index=_internal | stats count AS values by group series | sort - values | stats list(series) AS categories list(values) AS data BY group | eval categories=mvindex(categories, 0, 10) | eval data=mvindex(data, 0, 10) | rename group AS name
vsid = h5e7gmsx
