# Copyright (C) 2012-2018 Sideview LLC.  All Rights Reserved.

[default]
request.ui_context = 
request.ui_edit_view = 


#[internal_number_report]
#display.general.type = statistics
#display.page.search.tab = statistics
#display.visualizations.show = 0
#request.ui_dispatch_app = shoretel
#request.ui_dispatch_view = search
#dispatch.earliest_time = -24h
#dispatch.latest_time = now
#search = `cdr_events` \
#| eval {Direction}_duration=CallDuration \
#| eval {Direction}_CallID=CallID \
#| `calculate_all_internal_parties` \
#| stats dc(inbound_CallID) as inbound dc(outbound_CallID) as outbound dc(internal_CallID) as internal dc(CallID) as total sum(inbound_duration) as #inbound_duration sum(outbound_duration) as outbound_duration sum(internal_duration) as internal_duration sum(Duration) as total_duration by party
