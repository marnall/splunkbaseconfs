<setup>

  <block title="Database connection"
         endpoint="loginIDS_dbc/loginIDS_dbconfig_endpoint"
         entity="dbconnection">

      <text>
        &lt;i&gt;Before changing databasesettings disable scripted inputs using the switch below and reenable them afterwards.&lt;/i&gt;
      </text>

      <input field="db_type">
        <label>Select database type (Valid choices are SQLite or MySQL):</label>
        <type>text</type>
      </input>

      <input field="db_location">
        <label>Select database location. This might be a path to a SQLite-DB or an IP-Address followed by a Portnumber for MySQL-DB</label>
        <type>text</type>
      </input>

      <input field="db_user">
        <label>Specify username that should be used to connect to the database.</label>
	<type>text</type>
      </input>

      <input field="db_password">
        <label>Specify password that should be used to connect to the database. WARNING: This password is saved in cleartext in a configuration file only readable by root.</label>
	<type>text</type>
      </input>

  </block>

  <block title="Enable scripted inputs"
         endpoint="data/inputs/script"
         entity="*"
         mode="bulk">

    <text>
      &lt;i&gt;Before changing databasesettings disable scripted inputs and reenable them afterwards.&lt;/i&gt;
    </text>
    <input field="enabled">
      <label>Enable all scripted inputs. </label>
      <type>bool</type>
    </input>
  </block>
  
  <block title="Scripted inputs execution interval"
         endpoint="data/inputs/script"
         entity="*"
         mode="bulk">

    <text>
      &lt;i&gt;Using differen values for each scripted input by modifying the input itself could result in annormalies.&lt;/i&gt;
    </text>
    <input field="interval">
      <label>Specify the interval for all scripted inputs:</label>
      <type>text</type>
    </input>

  </block>

</setup>
