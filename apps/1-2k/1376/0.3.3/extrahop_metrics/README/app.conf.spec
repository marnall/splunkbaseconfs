username = (Added by Extrahop Metrics) used as username configuration key
password = (Added by Extrahop Metrics) used as password configuration key
python_path = (Added by Extrahop Metrics) used as path to python executable configuration key
hostname = (Added by Extrahop Metrics) used as hostname configuration key
