[tsi_ip]
external_type = kvstore
collection = ts_iocs_ip
fields_list = _key, asn, classification, confidence, country, date_first, date_last, detail, id, itype, lat, lon, lookup_key_value, maltype, org, resource_uri, severity, source, actor, campaign, tipreport, _time, last_time, link, type

[tsi_domain]
external_type = kvstore
collection = ts_iocs_domain
fields_list = _key, asn, classification, confidence, country, date_first, date_last, detail, id, itype, lat, lon, lookup_key_value, maltype, org, resource_uri, severity, source, actor, campaign, tipreport, _time, last_time, link, type

[tsi_url]
external_type = kvstore
collection = ts_iocs_url
fields_list = _key, asn, classification, confidence, country, date_first, date_last, detail, id, itype, lat, lon, lookup_key_value, maltype, org, resource_uri, severity, source, actor, campaign, tipreport, _time, last_time, link, type

[tsi_email]
external_type = kvstore
collection = ts_iocs_email
fields_list = _key, asn, classification, confidence, country, date_first, date_last, detail, id, itype, lat, lon, lookup_key_value, maltype, org, resource_uri, severity, source, actor, campaign, tipreport, _time, last_time, link, type

[tsi_md5]
external_type = kvstore
collection = ts_iocs_md5
fields_list = _key, asn, classification, confidence, country, date_first, date_last, detail, id, itype, lat, lon, lookup_key_value, maltype, org, resource_uri, severity, source, actor, campaign, tipreport, _time, last_time, link, type
[ts_lookup_srcip]
filename=ts_ip.csv.gz

[ts_lookup_destip]
filename=ts_ip.csv.gz

[ts_lookup_domain]
filename=ts_domain.csv.gz

[ts_lookup_md5]
filename=ts_md5.csv.gz

[ts_lookup_url]
filename=ts_url.csv.gz

[ts_lookup_email]
filename=ts_email.csv.gz

[ts_lookup_itype]
filename=ts_itypes.csv.gz

[ts_lookup_severity]
filename=ts_severities.csv.gz

[tm0_actor]
filename = tm_actor.csv.gz

[tm_tipreport]
external_type = kvstore
collection = tm_tipreport
fields_list = id, name, tags, intelligence, resource_uri, created_ts, modified_ts, body_content_type, body_content_type, body, endpoint, status, owner_org_name, _time, link, last_time, composition

[ts_ioc_matches]
external_type = kvstore
collection = ts_ioc_matches
fields_list = _key, _time, sourcetype, source, host, count, action, src, dest, url, src_port, dest_port, file_hash, src_user, recipient, ts_asn, ts_classification, ts_confidence, ts_country, ts_date_first, ts_date_last, ts_detail, ts_id, ts_itype, ts_lat, ts_lon, ts_lookup_key_value, ts_maltype, ts_org, ts_resource_uri, ts_severity, ts_source, ts_type, ts_victim, ts_actor_id, ts_campaign_id, ts_tb_id, anotation, status, updated_by, last_modified, match_type
time_field = _time
time_format = %s.%Q

[ts_ioc_falsepos]
external_type = kvstore
collection = ts_ioc_falsepos
fields_list = _key, _time, indicator, type, updated_by, last_modified, id

[ts_scan_stats]
external_type = kvstore
collection = ts_scan_stats
fields_list = _key, _time, sourcetype, scan_count, first_et, last_et
time_field = _time
time_format = %s.%Q

[ts_audit_logs]
external_type = kvstore
collection = ts_audit_logs
fields_list = _key, _time, level, event, type

[ts_runtime_states]
external_type = kvstore
collection = ts_runtime_states
fields_list = prop, value, first_time, _time



