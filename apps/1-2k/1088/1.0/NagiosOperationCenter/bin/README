How to test scripts:

- dumpnagios is a binary which exports the status data and the action data
  (log file). Test with

  ./dumpnagios /etc/nagios3/nagios.cfg action
  ./dumpnagios /etc/nagios3/nagios.cfg status

  NOTE: This is a x86 file (must works in x86_64 too with 32bits library)

- readnagios.py parse the nagiosoc.conf file and exec dumpnagios using
  nagiosoc parameters.

- setup_nagiosoc_handler is the handler for setup.
