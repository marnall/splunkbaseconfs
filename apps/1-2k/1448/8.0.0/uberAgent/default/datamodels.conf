[uberAgentUXM_Application]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_CitrixBaselineInsights]
acceleration = false
acceleration.earliest_time = -1mon
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_Citrix]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_CitrixADC]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_CitrixSession]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_Config]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_License]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_Logoff]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_Logon]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_OnOffTransition]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_Process]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_Score]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_Session]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_System]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d

[uberAgentUXM_Tags]
acceleration = false
acceleration.earliest_time = -1w
# The following setting "acceleration.allow_skew" is supported on Splunk 7.1+. It causes a (harmless) warning message on older versions during a restart of Splunkd. To remove that, simply comment the next line out and restart Splunkd.
acceleration.allow_skew = 100%
acceleration.manual_rebuilds = true
acceleration.backfill_time = -1d
