[sysstat-sadf]
#REGEX = ^(?<src_host>[^\t]+)\t+(?<interval>[^\t]+)\t+(?<timestamp>[^\t]+)\t+(?<device>[^\t]+)\t+(?<param>[^\t]+)\t+(?<value>[^\t]+)
REGEX = ^([^\t]+)\t+([^\t]+)\t+([^\t]+)\t+([^\t]+)\t+([^\t]+)\t+([^\t]+)
FORMAT = host::$1 interval::$2 timestamp::$3 device::$4 param::$5 value::$6

[hosts_from_sysstat]
filename = hosts_from_sysstat.csv

[params_from_sysstat]
filename = params_from_sysstat.csv

