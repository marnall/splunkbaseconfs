#The below transformations are no longer needed in 4.2 controller APIs as the Application Name and Business transaction
#name comes part of the REST apis. Keeping the entries to support backward compatibility.

#[Controller]
#url = http://<Controller_Host>:<Controller_Port>/
