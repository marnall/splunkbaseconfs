[@internal Commit actions]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(xact_commit) as curr latest(xact_commit) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Rollback actions]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(xact_rollback) as curr latest(xact_rollback) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Blocks read]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(blks_read) as curr latest(blks_read) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Block hits]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(blks_hit) as curr latest(blks_hit) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Returned tuples]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(tup_returned) as curr latest(tup_returned) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Fetched tuples]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(tup_fetched) as curr latest(tup_fetched) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Inserted tuples]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(tup_inserted) as curr latest(tup_inserted) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Updated tuples]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(tup_updated) as curr latest(tup_updated) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Deleted tuples]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(tup_deleted) as curr latest(tup_deleted) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Conflicts]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_database | streamstats current=t global=f window=2 earliest(conflicts) as curr latest(conflicts) as next by datname | eval delta=if(next>curr,next-curr,0) | timechart span=$span$ sum(delta) as delta by datname

[@internal Average operation times]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` eventtype=pgLog eventtype=executionDuration database="$database$" | timechart span=$span$ avg(execDuration) as execution, avg(parseDuration) as parse, avg(bindDuration) as bind

[@internal Average transaction times]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` eventtype=pgLog eventtype=statementExecution database="$database$" | transaction tid startswith="execute *: BEGIN" (endswith=" *: END" OR endswith=" *: COMMIT")| timechart span=$span$ avg(duration) by database

[@internal Transaction count by database]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` eventtype=pgLog eventtype=statementExecution database="$database$" | transaction tid startswith="execute *: BEGIN" (endswith=" *: END" OR endswith=" *: COMMIT")| timechart span=$span$ count by database

[@internal Select count by client host]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=selectStatement eventtype=statementExecution | timechart span=$span$ count by rhost

[@internal Insert count by client host]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=insertStatement eventtype=statementExecution | timechart span=$span$ count by rhost

[@internal Update count by client host]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=updateStatement eventtype=statementExecution | timechart span=$span$ count by rhost

[@internal Delete count by client host]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=deleteStatement eventtype=statementExecution | timechart span=$span$ count by rhost

[@internal Create count by client host]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=createStatement eventtype=statementExecution | timechart span=$span$ count by rhost

[@internal Drop count by client host]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=dropStatement eventtype=statementExecution | timechart span=$span$ count by rhost

[@internal Statement types in time]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=statementExecution statementType=* | eval statementType=lower(statementType) | timechart span=$span$ count by statementType

[@internal Top operations longer than 250 ms]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` eventtype=pgLog eventtype=executionDuration OR eventtype=statementExecution database="$database$" | transaction tid maxevents=2 maxopenevents=2 keepevicted=true startswith=(eventtype=statementExecution) endswith=(eventtype=executionDuration) | where execDuration>250 AND closed_txn=1 | stats max(execDuration) as execDuration last(database) as database by statementText | sort -execDuration | head 10 | fields + execDuration database statementText

[@internal Top relations by input/output metrics (current)]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_statio_all_tables datname="$database$" schemaname="$schema$" | eval relation=datname . "." . schemaname . "." . relname  | chart max(heap_*), max(idx_*), max(toast_*) by relation | addtotals fieldname=total | sort -total | head 20 | rename max(heap_blks_hit) as heapBlocksHit, max(heap_blks_read) as heapBlocksRead, max(idx_blks_hit) as indexBlocksHit, max(idx_blks_read) as indexBlocksRead, max(toast_blks_hit) as toastBlocksHit, max(toast_blks_read) as toastBlocksRead | fields + relation heapBlocksHit heapBlocksRead indexBlocksHit indexBlocksRead toastBlocksHit toastBlocksRead total

[@internal Top indices by input/output metrics (current)]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_statio_all_indexes datname="$database$" schemaname="$schema$" | eval index=datname . "." . schemaname . "." . relname . "." . indexrelname | chart max(idx_*) by index | addtotals fieldname=total | sort -total | head 20 | rename max(idx_blks_hit) as indexBlocksHit, max(idx_blks_read) as indexBlocksRead | fields + index indexBlocksHit indexBlocksRead total

[@internal Top sequences by input/output metrics (current)]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_statio_all_sequences datname="$database$" schemaname="$schema$" | eval sequence=datname . "." . schemaname . "." . relname | chart max(blks_*) by sequence | addtotals fieldname=total | sort -total | head 20 | rename max(blks_hit) as blocksHit, max(blks_read) as blocksRead | fields + sequence blocksHit blocksRead total

[@internal Top relations by tuple metrics (current)]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_all_tables datname="$database$" schemaname="$schema$" | eval relation=datname . "." . schemaname . "." . relname | chart max(n_*) by relation | addtotals fieldname=total | sort -total | head 20 | rename max(n_dead_tup) as deadTuples, max(n_live_tup) as liveTuples, max(n_tup_del) as deletedTuples, max(n_tup_hot_upd) as hotUpdates, max(n_tup_ins) as insertedTuples, max(n_tup_upd) as updatedTuples | fields + relation liveTuples deadTuples deletedTuples insertedTuples updatedTuples hotUpdates total

[@internal Top indices by tuple metrics (current)]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` source=pg_stat_all_indexes datname="$database$" schemaname="$schema$" | eval index=datname . "." . schemaname . "." . relname . "." . indexrelname | stats max(idx_*) by index | addtotals fieldname=total | sort -total | head 20 | rename max(idx_scan) as idxScans, max(idx_tup_fetch) as idxFetchedTuples, max(idx_tup_read) as idxReadTuples | fields + index idxScans idxFetchedTuples idxReadTuples total

[@internal Top statement type by user]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=statementExecution | eval statementType=lower(statementType) | chart count over user by statementType usenull=f

[@internal Select executions by user]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=selectStatement eventtype=statementExecution | timechart span=$span$ count by user

[@internal Insert executions by user]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=insertStatement eventtype=statementExecution | timechart span=$span$ count by user

[@internal Update executions by user]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=updateStatement eventtype=statementExecution | timechart span=$span$ count by user

[@internal Delete executions by user]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=deleteStatement eventtype=statementExecution | timechart span=$span$ count by user

[@internal Create executions by user]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=createStatement eventtype=statementExecution | timechart span=$span$ count by user

[@internal Drop executions by user]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog eventtype=dropStatement eventtype=statementExecution | timechart span=$span$ count by user

[@internal Sessions by user]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgLog | transaction sessionid | timechart span=$span$ count by user

[@internal Most errorneous users]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgError | chart count as errors, latest(_raw) as latest by user | sort -errors | head 10

[@internal Latest errors]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
auto_summarize.dispatch.earliest_time = -1d@h
auto_summarize.timespan = 1m
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = `pgeventdata` `pglog` database="$database$" eventtype=pgError OR eventtype=pgFatal | fields eventtype

[@internal Databases]
is_visible=false
action.email.reportServerEnabled = 0
alert.suppress = 0
alert.track = 0
action.email.inline = 1
alert.digest_mode = True
auto_summarize = 1
auto_summarize.dispatch.earliest_time = 0
cron_schedule = */15 * * * *
dispatch.earliest_time = 0
enableSched = 0
search = `pgeventdata` | stats count by database
