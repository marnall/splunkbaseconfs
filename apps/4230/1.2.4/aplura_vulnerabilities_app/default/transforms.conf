#### SEARCH EXTRACTIONS ###
#Note these collections and items don't actually exist, need to be made
### KV STORE LOOKUPS ####
[apl_va_content_searches]
external_type = kvstore
collection = apl_va_content_searches
fields_list = _key, version, spl, description, guid, created_timestamp, kb_need, search_name

[apl_va_content_news]
external_type = kvstore
collection = apl_va_content_news
fields_list = _key, title, content_blurb, created_timestamp

[apl_va_checkpoints]
external_type = kvstore
collection = apl_va_checkpoints
fields_list = _key, object, last_time

### CSV LOOKUPS ###
