
[source::...(/|\\)var(/|\\)log(/|\\)splunk(/|\\)canary.log]
sourcetype = canary_log
#DATETIME_CONFIG = NONE
LINE_BREAKER = ([\r\n]+)
MAX_TIMESTAMP_LOOKAHEAD = 25
ADD_EXTRA_TIME_FIELDS = false
TIME_PREFIX = ^
BREAK_ONLY_BEFORE_DATE = True
#Commented out until bug for "," in spec fixed.  Auto-timestamp does fine for now.
TIME_FORMAT = %Y-%m-%d %H:%M:%S,%3N
category = Custom
pulldown_type = true
ANNOTATE_PUNCT = false
NO_BINARY_CHECK = true
EXTRACT-canary_log_format = ^\d{4}-\d{2}-\d{2}\s+\d{2}:\d{2}:\d{2},\d{3}\s+(?<log_level>\w+)\s+(?<filename>[^:]*):\s*(?<linenumber>\d+)\s+-\s+((?<runid>\d+)\s+)?(?<message>.*)