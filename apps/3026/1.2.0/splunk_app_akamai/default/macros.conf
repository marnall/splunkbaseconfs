[relative_time(1)]
args = diff
definition = tostring(round(time()) + tonumber($diff$))
iseval = 1

[cp_code_display_name]
definition = eval title=cp_code." (".alias.")" | eval title=coalesce(title,cp_code)

