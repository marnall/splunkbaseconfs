#
# Splunk app configuration file
#

[install]
is_configured = 0
build = 3

[ui]
is_visible = 0
label = World City Info

[launcher]
author = 10Data
description = Adds a command `cityinfo' that takes a column of city name and optionally columns of country and regional code, adds latitude and longitude information, e.g., index=test | eval country="us"| cityinfo cityname country=country
version = 0.9.2

[package]
id = worldcityinfo
