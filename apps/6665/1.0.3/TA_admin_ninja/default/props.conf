[ninja:apps]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")
EVAL-content.disabled = spath(_raw, "content.disabled")

[ninja:authentication]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:authtokens]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:configdirectory]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")
EVAL-content.disabled = spath(_raw, "content.disabled")

[ninja:dashboards]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
TRUNCATE = 99999
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")
EVAL-content.disabled = spath(_raw, "content.disabled")
EVAL-content.label = spath(_raw, "content.label")
EVAL-content.isDashboard = spath(_raw, "content.isDashboard")
EVAL-content.eai:data = spath(_raw, "content.eai:data")

[ninja:dsapps]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:dsclients]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:dsserverclasses]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
TRUNCATE = 99999
LINE_BREAKER = ([\r\n]+)

[ninja:httpinputs]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")

[ninja:indexes]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
TRUNCATE = 99999
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")

[ninja:licenserpools]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:licenses]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:lookups]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")
EVAL-content.disabled = spath(_raw, "content.disabled")

[ninja:messages]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:partitions]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:savedsearches]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
TRUNCATE = 99999
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")
EVAL-content.disabled = spath(_raw, "content.disabled")
EVAL-content.search = spath(_raw, "content.search")

[ninja:serverinfo]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:roles]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:eventtypes]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")
EVAL-content.disabled = spath(_raw, "content.disabled")

[ninja:macros]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")
EVAL-content.disabled = spath(_raw, "content.disabled")
EVAL-content.definition = spath(_raw, "content.definition")

[ninja:datamodel]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
TRUNCATE = 999999
LINE_BREAKER = ([\r\n]+)
EVAL-app = spath(_raw, "app")
EVAL-owner = spath(_raw, "owner")
EVAL-sharing = spath(_raw, "sharing")
EVAL-perms.read = spath(_raw, "perms.read{}")
EVAL-perms.write = spath(_raw, "perms.write{}")
EVAL-datamodel.constraints = spath((spath(_raw, "content.description")), "objects{}.constraints{}")
EVAL-datamodel.label = spath((spath(_raw, "content.description")), "displayName")
EVAL-content.disabled = spath(_raw, "content.disabled")

[ninja:kvstore]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:apps:support]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[ninja:calcfields]
DATETIME_CONFIG = CURRENT
SHOULD_LINEMERGE = 0
KV_MODE = json
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)

[admin_ninja:log]
SHOULD_LINEMERGE = 0
pulldown_type = 1
LINE_BREAKER = ([\r\n]+)