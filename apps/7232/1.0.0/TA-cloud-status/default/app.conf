# this add-on is powered by splunk Add-on builder

[id]
version = 1.0.0
name = TA-cloud-status

[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 1

[launcher]
author = Avotrix
version = 1.0.0
description = Cloud Status Add-on for Splunk

[ui]
is_visible = True
label = Cloud Status Add-on for Splunk
docs_section_override = AddOns:released

[package]
id = TA-cloud-status
check_for_updates = True

[triggers]
reload.addon_builder = simple
reload.ta_cloud_status_account = simple
reload.ta_cloud_status_settings = simple
reload.passwords = simple

