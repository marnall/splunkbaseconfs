#
# Splunk app configuration file
#

[package]
id = sysadmin

[install]
is_configured = 0
state_change_requires_restart = true

[ui]
is_visible = 1
label = sysadmin

[launcher]
author = Anjaneyulu Bollimuntha
description = The Sys Admin app provides an unique view of key metrics and trends derived using splunk internal indexes (_internal,_audit) and rest URI inputs.

version = 1.1

