# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 45

[launcher]
author = JupiterOne, Inc.
version = 2.0.0
description = JupiterOne Add-on for Splunk creates a bidirectional data integration with the JupiterOne platform.

[ui]
is_visible = 1
label = JupiterOne Add-on for Splunk
docs_section_override = AddOns:released

[package]
id = TA-JupiterOne

[id]
name = TA-JupiterOne
version = 2.0.0

[triggers]
reload.addon_builder = simple
reload.ta_jupiterone_account = simple
reload.ta_jupiterone_settings = simple

