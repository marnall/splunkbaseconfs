[Total DLP Incidents]
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" | stats dc(incident_id)

[DLP Incidents by Severity]
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" | chart count by severity

[DLP Incidents by Policy]
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" | chart count by policy

[DLP - Top Endpoint]
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" endpoint != "N/A" | top endpoint

[DLP - Top Protocols]
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" | top protocol

[DLP - Last 10 Incidents]
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" | head 10

[DLP - Last 10 Blocks]
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" blocked="Blocked" | table incident_id, protocol, sender, recipient | head 10

[DLP - Top Sender]
action.email.inline = 1
alert.digest_mode = True
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" sender!="N/A" | top sender

[DLP - Top Recipient]
action.email.inline = 1
alert.digest_mode = True
alert.suppress = 0
alert.track = 0
dispatch.earliest_time = @mon
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index="dlp" recipient!="N/A" | top recipient
