[dhcp]
search = index=network sourcetype=openwrt subsystem_name="dnsmasq-dhcp"

[dns]
search = index=network sourcetype=openwrt subsystem_name=dnsmasq

[wifi]
search = index=network sourcetype=openwrt subsystem_name=hostapd

[openvpn]
search = (sourcetype=openwrt) subsystem_name=openvpn
