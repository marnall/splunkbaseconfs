[ui]
is_visible = false
label = faup

[launcher]
author=Sebastien Tricaud
description=This is the add to help you normalize URLs to extract fields like TLD which can be rather complex for many reasons. Faup stands for Finally An Url Parser and makes sure things are done correctly and faster than with a regex.
version=1.2

[package]
id=faup

[install]
state=enabled
build = 3
