[default]

[install]
is_configured = 0

[launcher]
version = 0.1
description = Primary intent is to visual the average and max download times to specific sites.  Focus is on the Connect To line neat the header info, this is used in both direct and Proxy connections, meaning download performance of Proxy vs Direct can be measured.
author = Scott Bullock

[package]
id = wget

[ui]
is_visible = 1
label = WGET Parser

