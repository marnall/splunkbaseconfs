# Splunk app configuration file

[ui]
is_visible = false
label = MultiCastInput

[launcher]
author=Nimish Doshi
description=This uses scripted inputs to index text based multicast events.
version=1.0.3

[package]
id = multicast

[install]
state=enabled
