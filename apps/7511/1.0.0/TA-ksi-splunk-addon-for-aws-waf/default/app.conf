# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 1

[launcher]
author = Keystone Solutions, Inc.
version = 1.0.0
description = This app automates the blocking of IP address to AWS WAF.

[ui]
is_visible = 1
label = KSI Splunk Addon For AWS WAF
docs_section_override = AddOns:released

[package]
id = TA-ksi-splunk-addon-for-aws-waf

[triggers]
reload.addon_builder = simple
reload.ta_ksi_splunk_addon_for_aws_waf_account = simple
reload.ta_ksi_splunk_addon_for_aws_waf_settings = simple
reload.passwords = simple

