#
# Splunk app configuration file
#

[install]
is_configured = 0
build = 8

[ui]
is_visible = 1
label = Dell ECS App for Splunk
setup_view = setup_view_dashboard

[launcher]
version = 1.2.3
author = Dell Inc.
description = Dell ECS App for Splunk allows you to leverage the ECS platform data within your Splunk Enterprise instance.

[package]
id = DellECSAppforSplunk

[triggers]
reload.app_defaults = simple