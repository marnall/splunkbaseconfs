# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 1

[launcher]
author = Radiflow
version = 1.0.3
description = Radiflow for Splunk allows Radiflow admins and users to view and correlate their existing data within Splunk.

[ui]
is_visible = 1
label = Radiflow Addon For Splunk
docs_section_override = AddOns:released

[package]
id = TA-radiflow-addon-for-splunk

[triggers]
reload.addon_builder = simple
reload.ta_radiflow_addon_for_splunk_account = simple
reload.ta_radiflow_addon_for_splunk_settings = simple
reload.passwords = simple

