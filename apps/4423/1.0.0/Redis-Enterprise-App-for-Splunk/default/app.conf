# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 3

[launcher]
author = Julien Ruaux
version = 1.0.0
description = Searches and dashboards for Redis Enterprise monitoring. For full functionality use this app in combination with the Redis Enterprise Add-On for Splunk

[ui]
is_visible = 1
label = Redis Enterprise App for Splunk
docs_section_override = AddOns:released

[package]
id = Redis-Enterprise-App-for-Splunk

[triggers]
reload.addon_builder = simple
reload.redis_enterprise_app_for_splunk_account = simple
reload.redis_enterprise_app_for_splunk_settings = simple
reload.passwords = simple

