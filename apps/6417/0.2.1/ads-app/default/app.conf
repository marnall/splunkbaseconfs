#
# Splunk app configuration file
#

[install]
build = 0

[package]
id = ads-app

[ui]
is_visible = 1
label = Anomaly Detection Assistant for Splunk (beta)

[launcher]
author = Splunk
description = Anomaly Detection Assistant for Splunk (beta) automatically generates an SPL anomaly detection query which is specifically optimized to provide useful, accurate alerting for a given metric or KPI.
version = 0.2.1

[triggers]
reload.algos = simple
