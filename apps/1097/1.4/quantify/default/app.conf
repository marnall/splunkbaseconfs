#
# Splunk app configuration file
#

[install]
state=enabled
state_change_requires_restart=false
is_configured = 1
build=4

[ui]
is_visible = 0
label = Quantify

[launcher]
author = Ron Naken (ron@splunk.com)
description = Adds a search command to categorize data, like rangemap, while allowing ranges to be created dynamically.
version = 1.4

[package]
check_for_updates = 1
id=quantify
