[Ten Most Active Circles]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` POST_/v3/files/get | where isnull(error) | timechart useother=0 count(_raw) by circleName where max in top10

[Where Data Has Been Opened]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = visualizations
display.page.search.tab = visualizations
display.statistics.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` POST_/v3/files/get  | where isnull(error) | stats count as Total by userName, endpointName, clientIp | iplocation clientIp | eval safeCity=if(City=="", "Unknown City", City) | strcat "'" endpointName "' of '" userName "' in " safeCity userInCity | geostats globallimit=0 sum(Total) as Total by userInCity

[Who's Accessing Data]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` POST_/v3/files/get | where isnull(error) | strcat filePath fileName File | eval Time=strftime(_time, "%m/%d/%y %I:%M:%S:%p") | iplocation clientIp | sort - Time | table File, circleName, userEmail, endpointName, clientIp, Country, City, Time | rename circleName as "Circle", userEmail as "User", endpointName as "Device", clientIp as "IP" | replace "" with "Unknown" in City

[Ten Most Active Users]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` POST_/v3/files/get | where isnull(error) | timechart useother=0 count(_raw) as "Count Of Accesses To Protected Files" by userEmail where max in top10

[Unique Users]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` (((POST_/v3/files/get)) OR (POST_/v3/report type="Decrypt")) | where isnull(error) | timechart distinct_count(userItemId) as "Count Of Unique Users That Accessed Secured Files"

[Ten Most Active Devices]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` POST_/v3/files/get endpointName | where isnull(error) | timechart useother=0 count(_raw) as "Count Of Accesses To Protected Files" by endpointName where max in top10

[Active Users]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` POST_/v3/files/get userItemId | where isnull(error) | top 0 userItemId, userName, userEmail | rename userItemId as "Unique Id", userName as "Name", userEmail as "Email", count as "Actions", percent as "Activity Percent"

[Unique Devices]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` (((POST_/v3/files/get)) OR (POST_/v3/report type="Decrypt")) | where isnull(error) | timechart distinct_count(endpointId) as "Count Of Unique Devices That Accessed Secured Files"

[Active Devices]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` POST_/v3/files/get endpointId, userEmail | where isnull(error) | top 0 endpointId, endpointName, userEmail | rename endpointId as "Unique Id", endpointName as "Name", userEmail as "Owner", count as "Protected Data Access Count", percent as "Activity Percent"

[Number of Files Protected]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` (POST_/v3/report AND type="Encrypt") | where isnull(error) | timechart count(_raw) as "Secured Files"

[Number of Files Protected by Derivative]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` (POST_/v3/report AND type="FileHasBeenDerivedFrom") | where isnull(error) | timechart count(_raw) as "Secured Files"

[Files Protected]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` (POST_/v3/report AND type="Encrypt") | where isnull(error) | spath input=details | eval Time=strftime(_time, "%m/%d/%y %I:%M:%S:%p") | iplocation lang=code clientIp | replace "" with "Unknown" in City | sort - Time | table message, userEmail, endpointName, clientIp, Country, City, Time | rename message as "Protected File", userEmail as "User", endpointName as "Device", circleName as "Circle", clientIp as "IP"

[Whitelisted Files]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` (POST_/v3/fileHashes/add) | where isnull(error) | eval Time=strftime(_time, "%c") | table fileHashName, circleName, userEmail, endpointName, Time | rename fileHashName as "Whitelisted File", circleName as "Circle", userEmail as "User", endpointName as "Device"

[Ten Most Common Errors]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` error endpointId | where isnotnull(error) | top 10 countfield="Times" error | rename error as "Error Message"

[Locations with Suspicious Activity]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = visualizations
display.page.search.tab = visualizations
display.statistics.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` error endpointId | where isnotnull(error) | stats count as Total by userName, endpointName, clientIp | iplocation clientIp | eval safeCity=if(City=="", "Unknown City", City) | strcat "'" endpointName "' of '" userName "' in " safeCity userInCity | geostats globallimit=0 sum(Total) as Total by userInCity

[Suspicious Devices]
action.email.useNSSubject = 1
alert.track = 0
cron_schedule = */30 * * * *
dispatch.earliest_time = -1h@h
dispatch.latest_time = now
display.general.type = statistics
display.page.search.tab = statistics
display.visualizations.show = 0
display.visualizations.type = mapping
enableSched = 1
request.ui_dispatch_app = securecircle
request.ui_dispatch_view = search
search = `default_index` error endpointId | where isnotnull(error) | fillnull value=Unknown | eval Time=strftime(_time, "%c") | sort - Time  | iplocation clientIp | table traceId, error, circleName, userEmail, endpointName, clientIp, Country, City, Time | rename traceId as "ErrorTrace", error as "Error Message", circleName as "Circle", userEmail as "User", endpointName as "Device", clientIp as "IP"
