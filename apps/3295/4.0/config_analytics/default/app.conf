#
# Splunk Configuration Analytics for Nix addon configuration file
#
[ui]
is_visible = 1
label = Configuration Analytics Addon for Nix

[launcher]
author = Andrew Landen
description = This addon indexes Splunk's conf files and optionally summary indexes btool views for analytics, alerting, reporting, and dependency tracking (in development).
version = 4.0

[package]
check_for_updates = 1
id = config_analytics

[install]
build = 17
state = enabled
