# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 1
install_source_checksum = 6e1112e8b2ad903abd4780d89e12eec35bb8e284

[launcher]
author = Etienne LADENT
version = 1.0.4
description = CIM compliant TA for fail2ban events (standard log format)

[ui]
is_visible = 0
label = TA for fail2ban
docs_section_override = AddOns:released

[package]
id = TA-fail2ban

[triggers]
reload.addon_builder = simple
reload.ta_ta_for_fail2ban_account = simple
reload.ta_ta_for_fail2ban_settings = simple
reload.passwords = simple

