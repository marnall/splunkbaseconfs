[Instana Averages Base Search]
dispatch.earliest_time = -15m
search = sourcetype="instana:metrics" \
| stats avg(data.avg_response_time_ms) as "Avg Resp", \
        avg(data.calls_per_second) as "Calls", \
        avg(data.avg_error_rate) as "ErrorRate" by _time, name, type
