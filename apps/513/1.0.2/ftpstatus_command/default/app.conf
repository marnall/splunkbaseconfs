#
# Splunk app configuration file
#

[ui]
is_visible = 0
label = FTP Status


[launcher]
author = Nimish Doshi
version=1.0.2
description = ftpstatus command takes an ftpaddress field and tries to login anonymously using the ftp protocol. It returns a ftpstatus field with connected or "not connected" status per supplied address.

[package]
id=ftpstatus_command


