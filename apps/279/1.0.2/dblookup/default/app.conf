# Splunk app configuration file

[ui]
is_visible = false
label = DBLookup

[launcher]
author=Nimish Doshi
description=This is an example of lookup to use a DB to enrich fields.
version=1.0.2

[package]
id=dblookup
