[script:///opt/splunk/etc/apps/tor_app/bin/tor_input]
index = tor_server
interval = 20 * * * *

[tcp://10514]
connection_host = dns
sourcetype = sophos:utm:firewall

[udp://10514]
connection_host = ip
sourcetype = sophos:firewall
