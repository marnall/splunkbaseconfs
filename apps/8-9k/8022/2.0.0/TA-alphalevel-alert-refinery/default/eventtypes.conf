[alphalevel_notable]
# Events that should receive Alpha Level score enrichment in Splunk ES
# Incident Review. Defaults to notable events; override in local/eventtypes.conf
# to include additional custom alert indexes if needed.
search = index=notable
