[alphalevel_scores]
# Scores and determinations, joined to notables via auto-lookup on al_key.
# Rebuilt every few minutes by the "Alpha Level - Lookup Builder" saved search
# from HEC events landing in the index defined by the alphalevel_results_index macro.
enforceTypes = false

[alphalevel_readiness]
# Latest state of each environment readiness check. One row per check_name.
# Rebuilt on schedule by the readiness saved searches.
enforceTypes = false
