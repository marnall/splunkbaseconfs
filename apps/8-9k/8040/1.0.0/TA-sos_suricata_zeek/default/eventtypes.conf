[zeek_conn]
search = sourcetype=zeek:json source=*conn.log

[zeek_sip]
search = sourcetype=zeek:json source=*sip.log

[zeek_http]
search = sourcetype=zeek:json source=*http.log

[zeek_dns]
search = sourcetype=zeek:json source=*dns.log

[zeek_kerberos]
search = sourcetype=zeek:json source=*kerberos.log

[zeek_ntlm]
search = sourcetype=zeek:json source=*ntlm.log

[zeek_notice]
search = sourcetype=zeek:json source=*notice.log

[zeek_ssl]
search = sourcetype=zeek:json source=*ssl.log

[zeek_x509]
search = sourcetype=zeek:json source=*x509.log

[zeek_known_services]
search = sourcetype=zeek:json source=*known_services.log

[zeek_software]
search = sourcetype=zeek:json source=*software.log

[zeek_known_hosts]
search = sourcetype=zeek:json source=*known_hosts.log

[zeek_known_certs]
search = sourcetype=zeek:json source=*known_certs.log

[zeek_ntp]
search = sourcetype=zeek:json source=*ntp.log

[zeek_files]
search = sourcetype=zeek:json source=*files.log

[zeek_smb_files]
search = sourcetype=zeek:json source=*smb_files.log

[zeek_snmp]
search = sourcetype=zeek:json source=*snmp.log

[zeek_tds]
search = sourcetype=zeek:json source=*tds.log

[zeek_dce_rpc]
search = sourcetype=zeek:json source=*dce_rpc.log

[zeek_stun]
search = sourcetype=zeek:json source=*stun.log

[zeek_rdp]
search = sourcetype=zeek:json source=*rdp.log

[zeek_dhcp]
search = sourcetype=zeek:json source=*dhcp.log

[zeek_broker]
search = sourcetype=zeek:json source=*broker.log

[zeek_weird]
search = sourcetype=zeek:json source=*weird.log

[zeek_smb_mapping]
search = sourcetype=zeek:json source=*smb_mapping.log

[zeek_pe]
search = sourcetype=zeek:json source=*pe.log

[zeek_ocsp]
search = sourcetype=zeek:json source=*ocsp.log

[zeek_tunnel]
search = sourcetype=zeek:json source=*tunnel.log

[zeek_analyzer]
search = sourcetype=zeek:json source=*analyzer.log

[zeek_dpd]
search = sourcetype=zeek:json source=*dpd.log

[zeek_capture_loss]
search = sourcetype=zeek:json source=*capture_loss.log

[suricata_alert]
search = sourcetype=suricata:json event_type=alert