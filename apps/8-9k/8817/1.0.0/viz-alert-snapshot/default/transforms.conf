# Lookup definition over the KV store collection, so configs can also be read
# from SPL (| inputlookup alert_viz_configs) — handy for ops/debugging.
[alert_viz_configs]
external_type = kvstore
collection = alert_viz_configs
fields_list = _key, search_name, app, owner, viz_type, width, height, theme, data_strategy
