[install]
state = enabled
is_configured = false

[ui]
is_visible = 1
label = Visual Alerts

[launcher]
author = Will Searle (LiveHybrid)
description = Render an alert's results as a native Splunk visualization (PNG, via Splunk's bundled Chromium) and deliver it (email now; Telegram/Slack/Teams on the roadmap). Includes a config UI to point saved searches at a viz and preview the exact image that will be sent.
version = 1.0.0

[package]
id = viz-alert-snapshot

[id]
name = viz-alert-snapshot
version = 1.0.0
