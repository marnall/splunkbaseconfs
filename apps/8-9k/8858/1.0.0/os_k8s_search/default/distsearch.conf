# All Kubernetes Search commands and REST handlers run on the search head;
# nothing under the app's bin/ runs on indexer search peers. Exclude bin/
# from the knowledge bundle replicated to peers — it is dead weight there.
# The .conf files still replicate.
[replicationDenylist]
k8s_search_bin = apps/os_k8s_search/bin/...
