[install]
is_configured = 0
build = 20260324

[ui]
is_visible = 1
show_in_nav = 1
label = Kubernetes Search
attribution_link = https://www.outcoldsolutions.com
supported_themes = light, dark
setup_view = setup

[launcher]
author = outcoldsolutions
description = Live Kubernetes API access from Splunk search. Run `| k8s kind=pods` and `| k8slogs pods=*` against any configured cluster — no ingestion required.
version = 1.0.0

[package]
id = os_k8s_search
check_for_updates = 1

[id]
name = os_k8s_search
version = 1.0.0

[triggers]
reload.kubeclusters = simple
reload.kubeauth = simple
reload.k8s_search = simple
