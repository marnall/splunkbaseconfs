# Empty default. Per-user cluster credentials are written per user under
# etc/users/<user>/os_k8s_search/local/kubeauth.conf by the setup page.
# Schema and field reference: README/kubeauth.conf.spec.
