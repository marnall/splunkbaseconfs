#
# Role definitions for Whisper Security Add-on
#
# The whisper_user role grants access to Whisper search commands
# and knowledge objects without requiring admin privileges.
#

[role_whisper_user]
importRoles = user
srchIndexesAllowed = whisper;_internal
srchIndexesDefault = whisper
