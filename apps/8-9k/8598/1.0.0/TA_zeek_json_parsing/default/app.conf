[install]
build = 1
is_configured = 0
state = enabled

[ui]
is_visible = 0
label = TA Zeek JSON Parsing

[launcher]
author = Kaled Aljebur
description = Splunk Technology Add-on for breaking grouped newline-delimited Zeek JSON into individual events and extracting fields at index time
version = 1.0.0

[package]
id = TA_zeek_json_parsing

[id]
name = TA_zeek_json_parsing
version = 1.0.0
