[script://$SPLUNK_HOME/etc/apps/threatbook-api/bin/app_runner.py]
# 启用脚本
disabled = true
interval = 0
# 持续运行模式，不设置interval参数
# 脚本内部会持续运行并管理任务调度
# 设置源类型和索引
sourcetype = threatbook_api
index = _internal
# 设置主机名
host = threatbook-app
# 设置源
source = threatbook_api_runner
python.version = python3.7
