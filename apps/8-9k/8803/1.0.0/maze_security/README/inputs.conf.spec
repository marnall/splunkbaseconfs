[maze_investigations_search://<name>]
account = Maze API account to use.
backfill_days = On first run (no checkpoint), pull investigations updated in the last N days. Set to 0 for all history. (Default: 30)
index = Splunk index for investigation events. (Default: default)
interval = Polling interval in seconds. (Default: 86400)
