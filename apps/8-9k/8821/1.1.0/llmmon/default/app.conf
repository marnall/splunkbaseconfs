[launcher]
author=LLMmon
description=LLM Security Gateway - logs every LLM request and response, proxies traffic to real LLM endpoints
version=1.1.0

[ui]
is_visible = 1
label = LLMmon Gateway

[package]
id = llmmon
check_for_updates = true

[app]
version = 1.1.0
build = 2
is_configured = false
