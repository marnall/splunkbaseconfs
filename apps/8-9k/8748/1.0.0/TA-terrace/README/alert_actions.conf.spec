[terrace_enrich]
param.ip_field = <string>
* Field containing the IP address to look up.
* Default: src_ip

param.api_key = <string>
* Optional: Terrace API key override. If not set, reads from terrace.conf.

param.base_url = <string>
* Optional: API base URL override. If not set, reads from terrace.conf.
