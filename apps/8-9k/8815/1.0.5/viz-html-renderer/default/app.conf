#
# Splunk app configuration file
#

[install]
is_configured = false
state = enabled
build = 1
state_change_requires_restart = false

[ui]
is_visible = true
label = HTML Renderer

[launcher]
author = Will Searle (LiveHybrid)
description = Render arbitrary HTML inside a Splunk Dashboard Studio panel with token interpolation, theme-aware CSS variables, and optional sanitisation. Built for the Splunk Dashboard Contest 2026.
version = 1.0.5

[package]
id = viz-html-renderer
check_for_updates = true

[id]
name = viz-html-renderer
version = 1.0.5
