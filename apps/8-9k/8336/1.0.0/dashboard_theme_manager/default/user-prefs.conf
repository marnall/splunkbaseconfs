# user-prefs.conf for Dashboard Theme Manager
# This file defines user-specific preferences that persist across sessions

[theme_favorites]
# Comma-separated list of favorite theme IDs for the current user
favorites =
