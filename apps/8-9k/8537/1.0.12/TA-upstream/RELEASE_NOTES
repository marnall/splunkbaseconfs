v1.0.12: Improved checkpoint reliability to prevent duplicate alert ingestion.
v1.0.11: Added configurable max events per poll cycle.
v1.0.10: Added support for paginated API responses using marker-based checkpointing.
v1.0.9: Initial public release. Polls the Upstream Security REST API for vehicle cybersecurity alerts, supports configurable polling interval, and writes events to Splunk with UpstreamData sourcetype.

