# Dedicated index for vCISO IOCs. If your environment manages indexes centrally,
# delete this file and point the input's `index =` at an existing index instead.
[vciso_iocs]
homePath   = $SPLUNK_DB/vciso_iocs/db
coldPath   = $SPLUNK_DB/vciso_iocs/colddb
thawedPath = $SPLUNK_DB/vciso_iocs/thaweddb
frozenTimePeriodInSecs = 7776000
