# Make the alert action and config global so any saved search (in any
# app) can enable "Send to Mars" and read the shared webhook settings.

[]
access = read : [ * ], write : [ admin, sc_admin ]
export = system

[alert_actions/mars_alert]
export = system

[mars_alerts]
export = system

[views/mars_setup]
export = system
