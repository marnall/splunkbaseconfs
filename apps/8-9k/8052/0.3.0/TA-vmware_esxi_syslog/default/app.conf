[ui]
is_visible = true
is_manageable = false
label = Technology Add-on for VMware ESXi Syslog (Community)

[launcher]
author = You
version = 0.3.0
description = Splunk TA for VMware ESXi syslog (vmw-syslog) with CIM Authentication mapping and a simple Auth & Storage dashboard

[install]
state = enabled

[package]
id = TA-vmware_esxi_syslog

[id]
name = TA-vmware_esxi_syslog
version = 0.3.0
