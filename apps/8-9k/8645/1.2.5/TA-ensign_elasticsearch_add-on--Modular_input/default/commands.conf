# =========================================================================
# CUSTOM SPL COMMANDS FOR ELASTICSEARCH DIAGNOSTICS (v1.2.3)
# =========================================================================
# These commands are app-restricted — they only run within this add-on's
# context. Attempting to run them from another app will produce an error.
# =========================================================================

[escheck_connection]
filename = escheck_connection.py
chunked = true
python.version = python3
python.required = 3.9, 3.13

[escheck_config]
filename = escheck_config.py
chunked = true
python.version = python3
python.required = 3.9, 3.13

[escheck_indexes]
filename = escheck_indexes.py
chunked = true
python.version = python3
python.required = 3.9, 3.13
