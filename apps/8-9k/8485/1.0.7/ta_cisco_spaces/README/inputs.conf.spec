[cisco_spaces_firehose://<name>]
index = Select an index to ingest the data
interval = In case the connection is closed, how many seconds for the input to wait before establishing the connection again. (Default: 300)
stream = Meta Stream to use for this input.
