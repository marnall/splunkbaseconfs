[rest_profiler_send_alert]
param.profile = <list> Profile. It's a required parameter.
param.store_response = <bool> Index the response as an event.  It's default value is 1.
param.result_index = <string> Result index.  It's default value is main.
