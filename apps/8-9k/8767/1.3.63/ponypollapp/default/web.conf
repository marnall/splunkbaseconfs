[expose:ponypoll_kv_questions]
pattern = storage/collections/data/ponypoll_questions*
methods = GET,POST,DELETE

[expose:ponypoll_kv_config]
pattern = storage/collections/data/ponypoll_config*
methods = GET,POST,DELETE

[expose:ponypoll_kv_quizzes]
pattern = storage/collections/data/ponypoll_quizzes*
methods = GET,POST,DELETE

[expose:ponypoll_receivers]
pattern = receivers/simple*
methods = POST

[expose:ponypoll_search]
pattern = search/jobs*
methods = GET,POST

[expose:ponypoll_kv_session]
pattern = storage/collections/data/ponypoll_session*
methods = GET,POST,DELETE

[expose:ponypoll_kv_presence]
pattern = storage/collections/data/ponypoll_presence*
methods = GET,POST,DELETE

# Read + write the `ponypoll_index` search macro so the Settings UI can show
# the currently-configured index and persist changes without leaving the app.
# Scoped to a single named macro (no wildcard) so the app cannot read or alter
# any other search macros in the namespace.
[expose:ponypoll_macros]
pattern = configs/conf-macros/ponypoll_index*
methods = GET,POST

# Allow the System Check to call /data/indexes/<name> to verify the configured
# index exists and report its event count.
[expose:ponypoll_indexes]
pattern = data/indexes*
methods = GET
