[crowdstrike_falcon_detections://<name>]
account = OAuth2 API credential with 'alerts:read' scope
cloud = Select the appropriate cloud environment
index = (Default: default)
interval = Time interval of input in seconds. (Default: 300)
product = Select the products to collect detection events from
start_date = Only collect events on or after this date (YYYY-MM-DD)
sourcetype = crowdstrike:falcon:detections:json
python.version = <string>