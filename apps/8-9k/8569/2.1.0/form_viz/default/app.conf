#
# Splunk app configuration file
#
[id]
name = form_viz
version = 2.1.0
build = 21

[install]
is_configured = 0

[ui]
is_visible = 1
label = Form Viz

[launcher]
author = yuri.baker
description = Render SPL results as a dynamic form with freetext, dropdown, radio, checkbox, number, date, spl, description, markdown, and spl_display fields. Supports optional order/helper markdown and SPL auto-formatting, and writes responses to KV Store.
version = 2.1.0

[package]
id = form_viz
check_for_updates = true
