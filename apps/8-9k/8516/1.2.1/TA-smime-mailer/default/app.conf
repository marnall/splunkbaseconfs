[install]
is_configured = false
build = 1

[ui]
is_visible = true
label = S/MIME Mailer
docs_section_override = AddOns:released

[launcher]
author = Service & Support
description = Send S/MIME signed and encrypted emails from Splunk. Supports SMTP (port 25 with STARTTLS), SMTPS (port 465), Submission (port 587), and OAuth2 (XOAUTH2) authentication. Integrates with Splunk Enterprise Security alert actions.
version = 1.2.1

[package]
id = TA-smime-mailer

[id]
name = TA-smime-mailer
version = 1.2.1

[triggers]
reload.smime_mailer_settings = simple
reload.smime_recipient_certs = simple
reload.smime_sender_certs = simple
reload.script:smime_cert_manager = simple
reload.script:smime_cert_monitor = simple
