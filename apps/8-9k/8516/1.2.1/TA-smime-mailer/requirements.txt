# requirements.txt - Python dependencies for TA-smime-mailer
#
# These must be installed into the app's lib/ directory.
# Run: pip install -r requirements.txt --target lib/
#
# Note: 'splunklib' is provided by the Splunk SDK and should be installed
# separately or bundled from the Splunk Python SDK.

cryptography>=41.0.0
asn1crypto>=1.5.0
oscrypto>=1.3.0
