[bitbucket_user://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_projects://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_workspace://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_repository://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_commits://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_pullrequests://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_pullrequests_activity://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_branch://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_repository_user_permissions://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_repository_group_permissions://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_pipelines://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_workspace_user_permissions://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_deployments://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_project_user_permissions://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_project_group_permissions://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_workspace_webhooks://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_repository_webhooks://<name>]
account = 
index = Default: default
interval = Time interval of the data input, in seconds.
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes

[bitbucket_audit_log://<name>]
account = 
api_key = 
end_date = The date and time on or after which returned audit records must have been created. Format: YYYY-MM-DDThh:mm:ss Note: Please use UTC time  Default:
index = Default: default
interval = Time interval of the data input, in seconds.
org_id = 
start_date = The date and time on or after which returned audit records must have been created. Format: YYYY-MM-DDThh:mm:ss Note: Please use UTC time  Default:
use_existing_checkpoint = Select whether or not to reuse the checkpoint file already available for the named input.  Default: yes
