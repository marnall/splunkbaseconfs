#
# Declare shared visualizations
#

[dag_viewer]
label = DAG Viewer
description = Render Directed Acyclic Graphs (DAG) from GraphViz DOT format. Supports both raw DOT strings and structured node/edge data.
