[phishiqplus_source_hash_extract]
REGEX = \"phishiq_source_event_hash\"\s*:\s*\"([^\"]+)\"
FORMAT = phishiq_source_event_hash::$1

# Shipped demo lookup (benign URLs) for Manual Test and dynamic mode examples.
[demo_phishiq_urls]
filename = demo_phishiq_urls.csv
