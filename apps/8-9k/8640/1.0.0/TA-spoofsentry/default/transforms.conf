#
# Transforms for SpoofSentry CIM mapping and lookups
#

[spoofsentry_severity_lookup]
filename = spoofsentry_severity.csv
match_type = WILDCARD(severity)
min_matches = 1
default_match = unknown

[spoofsentry_event_type_lookup]
filename = spoofsentry_event_types.csv
match_type = WILDCARD(event_type)
min_matches = 1
default_match = unknown
