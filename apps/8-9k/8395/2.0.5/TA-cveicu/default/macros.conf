# Configurable index for all TA-cveicu searches.
# Override in local/macros.conf to use a different index:
#   [cveicu_index]
#   definition = index=my_cve_index

[cveicu_index]
definition = index=main
iseval = 0
