
[ccx_hitachi_products_log_extraction]
CLEAN_KEYS = 0
REGEX = CEF:\d+\|(?<log_type>[^\|]+)\|HNAS\|(?<log_ip>(?:\d{1,4}\.){3}\d{1,4})\|\d+\|(?<log_category>[^\|]+)\|(?<log_level>\d+)\|.+

[ccx_hitachi_products_extraction]
CLEAN_KEYS = 0
FORMAT = $1::$2
REGEX = \s+(\w+)=\"?(.*?(?=\"?\s\w+=|$))
