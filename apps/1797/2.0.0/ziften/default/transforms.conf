[vtlookup]
external_cmd = vt_lookup.py md5 details score hits sources lookup_time
fields_list = md5, details, score, hits, sources, lookup_time

[otxlookup]
external_cmd = otx_lookup.py ipaddress reliability risk classification countrycode city lat lon revision unknown
fields_list = ipaddress, reliability, risk, classification, countrycode, city, lat, lon, revision, unknown


[system_info]
filename = systems.csv

[image_info]
filename = image.csv

[ports]
filename = service-names-port-numbers.csv

