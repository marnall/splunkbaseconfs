# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 16

[launcher]
author = IONIX
version = 1.0.4
description = IONIX's official Splunk app. Adds visualizations and correctly indexes data sent from IONIX via HTTP Event Collector.

[ui]
is_visible = 1
label = IONIX App for Splunk
docs_section_override = AddOns:released

[package]
id = cyberpion_security_logs
check_for_updates = true

[triggers]
reload.addon_builder = simple
reload.cyberpion_security_logs_account = simple
reload.cyberpion_security_logs_settings = simple
reload.passwords = simple

