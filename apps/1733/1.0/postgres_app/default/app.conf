#
# Splunk app configuration file
#

[install]
is_configured = 0
build = 0

[ui]
is_visible = 1
label = App for Postgres

[launcher]
author = Linux Polska
description = An application dedicated to PostgreSQL monitoring and insight.  It is intended to provide detailed view of PostgreSQL database condition, including metrics and query analysis. The application is build on top of PostgreSQL TA.
version = 1.0

[package]
id = postgres_app
