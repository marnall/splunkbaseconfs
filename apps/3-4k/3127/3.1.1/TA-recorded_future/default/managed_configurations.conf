###############################
# Splunk managed_configuration file
###############################

[lookup:recordedFutureIpThreatList]
description = Recorded Future IP Threat List
endpoint = /services/data/transforms/lookups/recordedFutureIpThreatList
label = recordedFutureIpThreatList
lookup_type = adhoc

[lookup:recordedFutureHashThreatList]
description = Recorded Future Hash Threat List
endpoint = /services/data/transforms/lookups/recordedFutureHashThreatList
label = recordedFutureHashThreatList
lookup_type = adhoc

[lookup:recordedFutureDomainThreatList]
description = Recorded Future Domain Threat List
endpoint = /services/data/transforms/lookups/recordedFutureDomainThreatList
label = recordedFutureDomainThreatList
lookup_type = adhoc

[lookup:recordedFutureVulnerabilityThreatList]
description = Recorded Future Vulnerability Threat List
endpoint = /services/data/transforms/lookups/recordedFutureVulnerabilityThreatList
label = recordedFutureVulnerabilityThreatList
lookup_type = adhoc
