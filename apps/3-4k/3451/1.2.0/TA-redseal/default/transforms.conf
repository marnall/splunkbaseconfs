#
# Splunk app KV Store transforms file
#

[kv_host_metric_lookup]
external_type = kvstore
collection = host_metric_collection
fields_list = _key, name, ipAddress, serverName, attackDepth, exposure, value, risk, downstreamrisk, exploitable, accessibleFromUntrusted, hasaccessToCritical
