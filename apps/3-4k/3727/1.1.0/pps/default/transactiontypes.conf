[pps-message-processed]
startswith="mod=session cmd=connect"
endswith="mod=session cmd=disconnect"
keepevicted=true
fields=s

[mta-mm-message-processed]
startswith="tls_verify"
endswith="mailer=* delay=* stat=sent"
keepevicted=true
fields=host, qid
connected=f

