# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 2

[launcher]
author = Coen Meerbeek
version = 2.0.0
description = Version 2 of the TA for Octopus Deploy build using the Splunk AoB.

[ui]
is_visible = 1
label = OctopusDeploy
docs_section_override = AddOns:released

[package]
id = TA-octopus_deploy

[triggers]
reload.addon_builder = simple
reload.ta_octopus_deploy_account = simple
reload.ta_octopus_deploy_settings = simple
reload.passwords = simple

