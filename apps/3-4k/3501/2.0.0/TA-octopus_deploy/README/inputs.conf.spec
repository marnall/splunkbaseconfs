[octopus_endpoint://<name>]
octopus_uri = e.g. http(s)://example.com/<subdir>
octopus_verify_ssl = Check box if you want to verify SSL cert.
octopus_api_endpoint = Select an endpoint from the dropdown.
octopus_use_checkpoint = Check box if checkpointing is needed