
[default]

[install]
is_configured = 0

[launcher]
description = SYSMON Integration Framework Basic is a set of rules and dashboards for visualization of multiple security checks on Sysmon’s events of Windows hosts. It allows to detect anomalies (further analysis is needed to check if they are caused by malware, user's data leakage intentions, etc.).
author = SOC Prime
version = 1.0.1

[package]
check_for_updates = 1
id = SOCPrimeSYSMONIntegrationFrameworkBasic

[ui]
label = SYSMON Integration Framework Basic
is_visible = 1
