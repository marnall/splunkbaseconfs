<setup>

<!-- Note that the path to the script uses URI encoding -->	
<block title="Scheduled Searches">
	<text>
	  &lt;i&gt;Below are the default Cron Schedule times. If needed adjust the schedule now or later under 'manage apps' selecting 'Set up' under the Actions column for Splunk Assessment of Mitigation Implementations.&lt;/i&gt;
	</text>
</block>

<block title="Configure Cron Schedule" 
       endpoint="saved/searches" entity="*" mode="iter">
    <text>
      &lt;i&gt;&lt;b&gt;Iteration mode&lt;/b&gt;:
      specify the cron schedule for each search in the conf file.&lt;/i&gt;</text>
    <input field="cron_schedule">
      <label>$name$</label>
      <type>text</type>
    </input>
  </block>

</setup>