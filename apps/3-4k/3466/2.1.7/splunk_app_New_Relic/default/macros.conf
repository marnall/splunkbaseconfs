[get_sources(4)]
args = new_sourcetype,new_source,old_source,account_id
definition = (sourcetype=$new_sourcetype$ source=$new_source$) OR \
(sourcetype=newrelic_account source="$old_source$" account_id=$account_id$ )\
| eval new_source = if(sourcetype="newrelic_account",input_name +":" + account_id,source),\
       account_id = if(sourcetype="newrelic_account",account_id, rtrim(new_source,":") )
iseval = 0
