[hex2binary(1)]
args = hex
definition = eval binary = upper($hex$)\
| eval binary = replace(binary,"0","0000")\
| eval binary = replace(binary,"1","0001")\
| eval binary = replace(binary,"2","0010")\
| eval binary = replace(binary,"3","0011")\
| eval binary = replace(binary,"4","0100")\
| eval binary = replace(binary,"5","0101")\
| eval binary = replace(binary,"6","0110")\
| eval binary = replace(binary,"7","0111")\
| eval binary = replace(binary,"8","1000")\
| eval binary = replace(binary,"9","1001")\
| eval binary = replace(binary,"A","1010")\
| eval binary = replace(binary,"B","1011")\
| eval binary = replace(binary,"C","1100")\
| eval binary = replace(binary,"D","1101")\
| eval binary = replace(binary,"E","1110")\
| eval binary = replace(binary,"F","1111")\
| eval binary = ltrim(tostring(binary),"0")
errormsg = 
iseval = 0
validation = 
