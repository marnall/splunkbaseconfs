[earthquake-fields]
DELIMS = ,
FIELDS = time,latitude,longitude,depth,mag,magType,nst,gap,dmin,rms,net,id,updated,place,type

[email-fields]
DELIMS = ,
FIELDS = time,message_id,sc_ip,sender,recipient_count,recipients,total_bytes

[vendors_lookup]
filename = vendors.csv

[prices_lookup]
filename = prices.csv

[httpstatuscodes_lookup]
filename = httpstatuscodes.csv

[usertoip]
filename = usertoip.csv

[expected_hosts]
filename = expected_hosts.csv

[cmdlookup]
filename = cmdlookup.csv
