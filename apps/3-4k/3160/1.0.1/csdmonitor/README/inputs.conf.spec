[csd://<name>]

* Edge cache IP
client_ip = <value>

* ClearSky customer ID
customer_id = <value>

* login username
auth_user = <value>

* password
auth_password = <value>

* time to wait for reconnect after timeout or error
backoff_time = <value>

* in seconds
polling_interval= <value>
