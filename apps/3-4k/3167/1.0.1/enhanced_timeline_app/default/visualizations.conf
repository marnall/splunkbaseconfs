#
# Declare shared visualizations
#

[enhanced_timeline]
label = Enhanced Timeline
description = Display and categorize events over time on a timline and allow to zoom in/out (using mouse wheel).
search_fragment = | table content start end category