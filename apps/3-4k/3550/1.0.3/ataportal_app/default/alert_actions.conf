[ataportal]
is_custom = 1
label = ATA IR Portal Alert Action 
description = Send Security Events to ATA IR Portal
icon_path = ata_portal_alert_icon.png
payload_format = json

param.base_url =
param.auth_token =
param.psa_id =

param.search_query = $job.search$
param.search_earliest = $job.earliestTime$
param.search_latest = $job.latestTime$