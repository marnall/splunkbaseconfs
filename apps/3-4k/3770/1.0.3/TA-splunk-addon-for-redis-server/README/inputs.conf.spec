[redis://<name>]
redis_host = Redis server host
redis_port = Redis server port
authentication = Redis server authentication
password = Required only if authentication is set to password. Otherwise leave it blank.