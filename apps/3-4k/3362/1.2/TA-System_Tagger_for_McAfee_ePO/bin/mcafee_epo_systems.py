import ta_system_tagger_for_mcafee_epo_declare

import os
import sys
import time
import datetime

import modinput_wrapper.base_modinput
from solnlib.packages.splunklib import modularinput as smi



import input_module_mcafee_epo_systems as input_module


'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_mcafee_epo_systems.py
'''
class ModInputmcafee_epo_systems(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputmcafee_epo_systems, self).__init__("ta_system_tagger_for_mcafee_epo", "mcafee_epo_systems", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputmcafee_epo_systems, self).get_scheme()
        scheme.title = ("McAfee ePO Systems")
        scheme.description = ("Collects a list of all systems in McAfee ePO along with other system properties")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("input_name", title="Input Name",
                                         description="",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-System_Tagger_for_McAfee_ePO"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_fields = []
            customized_settings = {u'type': u'customized', u'description': u'Collects a list of all systems in McAfee ePO along with other system properties', u'customized_options': [{u'name': u'input_name', u'value': u'all_systems'}], u'name': u'mcafee_epo_systems', 'sourcetype': u'mcafee:epo:systems', u'data_inputs_options': [{u'required_on_edit': False, u'required_on_create': False, u'type': u'customized_var', u'format_type': u'text', u'description': u'', u'default_value': u'all_systems', u'name': u'input_name', u'title': u'Input Name', u'placeholder': u''}], u'is_loaded': True, 'interval': u'14400', 'use_external_validation': True, 'streaming_mode_xml': True, u'disabled': True, 'index': u'default', u'code': u'\n# encoding = utf-8\n\nimport os\nimport sys\nimport time\nimport datetime\n\n\'\'\'\n    IMPORTANT\n    Edit only the validate_input and collect_events functions.\n    Do not edit any other part in this file.\n    This file is generated only once when creating the modular input.\n\'\'\'\n\'\'\'\n# For advanced users, if you want to create single instance mod input, uncomment this method.\ndef use_single_instance_mode():\n    return True\n\'\'\'\n\ndef validate_input(helper, definition):\n    """Implement your own validation logic to validate the input stanza configurations"""\n    # This example accesses the modular input variable\n    # input_name = definition.parameters.get(\'input_name\', None)\n    try:\n        global_epo_server = helper.get_global_setting("epo_server")\n        global_epo_port = helper.get_global_setting("epo_port")\n        global_epo_user = helper.get_global_setting("epo_user")\n        global_epo_password = helper.get_global_setting("epo_password")\n    except:\n        sys.exit() \n    pass\n\ndef collect_events(helper, ew):\n    import base64, re, string\n    """Implement your data collection logic here\n\n    # The following examples get the arguments of this input.\n    # Note, for single instance mod input, args will be returned as a dict.\n    # For multi instance mod input, args will be returned as a single value.\n    opt_input_name = helper.get_arg(\'input_name\')\n    # In single instance mode, to get arguments of a particular input, use\n    opt_input_name = helper.get_arg(\'input_name\', stanza_name)\n\n    # get input type\n    helper.get_input_type()\n\n    # The following examples get input stanzas.\n    # get all detailed input stanzas\n    helper.get_input_stanza()\n    # get specific input stanza with stanza name\n    helper.get_input_stanza(stanza_name)\n    # get all stanza names\n    helper.get_input_stanza_names()\n\n    # The following examples get options from setup page configuration.\n    # get the loglevel from the setup page\n    loglevel = helper.get_log_level()\n    # get proxy setting configuration\n    proxy_settings = helper.get_proxy()\n    # get account credentials as dictionary\n    account = helper.get_user_credential_by_username("username")\n    account = helper.get_user_credential_by_id("account id")\n    # get global variable configuration\n    global_epo_server = helper.get_global_setting("epo_server")\n    global_epo_port = helper.get_global_setting("epo_port")\n    global_epo_user = helper.get_global_setting("epo_user")\n    global_epo_password = helper.get_global_setting("epo_password")\n\n    # The following examples show usage of logging related helper functions.\n    # write to the log for this modular input using configured global log level or INFO as default\n    helper.log("log message")\n    # write to the log using specified log level\n    helper.log_debug("log message")\n    helper.log_info("log message")\n    helper.log_warning("log message")\n    helper.log_error("log message")\n    helper.log_critical("log message")\n    # set the log level for this modular input\n    # (log_level can be "debug", "info", "warning", "error" or "critical", case insensitive)\n    helper.set_log_level(log_level)\n\n    # The following examples send rest requests to some endpoint.\n    response = helper.send_http_request(url, method, parameters=None, payload=None,\n                                        headers=None, cookies=None, verify=True, cert=None,\n                                        timeout=None, use_proxy=True)\n    # get the response headers\n    r_headers = response.headers\n    # get the response body as text\n    r_text = response.text\n    # get response body as json. If the body text is not a json string, raise a ValueError\n    r_json = response.json()\n    # get response cookies\n    r_cookies = response.cookies\n    # get redirect history\n    historical_responses = response.history\n    # get response status code\n    r_status = response.status_code\n    # check the response status, if the status is not sucessful, raise requests.HTTPError\n    response.raise_for_status()\n\n    # The following examples show usage of check pointing related helper functions.\n    # save checkpoint\n    helper.save_check_point(key, state)\n    # delete checkpoint\n    helper.delete_check_point(key)\n    # get checkpoint\n    state = helper.get_check_point(key)\n\n    # To create a splunk event\n    helper.new_event(data, time=None, host=None, index=None, source=None, sourcetype=None, done=True, unbroken=True)\n    """\n\n    \'\'\'\n    # The following example writes a random number as an event. (Multi Instance Mode)\n    # Use this code template by default.\n    import random\n    data = str(random.randint(0,100))\n    event = helper.new_event(source=helper.get_input_type(), index=helper.get_output_index(), sourcetype=helper.get_sourcetype(), data=data)\n    ew.write_event(event)\n    \'\'\'\n\n    \'\'\'\n    # The following example writes a random number as an event for each input config. (Single Instance Mode)\n    # For advanced users, if you want to create single instance mod input, please use this code template.\n    # Also, you need to uncomment use_single_instance_mode() above.\n    import random\n    input_type = helper.get_input_type()\n    for stanza_name in helper.get_input_stanza_names():\n        data = str(random.randint(0,100))\n        event = helper.new_event(source=input_type, index=helper.get_output_index(stanza_name), sourcetype=helper.get_sourcetype(stanza_name), data=data)\n        ew.write_event(event)\n    \'\'\'\n    # get input type\n    helper.get_input_type()\n    # The following examples get input stanzas.\n    # get all detailed input stanzas\n    helper.get_input_stanza()\n    # get specific input stanza with stanza name\n    #helper.get_input_stanza(stanza_name)\n    # get all stanza names\n    helper.get_input_stanza_names()\n    # get the loglevel from the setup page\n    loglevel = helper.get_log_level()\n    # get proxy setting configuration\n    proxy_settings = helper.get_proxy()\n    \n    epo_server = helper.get_global_setting("epo_server")\n    epo_port = helper.get_global_setting("epo_port")\n    epo_user = helper.get_global_setting("epo_user")\n    epo_password = helper.get_global_setting("epo_password")\n\n    # encode credentials for basic auth\n    u = \'{}:{}\'.format(epo_user,epo_password)\n    base64string = base64.b64encode(u)\n    headers = {\'Authorization\':\'Basic {} \'.format(base64string)}\n\n    # loop for each event to capture unique system_name\n    # configure alert to fire "for each event" for this to work correctly\n\n    epo_url = "https://"+epo_server+":"+epo_port+"/remote/system.find?searchText="\n    # hit the ePO API with auth, the tag, the system\n    response = helper.send_http_request(epo_url, "GET", parameters=None, payload=None, headers=headers, cookies=None, verify=False, cert=None, timeout=None, use_proxy=True)\n    \n    lines = response.content.split(\'\\n\')\n    data = \'\'\n    \n    for line in lines:\n        if line.find("OK") != -1:\n            continue\n        if line.find(":") == -1:\n            \n            data = data + \'\\r\\n\'\n            event = helper.new_event(source=helper.get_input_type(), index=helper.get_output_index(), sourcetype=helper.get_sourcetype(), data=data)\n            ew.write_event(event)\n            data =\'\'\n            continue\n        else:\n            line = line.replace(": ","=\\"")\n            data = data + line.rstrip(\'\\r\\n\') + \'\\" \' \n    ', u'parameters': [{u'required': False, u'type': u'text', u'format_type': u'text', u'value': u'all_systems', u'label': u'Input Name', u'default_value': u'all_systems', u'name': u'input_name', u'placeholder': u'', u'help_string': u''}], u'uuid': u'd79517cfa1f84f9fbe988802396b69e4', u'title': u'McAfee ePO Systems', u'sample_count': u'2'}.get('global_settings', {}).get('customized_settings', [])
            for global_var in customized_settings:
                if global_var.get('type', '') == 'checkbox':
                    checkbox_fields.append(global_var['name'])
            self.global_checkbox_fields = checkbox_fields
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputmcafee_epo_systems().run(sys.argv)
    sys.exit(exitcode)
