[instana_metrics://<name>]
entity_type = 
select_all = The "Entity Name" field below will be ignored once this check box is selected. Instead of one specific application/service, the whole set of all available applications/services will be selected.
entity_name =