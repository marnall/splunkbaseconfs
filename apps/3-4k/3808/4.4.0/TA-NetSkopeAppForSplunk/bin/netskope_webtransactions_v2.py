import ta_netskopeappforsplunk_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi

import input_module_netskope_webtransactions_v2 as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_netskope_webtransactions_v2.py
'''


class ModInputnetskope_webtransactions_v2(modinput_wrapper.base_modinput
                                          .BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputnetskope_webtransactions_v2, self).__init__(
              "ta_netskopeappforsplunk", "netskope_webtransactions_v2"
               , use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputnetskope_webtransactions_v2, self).get_scheme()
        scheme.title = ("Netskope Web Transactions")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("global_account", title="Netskope Account",
                                         description="Select the account for which you want to collect data.",
                                         required_on_create=True,
                                         required_on_edit=True))
        scheme.add_argument(smi.Argument("idle_connection_timeout", title="Idle Connection Timeout",
                                         description="Connection will be reestablished, once the provided timeout (in seconds) occurs on Idle connection.",
                                         required_on_create=True,
                                         required_on_edit=True))
        scheme.add_argument(smi.Argument("max_webtxn_files", title="Max Webtxn Files",
                                         description="If Splunk can't keep up with webtxn data ingestion and files exceed Max Webtxn Files, collection pauses until space is available. On setting value as 0, these restrictions won't be applied.",
                                         required_on_create=True,
                                         required_on_edit=True))
        scheme.add_argument(smi.Argument("enable_custom_spool_path", title="Enable Custom Webtxn Path",
                                         description="Enable using custom path to store the Webtransaction log files before it gets ingested into the Splunk.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("custom_spool_path", title="Custom path",
                                         description="Enter the absolute path to store the Webtransaction log files. E.g.: /opt/splunk/var/target_dir/",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("fields_include_exclude", title="Fields to Include",
                                         description="Refine the data by manually specifying the fields you'd like to include or exclude.",
                                         required_on_create=True,
                                         required_on_edit=True))
        scheme.add_argument(smi.Argument("fields_include", title="",
                                         description="Enter comma seprated fields to Include. (E.g. field1,field2,field3)",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("fields_exclude", title="Fields to Exclude",
                                         description="Enter comma seprated fields to Exclude. (E.g. field1,field2,field3)",
                                         required_on_create=False,
                                         required_on_edit=False))

        return scheme

    def get_app_name(self):
        return "TA-NetSkopeAppForSplunk"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        account_fields.append("global_account")
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields


if __name__ == "__main__":
    exitcode = ModInputnetskope_webtransactions_v2().run(sys.argv)
    sys.exit(exitcode)
