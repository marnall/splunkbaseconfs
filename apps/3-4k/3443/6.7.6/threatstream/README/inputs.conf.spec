[ts_ioc_ingest://<name>]
# python.version is required to ensure that older versions of Splunk don't error out on encountering it
# seen on 7.3.0
remote_username = <value>
remote_update = <bool>
remote_password = <value>
remote_port = <int>
remote_host = <value>
es_intel_upload = <bool>
python.version = <value>