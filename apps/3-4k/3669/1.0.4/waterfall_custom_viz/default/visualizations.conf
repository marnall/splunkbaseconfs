#
# Splunk visualization configuration file
#

[waterfall]

label = Waterfall Custom Visualization
description = Plot cumulative effect of sequential positive and negative values
search_fragment = | stats sum(fieldA) by fieldB | addcoltotals labelfield=fieldB label=Total
default_height = 300
allow_user_selection = true
supports_drilldown = true
supports_trellis = false
disabled = 0