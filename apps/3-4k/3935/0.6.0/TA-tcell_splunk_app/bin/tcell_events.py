from builtins import str
import ta_tcell_splunk_app_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi


import input_module_tcell_events as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_tcell_events.py
'''


class ModInputtcell_events(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputtcell_events, self).__init__(
            "ta_tcell_splunk_app", "tcell_events", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputtcell_events, self).get_scheme()
        scheme.title = ("Collect inputs from tCell")
        scheme.description = (
            "Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("tcell_company_name", title="tCell Company Name",
                                         description="If your tCell domain is widgets.tcell.io, enter widgets here.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("tcell_api_key", title="tCell API key",
                                         description="Generate a tCell user API key in the web UI:\n \"Log in as <Your Name>\" -> Account Settings -> API Keys -> User Key -> New User Key. Must be a global read-only API key if collecting from ALL apps.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("tcell_app_id", title="tCell app ID",
                                         description="Find your app ID under Admin -> Applications in the \"Application ID\" column. The app ID is in the url subdirectory when viewing a particular app in the web UI. Required if 'tCell apps' isn't selected.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("tcell_apps", title="tCell apps",
                                         description="Collect from ALL tCell apps. Selecting this overrides the 'tCell app ID' field.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("appsensor", title="Collect App Firewall events",
                                         description="Suspicious behavior by requests against the app",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("vulnerabilities", title="Package Vulnerabilities",
                                         description="Vulnerabilities in packages and libraries used within the app",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("packages", title="Packages",
                                         description="Packages and libraries used within the app",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("login", title="Collect User Logins",
                                         description="Unusual login behavior information",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("inline", title="Inline Scripts",
                                         description="Unexpected inline scripts detected by the jsagent within the users\' browsers.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("csp", title="Collect CSP Violations",
                                         description="Content Security Policy violation information reported by user browsers",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("cmdi", title="OS Commands",
                                         description="Unusual, unallowed shell command",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("lfi", title="Local File",
                                         description="A malicious local file included or imported in the app",
                                         required_on_create=False,
                                         required_on_edit=False))

        return scheme

    def get_app_name(self):
        return "TA-tcell_splunk_app"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("tcell_apps")
        checkbox_fields.append("appsensor")
        checkbox_fields.append("vulnerabilities")
        checkbox_fields.append("packages")
        checkbox_fields.append("login")
        checkbox_fields.append("inline")
        checkbox_fields.append("csp")
        checkbox_fields.append("cmdi")
        checkbox_fields.append("lfi")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(
                bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error(
                    'Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields


if __name__ == "__main__":
    exitcode = ModInputtcell_events().run(sys.argv)
    sys.exit(exitcode)
