
[block_ip_address]
payload_format = json
param.target_tcell_app_list = default
param.client_ip_field = src_ip
label = Block IP address in tCell
is_custom = 1
param._cam = {"technology": [{"vendor": "tCell", "product": "tCell", "version": ["1"]}], "supports_adhoc": true, "task": ["block"], "category": ["Device Control"], "subject": ["network.firewall"]}
icon_path = alert_block_ip_address.png
description = Block access for this IP address across the tCell instrumented application set

