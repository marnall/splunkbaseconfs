[http_localhost_login_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs
    
[sendmail_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[cloud_app_kill_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[balloon_message_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[sw_discover_ip2mac_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[im_kill_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[browser_redirection_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[p2p_kill_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[http_notification_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[sendmailtohost_ad_mail_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[syslog_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[http_install_agent4_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[adaptive_response_test_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[sw_access_port_acl_priority_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[sw_acl_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[sw_quarantine_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[dot1x_authorize_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[wifi_block_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[wifi_assign_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[vpn_block_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[virtual-fw-rule_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[block_bridging_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[sw_block_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[recheck_host_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs

[agent_shut_down_action]
param._cam = json
    * Prevent splunk startup and btool from erroring
param.index             = <string>
    * The name of index in which to store the modular action logs
