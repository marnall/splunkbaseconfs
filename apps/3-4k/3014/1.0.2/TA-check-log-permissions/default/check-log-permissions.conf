# this is the config file for check-log-permissions.sh - this determines the logs that the script will look for
# input or input wildcard to -be monitored - recursive inputs are not supported but wildcards are

# blank records or records that contain a # sign will be ignored

# just /var/log/messages
#/var/log/messages

# splunk forwarder logs
#/opt/splunkforwarder/var/log/splunk/*.log

# everything under /var/log - enabled by default
/var/log/*
