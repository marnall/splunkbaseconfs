#
# SA RS Charts and Views Transforms
# (C) 2016 rocket-consulting.eu
#

[custom_filter_lookup]
external_type = kvstore
collection = kvstorefiltercoll
fields_list =  _key, id, index, app_context, panel_id, user, visable, editable, deleteable, item, desc, concat, operand, value
# filter = <string>
