[netskope_url]
param._cam = <value>
* CIM Actions / Adaptive Response Requirement

param.action = <value>
* Which Action to perform.

param.column_name = <value>
* Optional. Defaults to "url"

[netskope_file_hash]
param._cam = <value>
* CIM Actions / Adaptive Response Requirement

param.action = <value>
* Which Action to perform

param.column_name = <value>
* Optional. Defaults to "file_hash"