[resolve_guided_resolution]
is_custom = 1
label = Resolve Guided Resolution
description = Given an ITSI Notable, invoke Resolve Systems and apply a guided resolution.
icon_path = resolve.png
payload_format = json

[resolve_worksheets]
is_custom = 1
label = Resolve Worksheets
description = Given an ITSI Notable, invoke Resolve Systems and show the resolution worksheet.
icon_path = resolve.png
payload_format = json

# [resolve_no_html]
# is_custom = 1
# label = Resolve Action No HTML
# description = Given an ITSI Notable, invoke Resolve Systems and show the resolution worksheet.
# icon_path = resolve.png
# payload_format = json

[resolve_incident_resolution]
is_custom = 1
label = Resolve Incident Resolution
description = Given an ITSI Notable, invoke Resolve Systems and resolve an incident.
icon_path = resolve.png
payload_format = json
