[db_access_history://<name>]
global_account = 
endpoint_domain = Enter only fqdn. no http or https
group_id = 24-hexadecimal digit string that identifies your project.
cluster_name = Human readable label that identifies the cluster.
start_date = Enter log collection start date in epoch format. If this is empty then input collects events from 7 last days.