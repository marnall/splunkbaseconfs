import ta_splunkbase_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_splunkbase_apps as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_splunkbase_apps.py
'''
class ModInputsplunkbase_apps(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputsplunkbase_apps, self).__init__("ta_splunkbase", "splunkbase_apps", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputsplunkbase_apps, self).get_scheme()
        scheme.title = ("Splunkbase Apps")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("search_query", title="Search Query",
                                         description="This is the equivalent of the search bar in the Splunkbase interface. If you are looking for apps with keywords, enter them here. To get all apps, leave this blank.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("fields", title="Fields",
                                         description="App information can be added or removed by including fields in the listing request.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("include_archived_apps", title="Archived Apps",
                                         description="Enable this to search for archived apps. If all apps are desired, archived or not, then a second input must be made where this checkbox is set to the opposite setting.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_products", title="Splunk Products",
                                         description="Splunkbase lists apps for Splunk Enterprise and SOAR. The specific product can be filtered for here.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("results_limit", title="Results limit",
                                         description="Limit the number of results. 0 = no limit",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("custom_source", title="Custom Source",
                                         description="A custom source value can be set here. Default source: \"splunkbase_apps\".",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("custom_sourcetype", title="Custom Sourcetype",
                                         description="A custom sourcetype can be set here. Defaults to \"splunkbase:apps\"",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-splunkbase"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("include_archived_apps")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputsplunkbase_apps().run(sys.argv)
    sys.exit(exitcode)
