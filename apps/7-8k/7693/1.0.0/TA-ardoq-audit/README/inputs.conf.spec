[ardoq_audit_logs://<name>]
account = 
since_date = Specify date since when audit events to be collected. Date format should be YYYY-MM-DD. If it's not specified then last 7 days events will be fetched.
source_type = defaults to ardoq:audit:events. If its updated, time extraction must be added under new sourcetype.