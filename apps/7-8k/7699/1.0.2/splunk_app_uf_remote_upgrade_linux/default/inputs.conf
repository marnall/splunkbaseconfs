# DO NOT touch this input!!
# This input is used for UF remote upgrade via Deployment Server:
# Copy UF packages to UF updater, and trigger the upgrade
[script://./bin/SPLUNK_UPDATER_INTERNAL_deliver_pkg.sh]
disabled = 0
index = _internal
sourcetype = splunk_upgrader
# Update the interval to schedule the UF package upgrade, 
# using either an integer (seconds) or a cron expression
interval = 60
