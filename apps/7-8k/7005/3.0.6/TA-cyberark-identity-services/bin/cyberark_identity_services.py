import ta_cyberark_identity_services_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_cyberark_identity_services as input_module
from io import open

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_cyberark_identity_services.py
'''
class ModInputcyberark_identity_services(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputcyberark_identity_services, self).__init__("ta_cyberark_identity_services", "cyberark_identity_services", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputcyberark_identity_services, self).get_scheme()
        scheme.title = ("CyberArk Identity Services")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("tenant_url", title="Tenant URL",
                                         description="Cyberark Tenant URL. e.g. https://aai0581.my.idaptive.app",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("event_types", title="Select Event Types",
                                         description="Select here or type below.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("enter_event_types", title="Enter Event Types ",
                                         description="Comma separated, e.g. Cloud.Core.MfaSummary, Cloud.Core.Login",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("client_id", title="Client Id",
                                         description="SIEM user name.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("client_password", title="Client Password",
                                         description="SIEM user password.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("oauth_app_id", title="OAuth App Id",
                                         description="App Id of OAuth App configured for SIEM.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("scope", title="Scope",
                                         description="Name of scope configured in OAuth App.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("rollback", title="Rollback",
                                         description="In hours. Will start fetching from Now - Rollback.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("batch_size", title="Batch Size",
                                         description="In minutes. Time window size for querying events internally. Reduce this, in case of high load of events.",
                                         required_on_create=True,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-cyberark-identity-services"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputcyberark_identity_services().run(sys.argv)
    sys.exit(exitcode)
