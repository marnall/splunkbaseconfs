import ta_jamf_pro_devices_and_analytics_by_loonsecio_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_jamf_mac_devices as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_jamf_mac_devices.py
'''
class ModInputjamf_mac_devices(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputjamf_mac_devices, self).__init__("ta_jamf_pro_devices_and_analytics_by_loonsecio", "jamf_mac_devices", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputjamf_mac_devices, self).get_scheme()
        scheme.title = ("Jamf Mac devices")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("jamf_pro_url", title="Jamf Pro URL",
                                         description="<yourServer>.jamfcloud.com",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("client_id", title="Client Id / Username",
                                         description="Client id for the API or Username",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("client_secret", title="Client Secret / Password",
                                         description="Client Secret or the API Password",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("device_management_status", title="Device Management Status",
                                         description="",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("limit_inventory_time", title="Limit Inventory Time",
                                         description="Time limit for devices that haven\'t inventories. 0 = Unlimited, 1 = 1 day ago, 14 = 2 weeks",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("api_sections", title="Sections",
                                         description="",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("meta_builder", title="Meta Builder",
                                         description="Leave blank for the default value",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("application_patching", title="Application Patching",
                                         description="Select Patching Guidance Feeds, Empty list means don\'t include the data. If empty no patch guidance given",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("share_analytics", title="Share Analytics",
                                         description="Shares the Analytics information, refer to documentation",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("vulnerability_detections", title="Vulnerability Detections",
                                         description="Add vulnerability information for the devices",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("vulnerability_requirements", title="Vulnerability Requirements",
                                         description="Refer to https://loonsecio.com/resourcesVulnerabilities must match this criteriaEmpty list requires all",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-jamf-pro-devices-and-analytics-by-loonsecio"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("share_analytics")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputjamf_mac_devices().run(sys.argv)
    sys.exit(exitcode)
