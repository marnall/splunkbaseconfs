[search_field_map]
collection = search_field_map
external_type = kvstore
fields_list = title, data_model, field_validation_dedup, app, disabled, is_scheduled, in_scope_for_dsn, mitre_attack

[data_field_map]
collection = data_field_map
external_type = kvstore
fields_list = unique_key, matched_fields, data_model, sourcetype, source

[tag_field_map]
collection = tag_field_map
external_type = kvstore
fields_list = data_model, datamodel_field, tag
