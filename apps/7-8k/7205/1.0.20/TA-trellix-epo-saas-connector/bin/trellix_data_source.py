import ta_trellix_epo_saas_connector_declare

import os
import sys

import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi
import splunklib.client as client

sys.path.insert(
    0,
    os.path.sep.join(
        [os.path.dirname(os.path.realpath(os.path.dirname(__file__))), "lib"]
    ),
)


import input_module_trellix_data_source as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_trellix_data_source.py
'''
class ModInputtrellix_data_source(modinput_wrapper.base_modinput.BaseModInput):

    nobody_client = None
    admin_client = None
    collection_name = None
    USERNAME = None
    ew = None
    max_past = None
    input_kv_key = None
    MASK = '<encrypted>'

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputtrellix_data_source, self).__init__("ta_trellix_epo_saas_connector", "trellix_data_source", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputtrellix_data_source, self).get_scheme()
        scheme.title = ("Trellix Data Source")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True
        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("audit_source", title="Audit Source",
                                         description="Audit Source to identify the customer",
                                         required_on_create=True,
                                         required_on_edit=True))
        scheme.add_argument(smi.Argument("iam_url", title="IAM URL",
                                         description="IAM URL to get Token",
                                         required_on_create=True,
                                         required_on_edit=True))
        scheme.add_argument(smi.Argument("api_gateway_url", title="API Gateway URL",
                                         description="API Gateway URL to fetch Trellix data",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("client_id", title="Client ID",
                                         description="Client ID to access Trellix APIs",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("client_secret", title="Client Secret",
                                         description="Client Secret to access Trellix APIs",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("api_key", title="API Key",
                                         description="API key to access Trellix APIs",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("trellix_epo_events", title="Trellix ePO Events",
                                         description="Check this option to ingest Trellix ePO Events.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("trellix_audit_events", title="Trellix Audit Events",
                                         description="Check this option to ingest Trellix Audit Events.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("trellix_insights_events", title="Trellix Insights Events",
                                         description="Check this option to ingest Trellix Insights Events.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("trellix_dlp_incidents", title="Trellix DLP Incidents",
                                         description="Check this option to ingest Trellix DLP Incidents.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("trellix_edr_events", title="Trellix EDR Events",
                                         description="Check this option to ingest Trellix EDR Events.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-trellix-epo-saas-connector"

    def validate_input(self, definition):
        """validate the input stanza"""
        #stanza_name = self.get_arg('name')
        #self.input_name = "trellix_data_source://"+stanza_name

        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        #for stanza_name in self.get_input_stanza_names():
        #stanza_name = self.get_input_stanza("name")

        stanza_name = self.get_arg('name')
        self.input_name = "trellix_data_source://"+stanza_name


        #uri =self.context_meta["server_uri"]
        #session_key = self.context_meta['session_key']
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("trellix_epo_events")
        checkbox_fields.append("trellix_audit_events")
        checkbox_fields.append("trellix_insights_events")
        checkbox_fields.append("trellix_dlp_incidents")
        checkbox_fields.append("trellix_edr_events")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

  
      

if __name__ == "__main__":
    exitcode = ModInputtrellix_data_source().run(sys.argv)
    sys.exit(exitcode)
