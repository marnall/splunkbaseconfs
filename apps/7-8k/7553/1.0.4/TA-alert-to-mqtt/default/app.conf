# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = false
state = enabled
build = 5

[launcher]
author = Gary Croker
version = 1.0.4
description = Send splunk alert results to MQTT Broker.

[ui]
is_visible = 1
label = Alert to MQTT Broker
docs_section_override = AddOns:released

[package]
id = TA-alert-to-mqtt

[triggers]
reload.addon_builder = simple
reload.ta_alert_to_mqtt_account = simple
reload.ta_alert_to_mqtt_settings = simple
reload.passwords = simple