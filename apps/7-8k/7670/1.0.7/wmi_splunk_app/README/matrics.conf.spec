# matrics.conf.spec
# Spec file for matrics.conf

[metrics_settings]
metrics_url = <string>
  * URL of the metrics endpoint to collect data from.
metrics_keywords = <comma-delimited list>
  * Comma-separated list of metric keywords to collect (e.g., go_goroutines,go_memstats_alloc_bytes).
