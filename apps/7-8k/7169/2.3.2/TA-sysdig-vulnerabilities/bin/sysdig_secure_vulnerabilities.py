import ta_sysdig_vulnerabilities_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_sysdig_secure_vulnerabilities as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_sysdig_secure_vulnerabilities.py
'''
class ModInputsysdig_secure_vulnerabilities(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputsysdig_secure_vulnerabilities, self).__init__("ta_sysdig_vulnerabilities", "sysdig_secure_vulnerabilities", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputsysdig_secure_vulnerabilities, self).get_scheme()
        scheme.title = ("Sysdig Secure Vulnerabilities")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("sysdig_secure_url", title="Sysdig Secure URL",
                                         description="Enter your Sysdig Secure URL for your Sysdig region (https must be included)Examples: https://secure.sysdig.com, https://us2.app.sysdig.com, https://eu1.app.sysdig.com, ...",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("sysdig_secure_token", title="Sysdig Secure token",
                                         description="Enter your Sysdig Secure API Token",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("nvd_api_key", title="NVD API Key",
                                         description="Provide a NVD API Key (see https://nvd.nist.gov/developers/request-an-api-key) to retrieve vulnerability descriptions.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("vulnerability_details", title="Vulnerability details",
                                         description="Choose the vulnerability details included as part of the event. If Vulnerability Description is selected, it is strongly encouraged to provide a NVD API Key to avoid hitting the rate limit.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("max_images", title="Max. number of images",
                                         description="Maximum number of images to retrieve, for testing purposes",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-sysdig-vulnerabilities"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputsysdig_secure_vulnerabilities().run(sys.argv)
    sys.exit(exitcode)
