# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 5

[launcher]
author = Scott Horton
version = 1.0.0
description = Collect and parse VMWare Horizon 8 logs from RDSH hosts and Connection Servers

[ui]
is_visible = 0
label = VMware Horizon Add-on
docs_section_override = AddOns:released

[package]
id = TA-vmware-horizon

[triggers]
reload.addon_builder = simple
reload.ta_vmware_horizon_account = simple
reload.ta_vmware_horizon_settings = simple
reload.passwords = simple

