[cyera_index]
definition = index=*
iseval = 0
description = Base index filter for Cyera dashboards. Override this macro to target a specific index (e.g. index=cyera).

[cyera_index(1)]
args = index_name
definition = index=$index_name$
iseval = 0
description = Parameterized index filter for Cyera dashboards.
