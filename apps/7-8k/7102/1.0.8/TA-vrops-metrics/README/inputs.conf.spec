[vrops_metrics://<name>]
global_account = vROPS Endpoint Account
vrops_endpoint = Endpoint of the vROPS Server, without http:// or https://
authsrc = Authentication Source for Account (Optional)
use_ssl = Enable this if you want to verify the certificate when making API requests

[vrops_health_logs://<name>]
global_account = vROPS Endpoint Account
vrops_endpoint = Endpoint of the vROPS Server, without http:// or https://
authsrc = Authentication Source for Account (Optional)
use_ssl = Enable this if you want to verify the certificate when making API requests