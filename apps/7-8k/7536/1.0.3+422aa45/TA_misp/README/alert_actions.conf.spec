[add_sighting]
param._cam = <json> Adaptive Response parameters.
param.misp_instance = <list> MISP Instance.
param.ioc = <string> IOC. It's a required parameter.
param.sighting_type = <list> Sighting Type.
