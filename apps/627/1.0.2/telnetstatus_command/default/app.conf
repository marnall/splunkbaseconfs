#
# Splunk app configuration file
#

[ui]
is_visible = 0
label = Telnet Status


[launcher]
author = Nimish Doshi
version=1.0.2
description = telnetstatus command takes a telnet_address field and tries to connect to it using telnet to see if a login prompt exists. It returns a telnetstatus field with None, LoginFound or LoginNotFound status per supplied address.

[package]
id=telnetstatus_command


