# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 8

[launcher]
author = Splunk Works
version = 1.2.0
description = Collects metadata and inventory data for correlation.

[ui]
is_visible = 1
label = Microsoft Azure Metadata Inventory Add-on for Splunk
docs_section_override = AddOns:released

[package]
id = TA-Azure-inventory

[triggers]
reload.addon_builder = simple
reload.ta_azure_inventory_account = simple
reload.ta_azure_inventory_settings = simple
reload.passwords = simple

