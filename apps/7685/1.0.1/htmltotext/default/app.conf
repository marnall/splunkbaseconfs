[install]
state = enabled

[ui]
is_visible = 0
label = htmltotext

[launcher]
version = 1.0.1
description = Custom search command to take a field as input, convert the input to plain-text and output the plain-text as a new field

[package]
check_for_updates = 1
id = htmltotext

[id]
name = htmltotext
version = 1.0.1
