#monitor the /var/syslog directory and use the 3rd part of the folder path to identity the host that is sending logs
#ignore zipped files in this location
[monitor:///var/syslog]
host_segment = 3
blacklist=(\.gz$|\.zip$|\.bz2$)

#default index for log types that do not match any rules in transforms.conf
index = syslog



