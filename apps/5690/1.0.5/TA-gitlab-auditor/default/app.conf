# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 5

[launcher]
author = Leonardo Armesto
version = 1.0.5
description = GitLab Auditor Add-On is a tool for GitLab owners and administrators to track important events such as who performed certain actions and the time they happened.

[ui]
is_visible = 1
label = GitLab Auditor
docs_section_override = AddOns:released

[package]
id = TA-gitlab-auditor

[triggers]
reload.addon_builder = simple
reload.ta_gitlab_auditor_account = simple
reload.ta_gitlab_auditor_settings = simple
reload.passwords = simple

