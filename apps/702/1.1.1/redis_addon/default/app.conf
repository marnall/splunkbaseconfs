# Splunk app configuration file

[ui]
is_visible = false
label = redis_addon

[launcher]
author=Nimish Doshi
description=This add-on provides a generic way to populate a redis database and use a generic python script to perform a lookup of your own tag values.
version=1.1.1

[package]
id=redis_addon
