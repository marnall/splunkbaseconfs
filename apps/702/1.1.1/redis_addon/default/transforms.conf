# Set a redis lookup for key value fields. Example:

#[redislookup]
#external_cmd = redislookup.py status status_desc
#external_type = python
#fields_list =  status, status_desc

# Use your own field names for the key and value, which are status and
# status_desc in this example.


# Uses hget instead of get

#[redislookup_hget]
#external_cmd = redislookup_hget.py status status_description
#external_type = python
#fields_list =  status, status_description

# Use your own field names for the key and value, which are status and
# status_description in this example.
