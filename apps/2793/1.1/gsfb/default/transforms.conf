[canonicalize_lookup]
external_cmd = canonicalize_lookup.py url
fields_list  = url, url_canonicalized

[suffix_expression_lookup]
external_cmd = suffix_expression_lookup.py url
fields_list  = url, url_prefixes

[full_length_hashes_lookup]
external_cmd = full_length_hashes_lookup.py hash prefix_length
fields_list  = hash, prefix_length, confirmed_lists

[safe_browsing_lookup_api_lookup]
external_cmd = safe_browsing_lookup_api_lookup.py url
fields_list  = url, verdict

[kv_prefixes_lookup]
external_type = kvstore
collection = prefixes
fields_list = prefix, prefix_length, chunk_key

[kv_chunks_lookup]
external_type = kvstore
collection = chunks
fields_list = _key, chunk_number, chunk_type, list_name

