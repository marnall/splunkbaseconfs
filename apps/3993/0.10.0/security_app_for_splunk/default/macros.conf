[search_dm(1)]
args = DM
definition = tstats prestats=false summariesonly=true allow_old_summaries=true count from datamodel=$DM$ | eval data_model=replace(mvindex(split(mvindex(split("$DM$","."),0)," "),0),"_"," ")