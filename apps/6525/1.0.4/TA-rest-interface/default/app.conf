#
# Splunk app configuration file
#

[install]
is_configured = 0

[ui]
is_visible = 0
label = Trifork.Security REST Interface

[launcher]
author = Trifork.Security
version = 1.0.4
description = Creates a new REST interface for ingesting data into Splunk, and returning a unique ID back to requester.

[package]
id = TA-rest-interface

[triggers]
reload.ta_rest_interface_settings = simple

