######
### Perform KPI Analysis
######
[kpi-command]
syntax = kpi (?dynamic)
shortdesc = Uses the automatic lookup for KPI and calculates compliance
description = The KPI command uses the lookups from the KV store to check KPI compliance. "dynamic" is optional, and defaults to false. Dynamic, if set as true, uses a calculation to determine compliance over the range of events.
comment1 = Per Event KPI
example1 = kpi
category = utility
maintainer = alacercogitatus
usage = public
appears-in=6.2
tags = utility kpi
