# Contains the list of embedding services and models supported

[<name>]

name.display = <string>
# Required
# Name of the embedding service

provider = <OPENAI|VOYAGE_AI>
# Required
# Provider of the embedding service

models = <string>
# Required
# Models supported by the embedding service
# Add multiples models using comma as separator
