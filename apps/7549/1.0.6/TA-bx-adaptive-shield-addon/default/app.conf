# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 4

[launcher]
author = Ben Tawiah
version = 1.0.6
description = The add-on offers multiple input types to fetch data from different API endpoints:1. Accounts2. Security Checks3. Integrations4. Alerts

[ui]
is_visible = 1
label = BX Adaptive Shield addon
docs_section_override = AddOns:released

[package]
id = TA-bx-adaptive-shield-addon

[triggers]
reload.addon_builder = simple
reload.ta_bx_adaptive_shield_addon_account = simple
reload.ta_bx_adaptive_shield_addon_settings = simple
reload.passwords = simple

