#
# Splunk app configuration file
#

[install]
state = enabled
is_configured = 0

[ui]
is_visible = 1
label = ORDR Insight

[launcher]
author = Ordr
description = Dashboard for ORDR data
version = 1.0.5

[id]
name = ordr_insight
version = 1.0.5

[package]
id = ordr_insight

[triggers]
reload.meta = never
reload.addon_builder = never

