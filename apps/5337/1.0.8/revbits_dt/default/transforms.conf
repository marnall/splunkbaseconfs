

[force_sourcetype_for_revbits_dt]
DEST_KEY = MetaData:Sourcetype
#REGEX = ^[^\|\n]*\|(?P<domain>.*)\|(?P<entity_type>.*)\|(?P<entity_name>.*)\|(?P<event>.*)\|By\|(?P<user>.*)\|
REGEX = ^[^\|\n]*\|Revbits DT[^\|\n]*\|[^\|\n]*\|[^\|\n]*\|[^\|\n]*\|[^\|\n]*\|[^\|\n]*\|By[^\|\n]*
FORMAT = sourcetype::revbits:dt
