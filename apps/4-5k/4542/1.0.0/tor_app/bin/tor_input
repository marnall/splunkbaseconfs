#!/bin/bash
URL='https://www.dan.me.uk/torlist/'
OUTPUT='/opt/splunk/etc/apps/tor_app/lookups/tor_exit_nodes.csv'
echo "src_ip,desc" >$OUTPUT
wget -O - "$URL" 2>/dev/null | while read L
do
   echo $L,toripaddress >>$OUTPUT
done
