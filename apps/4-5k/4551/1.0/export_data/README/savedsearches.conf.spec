# Export

action.export_data = [0|1]
* Enable action

action.export_data.param.directory = <string>
* The output directory
* (required)

action.export_data.param.filename_prefix = <string>
* The output filename prefix
* (required)