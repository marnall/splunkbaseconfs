import ta_rl_sqs_poller_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from solnlib.packages.splunklib import modularinput as smi



import input_module_rl_sqs_jsontest_1499677822_221 as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_rl_sqs_json.py
'''
class ModInputrl_sqs_json(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputrl_sqs_json, self).__init__("ta_rl_sqs_poller", "rl_sqs_json", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputrl_sqs_json, self).get_scheme()
        scheme.title = ("RL-SQS-JSON")
        scheme.description = ("This data input is the main one here. It pulls the SQS message and transforms it into a json model and writes the event with source type as rlsqs:jsonThis input collection runs on a 60 second interval")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("aws_access_key_id", title="AWS Access Key ID",
                                         description="Access key ID of the key generated to programmatically access IAM user\'s permissions.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("aws_secret_access_key", title="AWS Secret Access Key",
                                         description="Secret Key of the access key generated to programmatically access IAM user\'s permissions.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("sqs_queue_name", title="SQS Queue Name",
                                         description="Name of the queue accessible by the IAM user.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("sqs_queue_aws_region", title="SQS Queue AWS region",
                                         description="AWS Region identifier for the queue. NOTE: We will be searching ONLY this region for the provided queue name.",
                                         required_on_create=True,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-RL-SQS-Poller"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputrl_sqs_json().run(sys.argv)
    sys.exit(exitcode)
