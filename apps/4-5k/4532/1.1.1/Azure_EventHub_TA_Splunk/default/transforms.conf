#remove anything before the the \{ charactor. This transforms may be used if the data is onboarded with a timestamp or information prepended to a json file structure 
[remove_header_json]
FORMAT = $1
REGEX = ^(?:[^\{]+)(.+$)
DEST_KEY = _raw
LOOKAHEAD = 50000