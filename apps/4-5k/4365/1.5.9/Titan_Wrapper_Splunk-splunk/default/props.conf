#Example conf for automatic lookup and matching indicators with data.

#[default]
#LOOKUP-titan_ipv4_autolookup = titan_ipv4_lookup indicator AS dest OUTPUTNEW indicator AS indicator provider AS provider
#LOOKUP-titan_url_autolookup = titan_url_lookup indicator AS dest  OUTPUTNEW indicator AS indicator provider AS provider
