import ta_azure_devops_add_on_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from solnlib.packages.splunklib import modularinput as smi



import input_module_azure_devops_main as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_azure_devops_main.py
'''
class ModInputazure_devops_main(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputazure_devops_main, self).__init__("ta_azure_devops_add_on", "azure_devops_main", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputazure_devops_main, self).get_scheme()
        scheme.title = ("Azure DevOps Scrum")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("global_account", title="Global Account",
                                         description="Account details need to be entered in the add-on\'s configuration settings in order for them to be selectable here",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("organization", title="Organization",
                                         description="The name of the Azure DevOps organization. N.B This MUST be populated if details of a custom endpoint have not been provided in the setup params",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("project", title="Project",
                                         description="The name or ID (GUID) of the team project containing the team.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("team", title="Team",
                                         description="The name or ID (GUID) of the team.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("work_item_types", title="Work Item Types",
                                         description="Types of Work Items to Retrieve (Optional)",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("get_previous_work_items", title="Get Previous Work Items",
                                         description="Get Completed Work Items Associated With Previous Iterations. NOTE: This will increase licence use and should only be enabled with care!",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("get_team_info", title="Get Team Info",
                                         description="Iteration and Capacity Info (Optional)",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-azure-devops-add-on"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        account_fields.append("global_account")
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("get_previous_work_items")
        checkbox_fields.append("get_team_info")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputazure_devops_main().run(sys.argv)
    sys.exit(exitcode)
