from constants import IP_RANGE, LAST_REFRESH_IP_RANGES, LAST_REFRESH_CERTIFICATES, \
    LAST_REFRESH_DOMAINS, DOMAINS, CERTIFICATES, SERVICES, LAST_REFRESH_SERVICES, IP_RANGE_DATA_COLLECTION_NAME, \
    DOMAINS_COLLECTION_NAME, CERTIFICATE_COLLECTION_NAME, SERVICES_COLLECTION_NAME, UNASSOCIATED_IP_RANGE_NAME, \
    LAST_REFRESH_RESPONSIVE_IPS, RESPONSIVE_IP, LAST_REFRESH_CLOUD_RESOURCES, CLOUD_ASSETS_COLLECTION_NAME, CLOUD_ASSETS
from state_refresh import StateRefresh


def get_mapping_constants(input_name):
    DATA_TYPE_TO_STATE = {
        IP_RANGE: StateRefresh(1, f"{input_name}_{LAST_REFRESH_IP_RANGES}", IP_RANGE_DATA_COLLECTION_NAME),
        CERTIFICATES: StateRefresh(1, f"{input_name}_{LAST_REFRESH_CERTIFICATES}", CERTIFICATE_COLLECTION_NAME),
        DOMAINS: StateRefresh(1, f"{input_name}_{LAST_REFRESH_DOMAINS}", DOMAINS_COLLECTION_NAME),
        SERVICES: StateRefresh(1, f"{input_name}_{LAST_REFRESH_SERVICES}", SERVICES_COLLECTION_NAME),
        RESPONSIVE_IP: StateRefresh(1, f"{input_name}_{LAST_REFRESH_RESPONSIVE_IPS}", UNASSOCIATED_IP_RANGE_NAME),
        CLOUD_ASSETS: StateRefresh(1, f"{input_name}_{LAST_REFRESH_CLOUD_RESOURCES}", CLOUD_ASSETS_COLLECTION_NAME)
    }
    return DATA_TYPE_TO_STATE
