import ta_expanse_declare
import json
import os
import sys
from io import open

import modinput_wrapper.base_modinput
from solnlib.packages.splunklib import modularinput as smi

import input_module_expanse as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_expanse.py
'''


class ModInputexpanse(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputexpanse, self).__init__("ta_expanse", "expanse", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputexpanse, self).get_scheme()
        scheme.title = ("Xpanse")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("token", title="Token",
                                         description="Token to authenticate API calls",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("server_url", title="Server URL",
                                         description="Base URL of the API server",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("use_advanced_auth", title="Use Advanced Auth",
                                         description="Check this box if you are using advanced authentication",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("api_key_id", title="API Key id",
                                         description="The id of your api key",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("utc_offset", title="System time offset from UTC",
                                         description="UTC offset (in hours) of your system after considering any daylight changes. "
                                                     "Default is 0.0, Min offset is -12.0, Max offset is 14.0. "
                                                     "Ex: A Splunk server with timezone UTC-2:30 would use -2.5.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("global_account", title="Global Account",
                                         description="",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(
            smi.Argument("enable_alert_updates", title="If you are running Splunk in a distributed search "
                                                       "environment then this should be enabled on the Heavy "
                                                       "Forwarder only. Please reference documentation for "
                                                       "more details.",
                         description="",
                         required_on_create=True,
                         required_on_edit=False))
        scheme.add_argument(
            smi.Argument("alert_severity", title="Select the lowest severity of the alerts you wish to include",
                         description="",
                         required_on_create=True,
                         required_on_edit=False))
        scheme.add_argument(smi.Argument("enable_assets", title="If you are running Splunk in a distributed search "
                                                                "environment then this should be enabled on the Search "
                                                                "Head only. Please reference documentation for more "
                                                                "details.",
                                         description="",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("enable_services", title="If you are running Splunk in a distributed search "
                                                                  "environment then this should be enabled on the Search "
                                                                  "Head only. Please reference documentation for more "
                                                                  "details.",
                                         description="",
                                         required_on_create=True,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-expanse"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        account_fields.append("global_account")
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields


if __name__ == "__main__":
    exitcode = ModInputexpanse().run(sys.argv)
    sys.exit(exitcode)
