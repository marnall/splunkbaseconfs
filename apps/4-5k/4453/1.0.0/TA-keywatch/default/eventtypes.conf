# eventtypes.conf

[NoPass]
  search = sourcetype="key:priv" NOT ENCRYPTED	
  # tags = attack filesystem hids ids

[NoPassRoot]
  search = sourcetype="key:priv" src_user=root NOT ENCRYPTED	
  # tags = attack filesystem hids ids