# tags.conf 

[eventtype=NoPass]
  attack = enabled
  filesystem = enabled
  hids = enabled
  ids = enabled 
  endpoint = enabled

[eventtype=NoPassRoot]
  attack = enabled
  filesystem = enabled
  hids = enabled
  ids = enabled 
  endpoint = enabled
  privledge = enabled
  