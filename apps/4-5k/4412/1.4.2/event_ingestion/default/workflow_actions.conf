[splunk_primary_workflow]
type = search
search.preserve_timerange = 0
search.app = search
search.view = search
search.target = blank
fields = *
display_location = event_menu
search.search_string = | snsecingest "primary" "$@sid$" "$@offset$" "$_sourcetype$"
label = Ingest ServiceNow Security Event (Production)

[splunk_secondary_workflow]
type = search
search.preserve_timerange = 0
search.app = search
search.view = search
search.target = blank
fields = *
display_location = event_menu
search.search_string = | snsecingest "secondary" "$@sid$" "$@offset$" "$_sourcetype$"
label = Ingest ServiceNow Security Event (Staging)
