"""Modular input entry point for Cyware Threat Intelligence Exchange."""
import ta_cyware_ctix_declare   # noqa: F401

import os
import sys
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi


import input_module_cyware_threat_intelligence_exchange as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_cyware_threat_intelligence_exchange.py
'''


class mod_input_cyware_threat_intelligence_exchange(modinput_wrapper.base_modinput.BaseModInput):
    """Modular Input class for Cyware Threat Intelligence Exchange."""

    def __init__(self):
        """Initialize the mod_input_cyware_threat_intelligence_exchange class."""
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(mod_input_cyware_threat_intelligence_exchange, self).__init__(
            "ta_cyware_ctix", "cyware_threat_intelligence_exchange", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """Overloaded splunklib modularinput method."""
        scheme = super(mod_input_cyware_threat_intelligence_exchange, self).get_scheme()
        scheme.title = ("Cyware Threat Intelligence Exchange")
        scheme.description = (
            "Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("saved_result_set_tag", title="Saved Result Set Tag",
                                         description="Tag added to \"Saved Result Set\" Rule Action in Cyware Threat Intel. Leave the field blank to fetch all data",  # noqa: E501
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("account", title="Account",
                                         description="Select the account from where data will be fetched.",
                                         required_on_create=True,
                                         required_on_edit=True))
        scheme.add_argument(smi.Argument("ingest_to_index", title="Ingest to Index",
                                         description="Select the checkbox to enable the data collection in index.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("lookback_days", title="Lookback Duration (Days)",
                                         description="Specify the number of past days to collect indicators from Cyware. This setting is only used during the initial data fetch (default: 30 days).",  # noqa: E501
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("enrich_data", title="Fetch Enriched Data",
                                         description="Select the checkbox to enable the fetching of enriched data.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        """Return the name of the app."""
        return "TA-cyware-ctix"

    def validate_input(self, definition):
        """Validate the input stanza."""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """Write out the events."""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        """Return a list of fields related to account configuration for the input."""
        account_fields = []
        account_fields.append("account")
        return account_fields

    def get_checkbox_fields(self):
        """Return a list of checkbox fields for the input, if available."""
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        """Return a list of global checkbox fields for the input, if available."""
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields


if __name__ == "__main__":
    exitcode = mod_input_cyware_threat_intelligence_exchange().run(sys.argv)
    sys.exit(exitcode)
