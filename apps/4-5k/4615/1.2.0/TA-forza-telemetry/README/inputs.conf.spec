[forza_udp://<name>]
ip = Leave as 0.0.0.0 to accept all source IPs.
port = Enter the same port as you configured in Forza Data Out.
enabled_metrics = Select the values you want indexed, all other telemetry will be ignored.
rate_limit = Minimum time in milliseconds between updates that get indexed.