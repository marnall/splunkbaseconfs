import ta_gigamon_insight_add_on_for_splunk_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_insight_detections as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_insight_detections.py
'''
class ModInputinsight_detections(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputinsight_detections, self).__init__("ta_gigamon_insight_add_on_for_splunk", "insight_detections", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputinsight_detections, self).get_scheme()
        scheme.title = ("Insight Detections")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("start_date", title="Start Date  (Optional)",
                                         description="The first date to pull Insight detections from. If no date is entered, a default value of \"2021-11-01T00:00:00.000Z\" will be used.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("account_uuid", title="Account UUID (Optional)",
                                         description="Filter results to show only detections for the specified account UUID. If none is entered, detections will be pulled for all accounts you have access to.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("status", title="Status",
                                         description="Choose to pull detections that are active, resolved, or all (detections of any status).",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("severity_levels", title="Severity (Optional)",
                                         description="Choose to pull detections for rules of a particular severity level. Default is to pull detections for all severities (high, moderate, and low).",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("confidence_levels", title="Confidence (Optional)",
                                         description="Choose to pull detections for rules of a particular confidence level. Default is to pull detections for all confidences (high, moderate, and low).",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("pull_muted_rules", title="Muted Rules ",
                                         description="Choose to pull detections for rules that are muted, unmuted, or all (both muted and unmuted rules). Default is to pull only detections for unmuted rules.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("pull_muted_devices", title="Muted Devices",
                                         description="Choose to pull detections for devices that are muted for the account, unmuted devices, or all (both muted and unmuted devices for the account). Default is to pull only detections for unmuted devices.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("pull_muted_detections", title="Muted Detections",
                                         description="Choose to pull detections that are muted, unmuted, or all (both muted and unmuted detections). Default is to pull only unmuted detections.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("include_description", title="Include Description",
                                         description="Enable this option if you want to include the detection description with the results. Default is to include the description when pulling detections.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("include_signature", title="Include Signature",
                                         description="Enable this option if you want to include the detection IQL signature with the results. Default is to include the signature when pulling detections.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("include_pdns", title="Include PDNS",
                                         description="Enable this option if you want to include passive DNS information for the impacted asset for each detection.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("include_dhcp", title="Include DHCP",
                                         description="Enable this option if you want to include DHCP information for the impacted asset for each detection.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("include_events", title="Include Events",
                                         description="Enable this option if you want to import the events associated with each detection. NOTE: This may pull a large amount of events into Splunk (up to 1000).",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("filter_training_detections", title="Filter Training Detections",
                                         description="Filter out detections from the training environment. This is checked off by default and recommended.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-gigamon-insight-add-on-for-splunk"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("include_description")
        checkbox_fields.append("include_signature")
        checkbox_fields.append("include_pdns")
        checkbox_fields.append("include_dhcp")
        checkbox_fields.append("include_events")
        checkbox_fields.append("filter_training_detections")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputinsight_detections().run(sys.argv)
    sys.exit(exitcode)
