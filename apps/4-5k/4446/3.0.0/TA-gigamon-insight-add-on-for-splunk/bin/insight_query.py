import ta_gigamon_insight_add_on_for_splunk_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_insight_query as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_insight_query.py
'''
class ModInputinsight_query(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputinsight_query, self).__init__("ta_gigamon_insight_add_on_for_splunk", "insight_query", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputinsight_query, self).get_scheme()
        scheme.title = ("Insight Query")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("aws_access_key", title="AWS Access Key",
                                         description="The AWS Access Key to connect to the S3 buckets",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("aws_secret_key", title="AWS Secret Key",
                                         description="The AWS Secret Key to connect to the S3 Buckets",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("account_code", title="Account Code",
                                         description="Account code of the account we want to query.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("event_types", title="Event Types",
                                         description="List of Event Types to be retrieved.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("days_to_collect", title="Days to Collect (Optional)",
                                         description="The amount of days we must go back to retrieve the events (maximum 7 days). If no value is entered, or if the entered value is greater than 7, only events from the last 7 days will be retrieved.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("fetch_pdns", title="Fetch PDNS",
                                         description="Enable this to fetch passive DNS entity enrichment information for IPs in the query results. Will fetch this information for both internal and external IPs.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("fetch_dhcp", title="Fetch DHCP",
                                         description="Enable this to fetch DHCP lease entity enrichment information for IPs in the query results. Will only fetch this information for internal IPs.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-gigamon-insight-add-on-for-splunk"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("fetch_pdns")
        checkbox_fields.append("fetch_dhcp")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputinsight_query().run(sys.argv)
    sys.exit(exitcode)
