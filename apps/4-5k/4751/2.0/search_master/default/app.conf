[install]
build = 2

[ui]
is_visible = true
label = Search Master

[launcher]
version = 2.0
description = This app contains searches and macros for term and avalue searches, including time windows.  It also contains an EventCode map and a cross join macro plus two sample csv files A.csv and B.csv to demonstrate its capabilities.  The null_term macro was added to deal with cases with null result sets for the filter.
author = Andrew Landen

[package]
id = search_master