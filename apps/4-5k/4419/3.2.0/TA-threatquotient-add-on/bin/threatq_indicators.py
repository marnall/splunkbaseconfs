import ta_threatquotient_add_on_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_threatq_indicators as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_threatq_indicators.py
'''
class ModInputthreatq_indicators(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputthreatq_indicators, self).__init__("ta_threatquotient_add_on", "threatq_indicators", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputthreatq_indicators, self).get_scheme()
        scheme.title = ("ThreatQ indicators")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("checkbox_for_index", title="Enable Index",
                                         description="Select the checkbox to enable the data collection in index.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("export_id", title="Export ID",
                                         description="The ThreatQ export ID to use for data collection. Default: splunk",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("export_token", title="Export Token",
                                         description="The ThreatQ export token to use for data collection.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("export_hash", title="Export Hash",
                                         description="The ThreatQ export hash to use for data collection. Default: 1",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("threshold_score", title="Threshold Indicator Score",
                                         description="Minimum threshold score for collecting indicators. Indicators with score greater than equal to provided score will only be collected. Default: 8",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("indicator_status", title="Indicator Status",
                                         description="Indicator status for collecting indicators. Indicators with provided status will only be collected. Values must be separated by a single comma. Default: Active",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("pull_all_iocs", title="Pull All Indicators",
                                         description="Enabling this checkbox will force pull all data on input edit. On input creation it is mandatory to enable this checkbox before saving. Enable this when changing status or score value for any input.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("response_page_size", title="Response Page Size",
                                         description="Defines the number of indicators to retrieve per API request. The maximum allowed value is 10,000.",
                                         required_on_create=True,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-threatquotient-add-on"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("checkbox_for_index")
        checkbox_fields.append("pull_all_iocs")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputthreatq_indicators().run(sys.argv)
    sys.exit(exitcode)
