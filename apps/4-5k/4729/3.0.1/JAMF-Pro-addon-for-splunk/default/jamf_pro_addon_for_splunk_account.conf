# Account stanzas are created at runtime via the configuration UI.
# The [default] stanza below registers the conf type with Splunk so the
# REST endpoint returns an empty list (rather than 404) before any
# accounts have been configured. Splunk treats [default] as reserved —
# it does not appear as a real account in the UI.
[default]
