[automox_devices://<name>]
pending_patches = 
exclude_patched_devices = Check to only pull devices that are missing available patches.
exclude_exceptions = Check to only retrieve devices that have not been marked with exception status.

[automox_software://<name>]
exclude_utd =

[automox_events://<name>]
index =
