[cbthreathunter://default]
*This is how the Cb ThreatHunter For Splunk is configured

hostname = <value>
*This is the hostname for the Modular Input

connector_id = <value>
* This is the connector id configured within CarbonBlack.

verify_ssl = <value>
* Defaults to True

proxy_name = <value>
* Set to "not_configured" if not configured.