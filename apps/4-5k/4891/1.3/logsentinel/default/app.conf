[install]
is_configured = 0
build = 6

[package]
id = logsentinel
check_for_updates = 1

[ui]
is_visible = 1
label = LogSentinel

[launcher]
author = vminkov
description = This app can be configured to extract all the fields (actorId, display name, action, entity, etc.) from Splunk fields and send data to LogSentinel. It can be configured to not send all entries but just some, qualifying for a given regex.
version = 1.3

