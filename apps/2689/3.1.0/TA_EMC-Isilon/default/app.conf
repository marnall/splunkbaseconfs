# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = false
state = enabled
build = 3

[launcher]
author = Dell
description = Dell PowerScale Add-on for Splunk collects data from Dell PowerScale APIs and indexes it into the Splunk.
version = 3.1.0

[ui]
is_visible = 1
label = Dell PowerScale Add-on for Splunk
docs_section_override = AddOns:released

[package]
id = TA_EMC-Isilon

[triggers]
reload.addon_builder = simple
reload.ta_emc_isilon_account = simple
reload.ta_emc_isilon_settings = simple
reload.eventgen = simple