[expose:cvefeed_for_splunk_account]
pattern = cvefeed_for_splunk_account
methods = POST, GET

[expose:cvefeed_for_splunk_account_specified]
pattern = cvefeed_for_splunk_account/*
methods = POST, GET, DELETE

[expose:cvefeed_for_splunk_settings]
pattern = cvefeed_for_splunk_settings
methods = POST, GET

[expose:cvefeed_for_splunk_settings_specified]
pattern = cvefeed_for_splunk_settings/*
methods = POST, GET, DELETE

[expose:cvefeed_for_splunk_cvefeed_input]
pattern = cvefeed_for_splunk_cvefeed_input
methods = POST, GET

[expose:cvefeed_for_splunk_cvefeed_input_specified]
pattern = cvefeed_for_splunk_cvefeed_input/*
methods = POST, GET, DELETE
