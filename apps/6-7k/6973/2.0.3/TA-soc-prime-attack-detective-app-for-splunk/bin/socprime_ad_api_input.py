import ta_soc_prime_attack_detective_app_for_splunk_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_socprime_ad_api_input as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_socprime_ad_api_input.py
'''
class ModInputsocprime_ad_api_input(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputsocprime_ad_api_input, self).__init__("ta_soc_prime_attack_detective_app_for_splunk", "socprime_ad_api_input", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputsocprime_ad_api_input, self).get_scheme()
        scheme.title = ("SOC Prime Attack Detective App for Splunk")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("attack_detective_api_url", title="Attack Detective API URL",
                                         description="The API URL for integration with the SOC Prime Platform",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("attack_detective_api_key", title="Attack Detective API key",
                                         description="The API key generated when configuring the Data Plane integration on the SOC Prime Platform",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("parallel_jobs_count", title="Parallel Jobs Count",
                                         description="The number of searches that can be run simultaneously",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_rest_api_host_and_port", title="Splunk REST API host and port",
                                         description="May be necessary for remote execution. Format: [\"<splunk_host>:<port>\"]. Default: [\"localhost:8089\"]",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_rest_api_username", title="Splunk REST API username",
                                         description="May be necessary for remote execution",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_rest_api_password", title="Splunk REST API password",
                                         description="May be necessary for remote execution",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_rest_api_token", title="Splunk REST API Token",
                                         description="May be necessary for remote execution",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-soc-prime-attack-detective-app-for-splunk"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputsocprime_ad_api_input().run(sys.argv)
    sys.exit(exitcode)
