import ta_dragos_ot_add_on_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_notifications as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_notifications.py
'''
class ModInputnotifications(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputnotifications, self).__init__("ta_dragos_ot_add_on", "notifications", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputnotifications, self).get_scheme()
        scheme.title = ("Dragos Platform - Notifications")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("global_account", title="Global Account",
                                         description="",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("host_name", title="Hostname",
                                         description="Hostname or IP of the Dragos Platform.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("port_number", title="Port Number",
                                         description="Port number that the Dragos Platform is listening on for HTTP connections. Typically 443 but may vary based on your environment.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("timestamp_bookmark", title="Retrieve events after date",
                                         description="Leave empty to retrieve all events. If you you would like to only retrieve events after a specific date, then please specify the date in ISO 8601 format. For example 1970-01-01T00:00:00Z",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("certificate_file_name", title="Certificate File Name",
                                         description="Certificate chain file in the \"./local/data\" directory of the Add-On to verify the server identity. File name must be alphanumeric, \'_\'\', and \'-\' with a \'.pem extension.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("use_global_proxy", title="Use Global Proxy",
                                         description="Use global proxy in \"Configuration -> Proxy\" for this input. The global proxy must be enabled for this setting to work.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA_dragos_ot_add_on"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        account_fields.append("global_account")
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        checkbox_fields.append("use_global_proxy")
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputnotifications().run(sys.argv)
    sys.exit(exitcode)
