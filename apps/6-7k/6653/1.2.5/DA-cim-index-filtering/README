This app help a splunk admin to refine index filtering for CIM data models

It contains the definitions matching CIM 4.12 in a custom lookup

the app should automatically open a dashboard where you select the period and data models to analyze
after submitting, it will get ran searches directly on raw and compare with currently configured cim filtering then propose suggestion to remove or add index to the current list




