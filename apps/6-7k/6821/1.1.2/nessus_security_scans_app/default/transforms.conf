[whitelisted_vulnerabilities]
filename = whitelisted_vulnerabilities.csv
match_type = WILDCARD(signature), WILDCARD(dest)

[internal_networks]
filename = internal_networks.csv
max_matches = 1
match_type = CIDR(net_range)
