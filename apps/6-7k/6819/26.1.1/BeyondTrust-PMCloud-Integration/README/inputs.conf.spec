[beyondtrust_pm_cloud_rest_api_audit_data://<name>]
pmc_hostname = Be sure to include the '-services' portion of the hostname (ex: if you access the web site at 'mysite.example.com' then the value to enter here would be 'mysite-services.example.com')
pmc_client_id = 
pmc_client_secret = 
pmc_page_size = Audit activity is retrieved from your PM Cloud site in chunks of data (pages).  The page size determines how many records are retrieved by any single request. (default: 200)
connection_time_out =
read_time_out =

[beyondtrust_pm_cloud_rest_api_client_events://<name>]
pmc_hostname = Be sure to include the '-services' portion of the hostname (ex: if you access the web site at 'mysite.example.com' then the value to enter here would be 'mysite-services.example.com')
pmc_client_id = 
pmc_client_secret = 
pmc_batch_size = Events are retrieved from your PM Cloud site in chunks of data.  The batch size determines the size of these chunks.  (default: 1000)
connection_time_out =
read_time_out =