[validation:savedsearch]
# Require url to be set if webhook action is enabled
action.import_maintenance_window = case('action.import_maintenance_window' != "1", null(), 'action.import_maintenance_window.param.title' == "", "No title specified",  1==1, null())