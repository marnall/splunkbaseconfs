[token_cleaner://<name>]
interval = Time interval of the data input, in seconds.
limit = The maximum number of tokens to remove each iteration. (Default: 100)
min_expiry = The min number of seconds since token expired before removal.
