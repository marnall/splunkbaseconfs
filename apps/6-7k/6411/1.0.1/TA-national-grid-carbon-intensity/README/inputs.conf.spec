[national_grid_carbon_intensity_forecast://<name>]
url_path = No changes should be required. If included, "{from}" is replaced with a timestamp on execution.

[national_grid_carbon_intensity_forecast_by_region://<name>]
url_path = No changes should be required. If included, "{from}" is replaced with a timestamp on execution.

[national_grid_carbon_intensity://<name>]
url_path = No changes should be required. If included, "{from}" is replaced with a timestamp on execution.

[national_grid_carbon_intensity_by_region://<name>]
url_path = No changes should be required. If included, "{from}" is replaced with a timestamp on execution.