import ta_soc_prime_ccm_app_for_splunk_declare

import os
import sys
import time
import datetime
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi



import input_module_socprime_tdm_api_input as input_module

bin_dir = os.path.basename(__file__)

'''
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_socprime_tdm_api_input.py
'''
class ModInputsocprime_tdm_api_input(modinput_wrapper.base_modinput.BaseModInput):

    def __init__(self):
        if 'use_single_instance_mode' in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputsocprime_tdm_api_input, self).__init__("ta_soc_prime_ccm_app_for_splunk", "socprime_tdm_api_input", use_single_instance)
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(ModInputsocprime_tdm_api_input, self).get_scheme()
        scheme.title = ("SOC Prime CCM App for Splunk - Optimized")
        scheme.description = ("Go to the add-on\'s configuration UI and configure modular inputs under the Inputs menu.")
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(smi.Argument("name", title="Name",
                                         description="",
                                         required_on_create=True))

        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(smi.Argument("ccm_api_url", title="CCM API URL",
                                         description="The API URL for integration with the SOC Prime Platform.",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("client_secret_id", title="CCM API Key",
                                         description="Copy it from https://tdm.socprime.com/api-access/",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("job_names_list", title="Jobs",
                                         description="Specify the names of Jobs configured in CCM. Format: [\"<job1>\", \"<job2>\", ... ].",
                                         required_on_create=True,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_default_host_port_list", title="Splunk REST API host and port",
                                         description="May be necessary for remote content installation. Format: [\"<splunk_host>:<port>\"]. Default: [\"localhost:8089\"].",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_default_restapi_user", title="Splunk REST API username",
                                         description="May be necessary for remote content installation.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_default_restapi_password", title="Splunk REST API password",
                                         description="May be necessary for remote content installation.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("rule_exception_list", title="Rule exceptions",
                                         description="Optionally, specify rules to exclude from deployment. Format: [\"<rule_name1>\", \"<rule_name2>\", ... ].",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("force_updating_rules", title="Force updating content",
                                         description="Optionally, force synchronization of all savedsearches and parameters from CCM. Format: true/false. By default: true.",
                                         required_on_create=False,
                                         required_on_edit=False))
        scheme.add_argument(smi.Argument("splunk_default_rules_owner", title="Rules Owner",
                                         description="Optionally, assign owner to uploaded rules.",
                                         required_on_create=False,
                                         required_on_edit=False))
        return scheme

    def get_app_name(self):
        return "TA-soc-prime-ccm-app-for-splunk"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, 'global_checkbox_param.json')
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, 'r') as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error('Get exception when loading global checkbox parameter names. ' + str(e))
                self.global_checkbox_fields = []
        return self.global_checkbox_fields

if __name__ == "__main__":
    exitcode = ModInputsocprime_tdm_api_input().run(sys.argv)
    sys.exit(exitcode)
