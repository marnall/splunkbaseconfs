import ta_aiops_sfcc_logs_declare

import os
import sys
import json

import modinput_wrapper.base_modinput
from splunklib import modularinput as smi


import input_module_salesforce_commerce_cloud_ecdn_metrics as input_module

bin_dir = os.path.basename(__file__)

"""
    Do not edit this file!!!
    This file is generated by Add-on builder automatically.
    Add your modular input logic to file input_module_salesforce_commerce_cloud_ecdn_metrics.py
"""


class ModInputsalesforce_commerce_cloud_ecdn_metrics(
    modinput_wrapper.base_modinput.BaseModInput
):
    def __init__(self):
        if "use_single_instance_mode" in dir(input_module):
            use_single_instance = input_module.use_single_instance_mode()
        else:
            use_single_instance = False
        super(ModInputsalesforce_commerce_cloud_ecdn_metrics, self).__init__(
            "ta_aiops_sfcc_logs",
            "salesforce_commerce_cloud_ecdn_metrics",
            use_single_instance,
        )
        self.global_checkbox_fields = None

    def get_scheme(self):
        """overloaded splunklib modularinput method"""
        scheme = super(
            ModInputsalesforce_commerce_cloud_ecdn_metrics, self
        ).get_scheme()
        scheme.title = "Salesforce Commerce Cloud eCDN (Metrics)"
        scheme.description = "Go to the add-on's configuration UI and configure modular inputs under the Inputs menu."
        scheme.use_external_validation = True
        scheme.streaming_mode_xml = True

        scheme.add_argument(
            smi.Argument("name", title="Name", description="", required_on_create=True)
        )
        """
        For customized inputs, hard code the arguments here to hide argument detail from users.
        For other input types, arguments should be get from input_module. Defining new input types could be easier.
        """
        scheme.add_argument(
            smi.Argument(
                "ocapi_credentials",
                title="OCAPI Credentials",
                description="Credentials for the Client ID which will be used to authenticate the add-on against SFCC",
                required_on_create=True,
                required_on_edit=False,
            )
        )
        scheme.add_argument(
            smi.Argument(
                "ocapi_hostname",
                title="Hostname",
                description="",
                required_on_create=True,
                required_on_edit=False,
            )
        )
        scheme.add_argument(
            smi.Argument(
                "ocapi_endpoint",
                title="Endpoint",
                description="Example: /s/-/dw/data/v23_1/log_requests/ecdn",
                required_on_create=True,
                required_on_edit=False,
            )
        )
        scheme.add_argument(
            smi.Argument(
                "zone",
                title="Zone",
                description="Specify eCDN zone name",
                required_on_create=True,
                required_on_edit=False,
            )
        )
        scheme.add_argument(
            smi.Argument(
                "from_datetime",
                title="From datetime",
                description="Start date and time in format \"YYYY-MM-DD'T'HH:MM:SS.SSSZ\". NOTE: adjust to GMT",
                required_on_create=True,
                required_on_edit=False,
            )
        )
        scheme.add_argument(
            smi.Argument(
                "to_datetime",
                title="To datetime",
                description="End date and time in format \"YYYY-MM-DD'T'HH:MM:SS.SSSZ\". NOTE: adjust to GMT",
                required_on_create=False,
                required_on_edit=False,
            )
        )
        scheme.add_argument(
            smi.Argument(
                "filter_pattern",
                title="Filter pattern",
                description="Apply regex pattern to filter the data",
                required_on_create=False,
                required_on_edit=False,
            )
        )
        scheme.add_argument(
            smi.Argument(
                "time_buffer",
                title="Time buffer",
                description="End time buffer in seconds.",
                required_on_create=False,
                required_on_edit=False,
            )
        )
        scheme.add_argument(
            smi.Argument(
                "delta_period",
                title="Delta period",
                description="Split the time period in the given number of seconds",
                required_on_create=False,
                required_on_edit=False,
            )
        )
        return scheme

    def get_app_name(self):
        return "TA-aiops-sfcc-logs"

    def validate_input(self, definition):
        """validate the input stanza"""
        input_module.validate_input(self, definition)

    def collect_events(self, ew):
        """write out the events"""
        input_module.collect_events(self, ew)

    def get_account_fields(self):
        account_fields = []
        account_fields.append("ocapi_credentials")
        return account_fields

    def get_checkbox_fields(self):
        checkbox_fields = []
        return checkbox_fields

    def get_global_checkbox_fields(self):
        if self.global_checkbox_fields is None:
            checkbox_name_file = os.path.join(bin_dir, "global_checkbox_param.json")
            try:
                if os.path.isfile(checkbox_name_file):
                    with open(checkbox_name_file, "r") as fp:
                        self.global_checkbox_fields = json.load(fp)
                else:
                    self.global_checkbox_fields = []
            except Exception as e:
                self.log_error(
                    "Get exception when loading global checkbox parameter names. "
                    + str(e)
                )
                self.global_checkbox_fields = []
        return self.global_checkbox_fields


if __name__ == "__main__":
    exitcode = ModInputsalesforce_commerce_cloud_ecdn_metrics().run(sys.argv)
    sys.exit(exitcode)
