[universal_alert://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.
receive_alert_updates = 

[universal_audit_log://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.

[universal_variable://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.

[universal_node://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.

[universal_link://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.

[universal_session://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.

[universal_node_cve://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.

[universal_health_log://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.

[universal_asset://<name>]
global_account = 
universal_host = 
from_timestamp = Get data from the timestamp passed here, the format is epoch milliseconds
page_size = Data will be split into several pages (requests), each page will contain this maximum quantity of items.
how_assets_are_ingested = Select how assets are ingested: continuous updates in near real time, a one-time import without further updates, or periodic updates based on a user-defined interval in hours.
asset_update_interval_hours = How often asset updates are fetched, expressed in hours. Used only when fixed interval mode is selected.