[crowdstrike_fdr_data_sqs://<name>]
credentials = Select the FDR Credentials that are associated with the SQS queue and S3 bucket being accessed
sqs_queue = Enter the SQS queue URL from the CrowdStrike Falcon UI

[crowdstrike_fdrv2_managedassets_sqs://<name>]
credentials = Select the FDRv2 Credentials that are associated with the SQS queue and S3 bucket being accessed
sqs_queue = Enter the SQS queue URL from the CrowdStrike Falcon UI

[crowdstrike_fdrv2_aidmaster_sqs://<name>]
credentials = Select the FDR Credentials that are associated with the SQS queue and S3 bucket being accessed
sqs_queue = Enter the SQS queue URL from the CrowdStrike Falcon UI

[crowdstrike_fdrv2_notmanaged_sqs://<name>]
credentials = Select the FDR Credentials that are associated with the SQS queue and S3 bucket being accessed
sqs_queue = Enter the SQS Queue URL from the CrowdStrike Falcon UI

[crowdstrike_fdrv2_user_info_sqs://<name>]
credentials = Select the FDR Credentials that are associated with the SQS queue and S3 bucket being accessed
sqs_queue = Enter the SQS queue URL from the CrowdStrike Falcon UI

[crowdstrike_fdrv2_appinfo_sqs://<name>]
credentials = Select the FDR Credentials that are associated with the SQS queue and S3 bucket being accessed
sqs_queue = Enter the SQS queue URL from the CrowdStrike Falcon UI