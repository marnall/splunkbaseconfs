##
## SPDX-FileCopyrightText: 2025 Splunk LLC
## SPDX-License-Identifier: LicenseRef-Splunk-8-2021
##
##

[okta2_app_group_lookup]
external_type = kvstore
collection = okta2_app_assigned_group_lookup
fields_list = app_id,app_name,app_label,group_id

[okta2_user_detail_lookup]
external_type = kvstore
collection = okta2_user_detail_lookup
fields_list = user_id,firstName,lastName,loginName,email,secondEmail,primaryPhone,mobilePhone,state,city,countryCode,zipCode,streetAddress,status,created_time,lastUpdated_time,lastLogin_time,activated_time

[okta2_app_detail_lookup]
external_type = kvstore
collection = okta2_app_detail_lookup
fields_list = id,name,label,created,lastUpdated,status,signOnMode

[okta2_group_detail_lookup]
external_type = kvstore
collection = okta2_group_detail_lookup
fields_list = id,type,name,description,usersCount,appsCount,groupPushMappingsCount,app_id

[okta2_eventtype_lookup]
filename = okta2_eventType_related_info_220.csv

[okta2_group_member_lookup]
external_type = kvstore
collection = okta2_group_member_lookup
fields_list = group_id,group_name,user_id,user_name

[okta2_app_user_lookup]
external_type = kvstore
collection = okta2_app_assigned_user_lookup
fields_list = app_id,app_name,app_label,user_id,user_name

[okta2_system_log_lookup]
filename = okta2_system_log.csv

[REPORT-addonlogs]
DELIMS = "|"
FIELDS = "field1","field2","field3"

[okta_extract_transaction_data]
REGEX = transaction"\s*:\s*\{(\s*[^{}]*(?:\{[^{}]*\}[^{}]*)*)\}(?=,|\s*})
FORMAT = transaction_data::$1

[okta_extract_target_data]
REGEX = target\"\:\s*\[([\S\s]+)\]
FORMAT = target_data::$1

[okta_extract_target_data_first_occurrence]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_data_first_occurrence::$1

[okta_target_data_app_instance]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"AppInstance"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_app_instance_attr::$1

[extract_target_app_instance_id]
SOURCE_KEY = target_app_instance_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppId::$1

[extract_target_app_instance_alternate_id]
SOURCE_KEY = target_app_instance_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppAlternateId::$1

[extract_target_app_instance_display_name]
SOURCE_KEY = target_app_instance_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppDisplayName::$1

[okta_target_data_policy_rule]
SOURCE_KEY = target_data
REGEX= (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"PolicyRule"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_policy_rule_attr::$1

[extract_target_data_policy_rule_id]
SOURCE_KEY = target_policy_rule_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetPolicyRuleId::$1

[extract_target_data_policy_rule_alternate_id]
SOURCE_KEY = target_policy_rule_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetPolicyRuleAlternateId::$1

[extract_target_data_policy_rule_display_name]
SOURCE_KEY = target_policy_rule_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetPolicyRuleDisplayName::$1

[okta_target_data_policy_entity]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"PolicyEntity"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_policy_entity_attr::$1

[extract_target_data_policy_entity_id]
SOURCE_KEY = target_policy_entity_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetPolicyEntityId::$1

[extract_target_data_policy_entity_alternate_id]
SOURCE_KEY = target_policy_entity_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetPolicyEntityAlternateId::$1

[extract_target_data_policy_entity_display_name]
SOURCE_KEY = target_policy_entity_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetPolicyEntityDisplayName::$1

[okta_target_data_app_user]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"AppUser"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_app_user_attr::$1

[extract_target_data_app_user_id]
SOURCE_KEY = target_app_user_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppUserId::$1

[extract_target_data_app_user_alternate_id]
SOURCE_KEY = target_app_user_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppUserAlternateId::$1

[extract_target_data_app_user_display_name]
SOURCE_KEY = target_app_user_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppUserDisplayName::$1

[okta_target_data_user]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"User"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_user_attr::$1

[extract_target_data_user_id]
SOURCE_KEY = target_user_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetUserId::$1

[extract_target_data_user_alternate_id]
SOURCE_KEY = target_user_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetUserAlternateId::$1

[extract_target_data_user_display_name]
SOURCE_KEY = target_user_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetUserDisplayName::$1

[okta_target_data_role]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"Role"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_role_attr::$1

[extract_target_data_role_id]
SOURCE_KEY = target_role_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetRoleId::$1

[extract_target_data_role_alternate_id]
SOURCE_KEY = target_role_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetRoleAlternateId::$1

[extract_target_data_role_display_name]
SOURCE_KEY = target_role_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetRoleDisplayName::$1

[okta_target_data_access_token]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"access_token"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_access_token_attr::$1 targetaccesstokentype::"access_token"

[extract_target_data_access_token_id]
SOURCE_KEY = target_access_token_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetaccesstokenId::$1

[extract_target_data_access_token_display_name]
SOURCE_KEY = target_access_token_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetaccesstokenDisplayName::$1

[okta_target_data_active_directory]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"Active\sDirectory"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_active_directory_attr::$1

[extract_target_data_active_directory_id]
SOURCE_KEY = target_active_directory_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetactivedirectoryId::$1

[extract_target_data_active_directory_display_name]
SOURCE_KEY = target_active_directory_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetactivedirectoryDisplayName::$1

[okta_target_data_id_token]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"id_token"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_id_token_attr::$1 targetidtype::"id_token"

[extract_target_data_id_token_id]
SOURCE_KEY = target_id_token_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetidtokenId::$1

[extract_target_data_id_token_display_name]
SOURCE_KEY = target_id_token_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetidtokenDisplayName::$1

[okta_target_data_refresh_token]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"refresh_token"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_refresh_token_attr::$1 targetrefreshtype::"refresh_token"

[extract_target_data_refresh_token_id]
SOURCE_KEY = target_refresh_token_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetrefreshtokenId::$1

[extract_target_data_refresh_token_display_name]
SOURCE_KEY = target_refresh_token_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetrefreshDisplayName::$1

[okta_target_data_ad_agent]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"AD\sAgent"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = targetADAgent_attr::$1

[extract_target_data_ad_agent_id]
SOURCE_KEY = targetADAgent_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetADAgentId::$1

[extract_target_data_ad_agent_alternate_id]
SOURCE_KEY = targetADAgent_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetADAgentAlternateId::$1

[extract_target_data_ad_agent_display_name]
SOURCE_KEY = targetADAgent_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetADAgentDisplayName::$1

[okta_target_data_app_group]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"AppGroup"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_app_group_attr::$1

[extract_target_data_app_group_id]
SOURCE_KEY = target_app_group_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppGroupId::$1

[extract_target_data_app_group_alternate_id]
SOURCE_KEY = target_app_group_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppGroupAlternateId::$1

[extract_target_data_app_group_display_name]
SOURCE_KEY = target_app_group_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetAppGroupDisplayName::$1

[okta_target_data_user_group]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"UserGroup"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_user_group_attr::$1

[extract_target_data_user_group_id]
SOURCE_KEY = target_user_group_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetUserGroupId::$1

[extract_target_data_user_group_alternate_id]
SOURCE_KEY = target_user_group_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetUserGroupAlternateId::$1

[extract_target_data_user_group_display_name]
SOURCE_KEY = target_user_group_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetUserGroupDisplayName::$1

[okta_target_data_group_push_mapping]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"GroupPushMapping"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_group_push_mapping_attr::$1

[extract_target_data_group_push_mapping_id]
SOURCE_KEY = target_group_push_mapping_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetGroupPushMappingId::$1

[extract_target_data_group_push_mapping_alternate_id]
SOURCE_KEY = target_group_push_mapping_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetGroupPushMappingAlternateId::$1

[extract_target_data_group_push_mapping_display_name]
SOURCE_KEY = target_group_push_mapping_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetGroupPushMappingDisplayName::$1

[okta_target_data_resource_set]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"ResourceSet"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_resource_set_attr::$1

[extract_target_data_resource_set_id]
SOURCE_KEY = target_resource_set_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetResourceSetId::$1

[extract_target_data_resource_set_alternate_id]
SOURCE_KEY = target_resource_set_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetResourceSetAlternateId::$1

[extract_target_data_resource_set_display_name]
SOURCE_KEY = target_resource_set_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetResourceSetDisplayName::$1

[okta_target_data_resource_request]
SOURCE_KEY = target_data
REGEX = (\{(?:[^{}]|\{[^{}]*\})*?"type"\s*:\s*"ResourceRequest"(?:[^{}]|\{[^{}]*\})*?\})
FORMAT = target_resource_request_attr::$1

[extract_target_data_resource_request_id]
SOURCE_KEY = target_resource_request_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"id"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetResourceRequestId::$1

[extract_target_data_resource_request_alternate_id]
SOURCE_KEY = target_resource_request_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"alternateId"\s*:\s*(?:"unknown"|"([^"]+)"|null)(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetResourceRequestAlternateId::$1

[extract_target_data_resource_request_display_name]
SOURCE_KEY = target_resource_request_attr
REGEX = \{(?:[^{}]|\{[^{}]*\})*?"displayName"\s*:\s*(?:"unknown"|"([^"]*)"|null|"")(?:[^{}]|\{[^{}]*\})*?\}
FORMAT = targetResourceRequestDisplayName::$1
