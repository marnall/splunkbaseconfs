[dx_search_tier]
definition = host=*

[dx_user_filter]
definition = | search [ | rest /services/authentication/current-context splunk_server=local | fields username, roles | eval owner = IF(MATCH(roles, "^(?:admin|sc_admin|cluster_admin)$"), "*", username) | fields owner ]
