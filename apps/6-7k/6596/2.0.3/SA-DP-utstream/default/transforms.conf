[utstream_replay_results]
collection = utstream_replay_results
external_type = kvstore
fields_list = _key, instance, index, host, sourcetype, filename, cribl_source, cribl_size, cribl_collector, cribl_discovery_job, cribl_replay_job, earliest, latest, user, creation_time, run_time, queue_job, alert_action

[utstream_replay_jobs]
collection = utstream_replay_jobs
external_type = kvstore
fields_list = _key, instance, index, host, sourcetype, filename, cribl_source, cribl_size, cribl_collector, cribl_discovery_job, earliest, latest, user, creation_time, queue_job, alert_action

[utstream_discovery_results]
collection = utstream_discovery_results
external_type = kvstore
fields_list = _key, instance, index, host, sourcetype, filename, earliest, latest, cribl_source, cribl_size, cribl_collector, cribl_discovery_job, cribl_already_replay, alert_action

[utstream_discovery_inventory]
collection = utstream_discovery_inventory
external_type = kvstore
fields_list = _key, instance, index, host, sourcetype, user, creation_time, earliest, latest, start_time, end_time, cribl_collector, cribl_discovery_job, alert_action

[utstream_discovery_jobs]
collection = utstream_discovery_jobs
external_type = kvstore
fields_list = _key, instance, cribl_collector, index, host, sourcetype, user, creation_time, earliest, latest, alert_action

[backup_utstream_discovery_inventory]
collection = backup_utstream_discovery_inventory
external_type = kvstore
fields_list = backup_time, instance, index, host, sourcetype, user, creation_time, earliest, latest, start_time, end_time, cribl_collector, cribl_discovery_job, alert_action

[backup_utstream_discovery_results]
collection = backup_utstream_discovery_results
external_type = kvstore
fields_list = backup_time, instance, index, host, sourcetype, filename, earliest, latest, cribl_source, cribl_size, cribl_collector, cribl_discovery_job, cribl_already_replay, alert_action

[backup_utstream_replay_results]
collection = backup_utstream_replay_results
external_type = kvstore
fields_list = backup_time, instance, index, host, sourcetype, filename, cribl_source, cribl_size, cribl_collector, cribl_discovery_job, cribl_replay_job, earliest, latest, user, creation_time, run_time, queue_job, alert_action
