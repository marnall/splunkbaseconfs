1. About

Optimis for Splunk is an application whose aim is to allows the Mixcom OptimIS appliance to export flows and statistics information to Splunk® Enterprise or Splunk® Cloud. With this application a Spunk owner can dispose of the analysis results done by Optimis directly in his Splunk implementation to :

View builtin reports of selected analysis corresponding to a measurement done from a specific context point by the OptimIS solution :

        ⁃    TCP / UDP Transactions: displays the statistics of TCP exchanges.
        ⁃    HTTP Transactions: displays HTTP transactions along with the various error codes and response times per transaction.
        ⁃    802.11 Activity: displays the statistics of radio exchanges between stations and wifi hotspots.

Enrich data at disposal of a Splunk implementation to consolidate information and permit best decisions making


The source data is collected from on-premises infrastructure network by the Optimis system at various probe locations. Then they are analyzed and aggregated before being sent to the Splunk infrastructure for indexing and presentation by Optimis for Splunk.

The data sent by OptimIS service to Splunk is indexed by the Splunk application following a documented structure.


2. Possible use cases

Receive relevant information related to an ongoing troubleshooting or analysis from the OptimIS collection and analysis system. Then present it as a dashboard to monitor the behaviour of a customer applications or devices.

The IT team of the customer and the Mixcom team can use the dashboards and reports already created in “OptimIS for Splunk” to visualize default information about relevant phenomena (flows, hosts, protocols, transactions, errors) like:

	• Flow statistics: Top Sender, Receiver, Port, bandwidth and other
	• HTTP , TCP statistics ...
	• Transaction response time
	• Radio wireless frame (802.11) statistics

or create new report or dashboard to highlight specific informations of interest. Alarms can also be set for specifics behaviours and generate events for Splunk or OptimIS.

3.  Requirements

	Being a customer of Mixcom services.
	This version of the App is compatible with Splunk® Enterprise or Splunk® Cloud.
	Adding a new index called "optimis" to the Splunk Enterprise or Splunk Cloud configuration.

4. Installation

4.1. Splunk Enterprise:

	When using Splunk Enterprise, you can install the Optimis for Splunk app from the Splunkbase. You can also install it as follows:


	cd SPLUNK_HOME/etc/apps/
	wget .../optimis.tar.gz
	tar -xzf optimis.tar.gz
	$SPLUNK_HOME/bin/splunk restart

	PS: the path of Splunk must be defined as an environment variable.
	
	Setup after installation - Splunk Enterprise:
		a. The OptimIS appliance send data to Splunk indexer over 4444 TCP port using Universal Splunk Forwarder. To configure this input data, please create the file $ SPLUNK_HOME/etc/apps/ Optimis/local/inputs.conf and add the following lines to it:

		[splunktcp://4444]
		disabled = 0
		connection_host = ip

		b. The "Optimis for splunk" application expects the index of events sent from the Optimis appliance to be set to "optimis". This index is used to optimize the data search performance for this application, please add a new index named "optimis" associated to “Optimis for Splunk” application in your Splunk configuration by creating the $SPLUNK_ROOT/etc/apps/Optimis/local/indexes.conf file, and adding the following lines to it: 

		[optimis] 
		coldPath = $SPLUNK_DB/optimis/colddb
		homePath = $SPLUNK_DB/optimis/db
		thawedPath = $SPLUNK_DB/optimis/thaweddb

		c. Restart splunk for the configuration changes to take effect


	Splunk is now listening for data from Optimis appliance on port 4444.  

4.2. Splunk Cloud:
	When using Splunk Cloud, you must install the OptimIS for Splunk app from the Splunkbase.

	Setup after installation - Splunk Cloud:
		a. The Splunk universal forwarder credentials package must be sent to Mixcom Team. It will be used to forward data from Splunk universal forwarder installed on OptimIS system.

		b. To optimize the data search performance for this application, please add the index named "optimis" associated to “Optimis for Splunk” application in your Splunk configuration.


5. Troubleshooting

6. Contact
Please send an email at support@mixcom.fr if you have any issues.

email: support@mixcom.fr

7. Release Notes
Version 1.0.0
November 5, 2021

