[cli]
* This stanza defines saml_cli_auth CLI parameters

base_url = <string>
* Used to override detection of hostname/port for URL generation

[web]
* This stanza defines saml_cli_auth Web parameters

time_before_cleanup = <int>
* This is the number of seconds to wait before removing the temporary file
