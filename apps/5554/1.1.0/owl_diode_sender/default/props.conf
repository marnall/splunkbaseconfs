# Define sourcetypes, sources or host event to send across the diode, or specify
# `default` to send everything.
#
[default]
TRANSFORMS-diode-1-rewrite=add_host, add_source, add_sourcetype, add_time, add_index
TRANSFORMS-diode-2-outputs=send-to-syslog-tcp
