[indicator_metadata]
collection = indicator_metadata
external_type = kvstore
fields_list = search_name, entity_field, fields_of_interest, _time, schedule_mode, drilldown_search

[scoring_search_metadata]
collection = scoring_search_metadata
external_type = kvstore
fields_list = search_name, entity_field, _time, schedule_mode, drilldown_search, drilldown_indicator, search_type, baseline_type, baseline_name
