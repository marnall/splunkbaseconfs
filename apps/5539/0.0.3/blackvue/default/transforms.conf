[latitude]
INGEST_EVAL = latitude = (latitude_degrees + (latitude_minutes/60)) * if(northsouth=="N",1,-1)

[longitude]
INGEST_EVAL = longitude = (longitude_degrees + (longitude_minutes/60)) * if(eastwest=="E",1,-1)