# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 3

[launcher]
author = Rapid7 LLC 
version = 0.6.0
description = Collect events from tCell web application security service, and integrate actions via adaptive response.

[ui]
is_visible = 1
label = tcell_splunk_app
docs_section_override = AddOns:released

[package]
id = TA-tcell_splunk_app

[triggers]
reload.addon_builder = simple
reload.ta_tcell_splunk_app_account = simple
reload.ta_tcell_splunk_app_settings = simple
reload.passwords = simple

