# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 4

[launcher]
author = Proofpoint Inc
description = Proofpoint on demand email security Add-on for Splunk
version = 2.7.0

[ui]
is_visible = 1
label = Proofpoint On Demand Email Security Add On
docs_section_override = AddOns:released

[package]
id = TA-pps_ondemand

[triggers]
reload.addon_builder = simple
reload.ta_pps_ondemand_account = simple
reload.ta_pps_ondemand_settings = simple
reload.passwords = simple

