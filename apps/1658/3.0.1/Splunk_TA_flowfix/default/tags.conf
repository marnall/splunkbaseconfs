[eventtype=netflow]
network = enabled
communicate = enabled

[eventtype=netflow_session_start]
network = enabled
session = enabled
start = enabled

[eventtype=netflow_session_end]
network = enabled
session = enabled
end = enabled
