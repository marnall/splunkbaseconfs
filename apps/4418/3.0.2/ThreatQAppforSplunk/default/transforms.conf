[master_lookup]
collection = master_lookup
external_type = kvstore
fields_list = ioc_id, status, type, ioc_value, _key, score, updated_at, sources, adversaries, index_time, malware_family

[threatq_indicator_types]
collection = threatq_indicator_types
external_type = kvstore
fields_list = id, name, _key

[threatq_indicator_status]
collection = threatq_indicator_status
external_type = kvstore
fields_list = id, name, _key

[threatq_retired_indicators]
collection = threatq_retired_indicators
external_type = kvstore
fields_list = ioc_id, status, type, ioc_value, _key, score, updated_at, sources, adversaries

[threatq_matched_indicators]
collection = threatq_matched_indicators
external_type = kvstore
fields_list = ioc_id, status, type, ioc_value, _key, score, updated_at, match_time, first_seen, last_seen, match_count, sources, adversaries, sid, last_run_first_seen, last_run_last_seen, last_run_match_count, malware_family, datamodel_name, raw_event

[threatq_retired_matched_indicators]
collection = threatq_retired_matched_indicators
external_type = kvstore
fields_list = ioc_id, status, type, ioc_value, _key, score, updated_at, match_time, first_seen, last_seen, match_count, sources, adversaries, sid, last_run_first_seen, last_run_last_seen, last_run_match_count

[temp_iocs_lookup]
batch_index_query = 0
case_sensitive_match = 1
filename = temp_iocs_lookup.csv