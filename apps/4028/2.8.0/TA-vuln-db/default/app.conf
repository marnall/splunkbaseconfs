# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = false
state = enabled
build = 3

[launcher]
author = Flashpoint
version = 2.8.0
description = VulnDB Flashpoint Add-on for Splunk collects the data from VulnDB Flashpoint portal using APIs and ingests it into Splunk.

[ui]
is_visible = 1
label = VulnDB Flashpoint Add-on for Splunk
docs_section_override = AddOns:released

[package]
id = TA-vuln-db

[triggers]
reload.addon_builder = simple
reload.ta_vuln_db_settings = simple