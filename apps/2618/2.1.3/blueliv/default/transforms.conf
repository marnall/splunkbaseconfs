#
# transforms file
#

#BOTS

[botips_lookup]
external_type = kvstore
collection = botips
fields_list = _key, botip, url, botnetip, type, portalurl, portaldomain, port, operatingsystem, country, city, asn, lat, lon, seenAt, createdAt

[bl_bot_ip_lookup]
filename  = botips.csv
fields_list = botip, count

[bl_bot_botnetip_lookup]
filename  = botbotnetips.csv
fields_list = botnetip, count

[bl_bot_type_lookup]
filename  = bottypes.csv
fields_list = type, count

[bl_bot_portaldomain_lookup]
filename  = botportaldomains.csv
fields_list = portaldomain, count

[bl_bot_operatingsystem_lookup]
filename  = botoperatingsystems.csv
fields_list = operatingsystem, count

[bl_bot_portalurl_lookup]
filename  = botportalurl.csv
fields_list = portalurl, count

[bl_bot_country_lookup]
filename  = botcountry.csv
fields_list = country, count

[bl_bot_map_lookup]
filename  = botmap.csv
fields_list = lat, lon, type


#ATTACKS

[attackips_lookup]
external_type = kvstore
collection = attackips
fields_list = _key,attackType,firstEvent,lastEvent,numEvents,sourceip,sourcecountry,sourcecity,sourceport,sourcelatitude,sourcelongitude,destinationport,destinationserviceName,destinationcountry,destinationlatitude,destinationlongitude,createdAt

[bl_attack_map_lookup]
filename  = attackmap.csv
fields_list = sourcelatitude, sourcelongitude, attackType

[bl_attack_ip_types_lookup]
filename = attacktypes.csv
fields_list = attackType, count

[bl_attack_ip_lookup]
filename  = attackips.csv
fields_list = sourceip, count

#MALWARES

[malwares_lookup]
external_type = kvstore
collection = malwares
fields_list = _key,filename,contentType,md5,sha1,sha256,analyzedAt,firstSeenAt,fileType,fileSize,malwareType,confidence,architecture

[bl_malware_types_lookup]
filename = malwaretypes.csv
fields_list = malwareType, count

[bl_malware_confidence_lookup]
filename  = malwareconfidence.csv
fields_list = confidence, count

[bl_malware_contentType_lookup]
filename  = malwarecontentType.csv
fields_list = contentType, count


#CRIMESERVERS

[crimeservers_lookup]
external_type = kvstore
collection = crimeservers
fields_list = _key, url, domain, host, type, subtype, ip, asn, lat, lon, status, country, firstSeenAt, lastSeenAt

[bl_ip_lookup]
filename  = ips.csv
fields_list = ip, count

[bl_domain_lookup]
filename  = domains.csv
fields_list = domain, count

[bl_host_lookup]
filename  = hosts.csv
fields_list = host, count

[bl_url_lookup]
filename  = urls.csv
fields_list = url, count

[bl_map_lookup]
filename  = map.csv
fields_list = type, lat, lon

[bl_country_lookup]
filename  = country.csv
fields_list = country, count

[bl_type_lookup]
filename  = types.csv
fields_list = type, count

[bl_subtype_lookup]
filename  = subtypes.csv
fields_list = subtype, count

[bl_asn_lookup]
filename  = asns.csv
fields_list = asn, count

[bl_status_lookup]
filename  = status.csv
fields_list = status, count