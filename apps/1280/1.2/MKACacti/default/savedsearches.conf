[Avg Processing Time by Host last 24h]
action.email.reportServerEnabled = 0
alert.track = 0
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index=cacti_system "SYSTEM STATS:" | timechart span=1h avg(ProcessingTime) by host
vsid = h9bgejbp

[Avg Num RRDs Processed by Host last 24h]
action.email.reportServerEnabled = 0
alert.track = 0
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index=cacti_system "SYSTEM STATS:" | timechart span=1h avg(RRDsProcessed) by host
vsid = h9bgfy2d

[Avg Hosts to RRDs Processed last 24h]
action.email.reportServerEnabled = 0
alert.track = 0
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index=cacti_system "SYSTEM STATS:" | timechart span="5m" avg(NumHosts) as TotalHosts, avg(RRDsProcessed) as RRDs by host
vsid = h9bgo17u

[Advanced Ping - Loss Avg Min Max last 24h]
action.email.reportServerEnabled = 0
alert.track = 0
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index=cacti_perf www NOT advanced | dedup target_host | sort - loss | rename target_host as Node, loss as loss(ms), max as max(ms), min as min(ms), avg as avg(ms) | table Node, loss(ms), max(ms), min(ms), avg(ms)
vsid = h9bqz7it

[Number Errors by Host last 24h]
action.email.reportServerEnabled = 0
alert.track = 0
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index=cacti_system error | timechart span=1h count by host
vsid = h9bfbowi

[Polling failure - gethostbyname last 24h]
action.email.reportServerEnabled = 0
alert.track = 0
dispatch.earliest_time = -24h@h
dispatch.latest_time = now
displayview = flashtimeline
request.ui_dispatch_view = flashtimeline
search = index=cacti_system gethostbyname failed | timechart count by host
vsid = h9bez0bl
