# this add-on is powered by splunk Add-on builder
[install]
is_configured = 0
state = enabled
build = 2

[launcher]
author = Darren Dance
version = 1.0.1
description = This TA reduces the size of certain windows events by removing the description from the end that is repeated on each event and adds little to no value.

[ui]
is_visible = 0
label = windows_eventsize_reducer
docs_section_override = AddOns:released

[package]
id = TA-windows_eventsize_reducer
