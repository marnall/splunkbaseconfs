[package]
id = TA-dns_protocol_translate

[install]
is_configured = false
state = enabled

[ui]
is_visible = false
label = DNS Protocol Translation External Lookup

[launcher]
author=Hurricane Labs
description=This TA provides a lookup called protocol_translation_lookup that allows you to lookup DNS RDATA automatically based on type.
version=1.0.1
