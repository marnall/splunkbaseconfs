[silk_vendor_info_lookup]
filename = silk_vendor_info.csv

[silk_protocol_lookup]
filename = silk_protocols.csv

[SiLKRmHeader]
REGEX = ^\s+sIP\|
DEST_KEY = queue
FORMAT = nullQueue

[SiLK_log_extractions]
DELIMS = "|"
FIELDS = "src_ip", "dest_ip", "src_port", "dest_port", "protocol_id", "packets", "bytes", "tcp_flag", "start_time", "duration", "end_time", "dvc" 
