# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 7

[launcher]
author = Dmytro Sobolta
version = 1.0.0
description = This app allows you to send data to an MQTT broker

[ui]
is_visible = 1
label = Send to MQTT
docs_section_override = AddOns:released

[package]
id = TA-send-to-mqtt

[triggers]
reload.addon_builder = simple
reload.ta_send_to_mqtt_account = simple
reload.ta_send_to_mqtt_settings = simple
reload.passwords = simple

