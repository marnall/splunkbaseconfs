# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = true
is_configured = 0
state = enabled
build = 10

[launcher]
author = Julien Ruaux
version = 1.2.0
description = This add-on provides inputs to access logs, metrics, configurations, and alerts from Redis Enterprise

[ui]
is_visible = 1
label = Redis Enterprise
docs_section_override = AddOns:released

[package]
id = TA-redis-enterprise

[triggers]
reload.addon_builder = simple
reload.ta_redis_enterprise_account = simple
reload.ta_redis_enterprise_settings = simple
reload.passwords = simple

