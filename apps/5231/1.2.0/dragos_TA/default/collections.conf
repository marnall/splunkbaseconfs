# replaces the ioc pull status
# plus contains the run interval
[dragos_ioc_cache_status]
replicate = false

# last n number of rus
# each run contains timestamp, status, number imported etc
# also have rest func that qyeries most recent to report error (should maybe be in cache status?)
[dragos_ioc_cache_history]
replicate = false

# the action list of IOCs
[dragos_iocs]
replicate = true
accelerated_fields.ioc_value = {"value": 1}
accelerated_fields.ioc_type_value = {"value": 1, "indicator_type": 1}

# can be used to manually remove IOCs from the list
[dragos_ioc_inactive_list]
replicate = false