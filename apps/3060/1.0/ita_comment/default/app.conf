#
# Splunk app configuration file
#

[install]
is_configured = 0
build = 0

[ui]
is_visible = 1
label = Comments

[launcher]
author = Itility B.V.
description = Adds a custom (streaming) search command to create notes and comments inside the Splunk query.
version = 1.0

[package]
id = ita_comment