# this add-on is powered by splunk Add-on builder
[install]
state_change_requires_restart = false
is_configured = 0
state = enabled
build = 3

[launcher]
version = 1.0.2
description = This technical add-on is designed to normalize Tines data at both search time and index time in Splunk Enterprise. In addition to these features, it also incorporates customized webhook alert actions for enhanced usability and functionality.

[ui]
is_visible = 1
label = tines
docs_section_override = AddOns:released

[package]
id = TA-tines

[triggers]
reload.addon_builder = simple
reload.ta_tines_account = simple
reload.ta_tines_settings = simple
reload.passwords = simple

